• This is a read-only adversarial security review task. It's a substantial review task with many files. The unit is "U3" though the text confusingly says "What U1 is" — the prompt mixes U1/U3 descriptions. The prompt says review the whole unit, files listed include VaultState.kt, DecoyAuthStore.kt, DecoyIdentity.kt, DecoyRelayApi.kt, DecoyAccountProvisioner.kt, DecoyCounterReservation.kt, plus test files. But also the round-4 fix items talk about MessagingCoordinator, teardown, quiesce, stop(), cover(), DecoySendPairingTest — that's U3 (decoy sending/pairing). The prompt is a mix: "Unit U3 — ROUND 4" but scope lists U1 provisioning files and also the pairing/teardown machinery.

  So I need to find all relevant files: the U1 files listed, plus the U3 pairing code (MessagingCoordinator, DecoySendPairingTest, cover(), quiesce, stop(), UnlockController, terminalTeardownDone, etc.).

  This is a big review task. Delegating to explore/coder subagents makes sense. But findings quality matters — adversarial review requires deep reasoning. I can delegate file reading/gathering to subagents and do the analysis myself, or delegate parallel investigations.

  Strategy: spawn a few explore agents to gather the code and docs:
  1. U1 provisioning files (read fully).
  2. U3 pairing machinery: MessagingCoordinator, cover pipeline, stop/quiesce/teardown, DecoySendPairingTest.
  3. Spec + invariant table + VAULT_ARCHITECTURE.
  4. Test files.

  Actually since it's read-only review, explore agents can read. But findings need careful cross-referencing. Perhaps better: I do the core reading myself of the security-critical code, delegating doc reading to subagents to summarize. However, the instructions say delegating keeps bulk out of context — but for an adversarial review I need the actual code. The reviewers' findings must be precise with file:line.

  Given the volume (7+ source files, 5 test files, 3 docs), I'll delegate to parallel explore agents that each return full technical details, then I verify suspicious points myself with targeted reads.

  Let me first locate files. Let me do a quick Glob for the U3 machinery files.

  Plan:
  - Glob/Grep for MessagingCoordinator, DecoySendPairingTest, quiesce, terminalTeardownDone, DecoyEnvelopeBuilder, cover(, UnlockController, DecoyAuthStore, etc.
  - Then launch parallel explore agents:
    A: U1 provisioning unit — read VaultState.kt, DecoyAuthStore.kt, DecoyIdentity.kt, DecoyRelayApi.kt, DecoyAccountProvisioner.kt, DecoyCounterReservation.kt, plus their tests; report full contents/structure with line numbers, looking at crash points, zeroing, codec, capacity, registration budget.
    B: U3 pairing machinery — MessagingCoordinator confinement, teardown, quiesce, stop() 250ms, terminalTeardownDone, cover sites, DecoySendPairingTest.
    C: Docs — DECOY_TRAFFIC_0.10.0_SPEC.md §2.3/§4/§4.2/§6.2a, invariant table, VAULT_ARCHITECTURE.md §3–§8; report the invariants verbatim enough to check against code.

  Then I verify claims with targeted reads before writing findings.

  The explore agents should be told to return detailed verbatim-ish summaries with line numbers since I'll write findings based on them and verify hotspots myself.

  Let me start with discovery in parallel.

• I'll start by locating the U3 machinery and the unit's files, then read the code and required docs in parallel.

• This is a huge review. Let me plan delegation. I'll use parallel explore agents to gather detailed content, but the review itself needs my judgment. Key code areas:

  1. U3 pairing machinery: `DecoySendPairing.kt`, `MessagingCoordinator.kt`, `DecoyEnvelopeBuilder.kt`, `ZitroneApp.kt` (teardown dispatch), `MainActivity.kt`, `UnlockController` (find file), `DecoySendPairingTest.kt`.
  2. U1 provisioning: VaultState.kt, DecoyAuthStore.kt, DecoyIdentity.kt, DecoyRelayApi.kt, DecoyAccountProvisioner.kt, DecoyCounterReservation.kt + tests (DecoyAccountProvisionerTest, VaultDecoySectionTest, DecoyEnvelopeBuilderTest, DecoyAuthStoreTest, VaultStateCodecTest?).

  Wait — the five test files added by "this unit": the prompt seems to mix U1 and U3 prompts. It says "All five test files under apps/android/app/src/test/java/com/zitrone/app/ added by this unit." The decoy tests: DecoySendPairingTest, DecoyAccountProvisionerTest, VaultDecoySectionTest, DecoyEnvelopeBuilderTest, DecoyAuthStoreTest — that's five. Plus possibly DecoyCounterReservation test? Not present apparently.

  3. Docs: spec, invariant table, VAULT_ARCHITECTURE.md §3–8.
  4. Also §4.3 and §5 of... probably the spec. "Check §4.3 and §5 against the code."

  Also "u3-fix-r4-composed.md" describes the fix — worth reading.

  Given size, I'll delegate in parallel:

  - Agent A (explore, thorough): U1 codec + provisioning files full read with line numbers: VaultState.kt, DecoyAuthStore.kt, DecoyIdentity.kt, DecoyRelayApi.kt, DecoyAccountProvisioner.kt, DecoyCounterReservation.kt. Report structure + verbatim key code + line numbers, focusing on: register-before-commit ordering, crash points, zeroing, isEmpty, strict codec, capacity budget, mutation discipline, presence≠readiness, registration budget/429 backoff.
  - Agent B (explore, thorough): U3 pairing machinery: DecoySendPairing.kt, MessagingCoordinator.kt (full), ZitroneApp.kt teardown, MainActivity, UnlockController, quiesce path. Report: confinement claim, suspension points between ws.sendMessage and inFlight.add, the three `if (publish…) cover(…)` sites, stop() 250ms, terminalTeardownDone re-entry, quiesce mid-drain reconnect.
  - Agent C (explore, medium): docs: spec §2.3/§4/§4.2/§4.3/§5/§6.2a, u1-invariant-table.md, VAULT_ARCHITECTURE.md §3–8, u3-fix-r4-composed.md summary. Report key claims verbatim.
  - Agent D (explore, thorough): the five test files + DecoySendPairingTest — for each test, what it pins and whether it would pass if broken.

  Then I verify hotspots myself by reading key files directly (I'll likely read VaultState.kt and DecoySendPairing.kt and MessagingCoordinator.kt myself too since precision matters — but that duplicates. Better: rely on agents for bulk, and I re-read specific suspicious regions before finalizing findings.)

  Actually, for an adversarial review I think I should read the core security files myself — the findings need exact file:line and exact sequences. Subagent summaries can mislead. But the volume is large. Compromise: delegate the docs + tests to subagents, read the main code myself. Main code: VaultState.kt (likely big), DecoySendPairing.kt, MessagingCoordinator.kt, DecoyEnvelopeBuilder.kt, DecoyAuthStore.kt, DecoyIdentity.kt, DecoyRelayApi.kt, DecoyAccountProvisioner.kt, DecoyCounterReservation.kt. That's maybe 3000-5000 lines total. Manageable if I read the critical ones fully and skim others.

  Let me check file sizes first, then decide.  1068 apps/android/app/src/main/java/com/zitrone/app/crypto/vault/VaultState.kt
   202 apps/android/app/src/main/java/com/zitrone/app/data/DecoyAuthStore.kt
   663 apps/android/app/src/main/java/com/zitrone/app/decoy/DecoyAccountProvisioner.kt
   639 apps/android/app/src/main/java/com/zitrone/app/decoy/DecoyEnvelopeBuilder.kt
   212 apps/android/app/src/main/java/com/zitrone/app/decoy/DecoyIdentity.kt
   150 apps/android/app/src/main/java/com/zitrone/app/decoy/DecoyRelayApi.kt
   658 apps/android/app/src/main/java/com/zitrone/app/decoy/DecoySendPairing.kt
  2619 apps/android/app/src/main/java/com/zitrone/app/MessagingCoordinator.kt
  1225 apps/android/app/src/test/java/com/zitrone/app/DecoyAccountProvisionerTest.kt
   237 apps/android/app/src/test/java/com/zitrone/app/DecoyAuthStoreTest.kt
   889 apps/android/app/src/test/java/com/zitrone/app/DecoyEnvelopeBuilderTest.kt
  1271 apps/android/app/src/test/java/com/zitrone/app/DecoySendPairingTest.kt
   621 apps/android/app/src/test/java/com/zitrone/app/VaultDecoySectionTest.kt
  1229 docs/design/DECOY_TRAFFIC_0.10.0_SPEC.md
   773 l00prite/.l00prite/reviews/decoy-0.10.0/u1-invariant-table.md
   402 docs/VAULT_ARCHITECTURE.md
   128 l00prite/.l00prite/reviews/decoy-0.10.0/u3-fix-r4-composed.md
 12986 total
apps/android/app/src/main/java/com/zitrone/app/data/:
AttachmentControlPayload.kt
AuthStore.kt
BiometricUnlockStore.kt
ConnectionMode.kt
ControlPayload.kt
ConversationRepository.kt
DecoyAuthStore.kt
DeviceSettings.kt
LemonDropCreator.kt
LemonDropRedeemer.kt
LemonDropScanOutcome.kt
LemonDropVeil.kt
MessageEnvelope.kt
MessageRepository.kt
Models.kt
PrivacyViewSettings.kt
QrDropLink.kt
RosterStore.kt
SettingsRepository.kt
VaultRosterStore.kt
VaultScopedSettings.kt
VaultUsePrefsWipe.kt

apps/android/app/src/main/java/com/zitrone/app/decoy/:
DecoyAccountProvisioner.kt
DecoyEnvelopeBuilder.kt
DecoyIdentity.kt
DecoyRelayApi.kt
DecoySendPairing.kt
Note: `DecoyCounterReservation.kt` doesn't exist in decoy/ — the prompt lists it but the directory shows only 5 files. Check where DecoyCounterReservation lives — maybe inside DecoyAccountProvisioner.kt or SignalProtocolManager. Grep for it.

  Total main-code to read: ~10k lines including tests and docs. This is too much for me to read everything carefully in full. Strategy: delegate docs + tests + U1 provisioning to subagents with instructions for detailed reporting; I read the U3 hot path myself (DecoySendPairing.kt 658, MessagingCoordinator.kt relevant parts of 2619, ZitroneApp teardown, UnlockController stop path). Also read VaultState.kt codec section myself (it's contract-canonical).

  Plan:
  - Agent A (explore thorough): U1 files (VaultState.kt, DecoyAuthStore.kt, DecoyIdentity.kt, DecoyRelayApi.kt, DecoyAccountProvisioner.kt + DecoyCounterReservation wherever it is) — detailed report with verbatim snippets and line numbers against the 9 invariants.
  - Agent B (explore thorough): tests — the 5 test files + how the claims map; report per-test what it pins and weaknesses.
  - Agent C (explore medium): docs — spec sections, invariant table, VAULT_ARCHITECTURE, u3-fix-r4-composed.md — report claims verbatim.
  - Me: read DecoySendPairing.kt fully, MessagingCoordinator.kt (relevant), ZitroneApp.kt teardown, UnlockController stop, quiesce.

  Then verify hotspots from agents' reports myself.

  Launch the three agents in parallel, then read my files while... actually tool calls are sequential per turn — if I launch agents in foreground I wait. I can launch them in one message (parallel foreground) and then read my files in subsequent turns. Foreground parallel agents: they run concurrently and I get results back. Then I do my own reading. That works — launch 3 agents in one block.Now I have three detailed reports. I need to do my own verification of the U3 hot path (items 1–7 in the prompt): DecoySendPairing.kt (658 lines), MessagingCoordinator.kt (relevant parts of 2619), ZitroneApp.kt teardown wiring, UnlockController stop path, quiesce. Let me read DecoySendPairing.kt fully myself, and the relevant MessagingCoordinator sections, plus ZitroneApp wiring and UnlockController.

  Also need to verify key claims:
  1. No suspension point between ws.sendMessage and inFlight.add — but wait, round 4 changed to build-then-admit: cover() builds the frame BEFORE admitting to the register. The claim: "with no suspension point between ws.sendMessage and inFlight.add". Build-then-admit means build happens between sendMessage and admission. The build is non-suspending presumably. Need to check the actual call chain: publish tail returns Boolean; then `if (publish…) cover(…)`; inside cover: buildCover (non-suspending?), then inFlight.add, then launch with gap sleep.

  2. Teardown dispatched onto same worker in every path including deleteAccountAndWipe.

  3. The three `if (publish…) cover(…)` sites in MessagingCoordinator (lines ~1210, 1422, 1574 per agent-0's grep).

  4. quiesce correctness vs mid-drain reconnect.

  5. stop() 250ms.

  6. terminalTeardownDone re-entry.

  7. Tripwires (agent-1 covered).

  Let me read DecoySendPairing.kt fully, and grep MessagingCoordinator for the relevant sections. Also read UnlockController's stop/lock path and ZitroneApp wiring (applyTransportLocked, deleteAccountAndWipe).

• The three evidence reports are in. Now I'll verify the U3 hot path myself — the pairing class, the coordinator tails, and the teardown wiring.

• Now I need to read key MessagingCoordinator sections: stop() (773-810), coverTeardown (806-830), runTerminalTeardownOnConfinedWorker (830-900), reconnectTransport (~876-900), the three publish sites and tails (publishOutgoing, publishReceipt — check they're non-suspending and return Boolean), deleteAccountAndWipe (~1850-1900), and the acceptingSends gate placement in each send path relative to the durability barrier.

  Also the prompt asks about `deleteAccountAndWipe → lockIf` re-entry from the worker handled by terminalTeardownDone skip — need to see stop() being called from the worker via deleteAccountAndWipe path. Line 786: `if (!terminalTeardownDone) runTerminalTeardownOnConfinedWorker(::coverTeardown)` — but wait, in deleteAccountAndWipe at line 1882 they call coverTeardown directly (already on worker). Then if stop() is later called... terminalTeardownDone is set in coverTeardown (817-818), so stop() skips. But what about stop() blocking on the worker while the worker waits on stop()? Line 781-786 mentions "Skipped ... worker from a caller the worker is itself waiting on would stall for the whole quiesce". Let me read the full sections.

  Also verify: publishOutgoing/publishReceipt are non-suspending private methods returning Boolean, and ws.sendMessage returns Boolean (handed to relay). Check "contact deleted → returns false" path. Also check `cover()` called from within a coroutine on confined — the call `coverTraffic.cover(envelope)` at line 1210: is it inside the confined coroutine after the publish tail? Since cover() is suspend and called from the send coroutine on confined, build+admit run on confined worker — good. Then sleep(gapMs()) suspends — teardown could be dispatched during the sleep and drain. Fine.

  One potential issue: `cover()` checks `if (teardown.withLock { transportInvalid }) return` and `buildCover` calls `recipient()` which is `DecoyAuthStore(rt).accountId` — takes vault runtime read lock. Is that on the confined worker — yes, fine, but lock ordering: teardown lock is released before buildCover? Look: line 460 `if (teardown.withLock { transportInvalid }) return` — lock released after check. Then buildCover without holding teardown. Then admission takes teardown again. Between check and admission, teardown could run on... under confinement contract, no. OK.

  But wait — potential issue in `finish()`: `if (inFlight.remove(pending) && !transportInvalid) emit(...)`. If stop() drained it (removed), finish does nothing — good. If finish removes it while stop is concurrently... serialized by lock. Fine.

  Another: `cover()` is suspend and contains `sleep` — but also the first `teardown.withLock` and `buildCover`. buildCover calls `builder.build` which does crypto — non-suspending presumably.

  Now the key verification points for the confinement argument:

  1. Is `coverTraffic.cover(envelope)` at 1210/1422/1574 invoked on the confined worker with no suspension between ws.sendMessage (inside publishOutgoing) and inFlight.add? Need to check publishOutgoing: non-suspend, does contactExists check + ws.sendMessage, returns Boolean. Then cover() runs: teardown.withLock check, buildCover (non-suspend), admit. Between publishOutgoing's sendMessage and inFlight.add — no suspend functions called, assuming buildCover is non-suspend (it is a private fun, non-suspend) and builder.build is non-suspend, recipient() non-suspend lambda, ensureProvisioning non-suspend (called inside buildCover when recipient is null). ensureProvisioning launches a coroutine — `job.start()` dispatches; scope.launch with Dispatchers.IO — fine.

  But wait: cover() is a `suspend fun` — calling it involves the state machine, but there's no suspension point until `sleep`. The tail between sendMessage and inFlight.add is uninterruptible by *dispatch* of teardown since the worker thread is busy. Correct — a coroutine only yields the worker at suspension points. Confirmed by code: no suspend calls in that slice. 

  2. Does anything else call cover() besides those three sites? Grep showed 3 sites. Also CoverTraffic.NONE. OK.

  3. teardown dispatch on worker in every path: stop() → runTerminalTeardownOnConfinedWorker(::coverTeardown) unless terminalTeardownDone; deleteAccountAndWipe → directly calls coverTeardown (line ~1882-1884) since already on worker. Need to check line 1876-1890 context. Also `close()` at 2277-2296: sets acceptingSends = false (line 2284) and launches confined tasks — does close() invoke teardown? Line 2289-2296 mentions "queued on the confined dispatcher: those queued deliveries would..." Need to read that section — this might be another path (scope cancel). If close() cancels the scope without running coverTeardown, pairings... wait — if the scope is cancelled, cover coroutines sleeping in gap get cancelled → finally → finish() → emits unless transportInvalid. But if scope is cancelled and ws disconnected without drain... Let me read close() and stop() fully.

  Also reconnectTransport at 891-896: `runTerminalTeardownOnConfinedWorker { coverTraffic.quiesce(swapTransport) }` — blocks calling thread for ≤250ms waiting for worker; caller holds transportLock. If timeout, runs on calling thread — violating confinement but degrading gracefully.

  Now, key attack surfaces to verify myself:

  A. The 250ms fallback: if worker doesn't become free in 250ms, teardown runs on the calling thread concurrently with a send's publish-then-pair slice on the worker. Then teardown CAN land mid-slice? No — the slice is uninterruptible by dispatch, but teardown on a *different thread* can now interleave with the slice. The teardown lock serializes though: cover's admission takes teardown lock; stop's drain takes teardown lock. So the lock (kept "even though a strictly confined caller would not need it") is what saves the fallback path: if stop() runs on calling thread while a send is mid-slice on the worker, the send might be between buildCover and admission; stop takes the lock, drains (doesn't see this pairing), sets transportInvalid, invalidates transport (ws.disconnect). Then the send's admission sees transportInvalid → refuses → returns without emitting. Result: real frame went out (before disconnect — actually real frame already sent before), cover frame never emitted → unpaired real frame. Hmm — but wait: in this scenario the send's ws.sendMessage already succeeded before disconnect, and the pairing is refused → lone real frame correlated with lock. That's the R-U3-3 marked frame. But it's bounded: only when the worker is stalled >250ms during a lock. The residual is declared ("the bound is on waiting for the worker, not on cover work") — but the prompt asks: is the trade right and is 250ms defensible? The declared residual frames the risk as hanging lock vs framing defect. Under fallback, the framing defect (unpaired frame) can still occur, but only when the worker was already stalled ≥250ms, which likely means something is already very wrong. I think this is defensible and matches declared residual. But note: what can stall the confined worker ≥250ms? The bootstrapLoop, flushSendRatchet (Argon? crypto?), persist sinks, EncryptedSharedPreferences... If worker is busy with a long task, stop() falls back and runs coverTeardown on the calling thread while the send later runs on the worker — wait no, teardown already done → acceptingSends false → send refused. The race is only with a send currently in its slice. Slice is short (crypto build). The window is small. OK.

  B. Re-entry: stop() blocks on worker while holding transportLock (in ZitroneApp?), and deleteAccountAndWipe → lockIf can re-enter from the worker. Handled by terminalTeardownDone skip at line 786 and 817. Need to verify: deleteAccountAndWipe runs on confined worker (line 1882 comment), calls coverTeardown directly. If it then calls lockIf → stop() on the worker thread itself: stop() calls `if (!terminalTeardownDone) runTerminalTeardownOnConfinedWorker(...)` — terminalTeardownDone is true (set by coverTeardown) → skip → no self-deadlock. But what if stop() is called from worker when terminalTeardownDone is false? E.g., deleteAccountAndWipe → lockIf(→ stop()) before coverTeardown? Line 781-786 comment says "Skipped ... worker from a caller the worker is itself waiting on would stall for the whole quiesce". Hmm — need to read stop() fully to see the guard. Actually the comment at 784 says the dispatch is skipped in some case — let me read.

  Also is there a guard detecting "am I on the confined worker"? limitedParallelism doesn't provide that. The skip relies solely on terminalTeardownDone. If deleteAccountAndWipe → coverTeardown sets terminalTeardownDone = true at line 818 BEFORE coverTraffic.stop — then a re-entrant stop() skips — fine. But if the re-entry happens from within coverTraffic.stop's invalidateTransport (ws.disconnect → callback → lockIf → stop())? Then terminalTeardownDone already true → skip. OK.

  But here's a subtle one: runTerminalTeardownOnConfinedWorker dispatches `scope.launch(confined + NonCancellable)`. If the scope is already cancelled (close() cancels scope?), the launch fails silently (job cancelled immediately) and done.await times out after 250ms → fallback runs teardown on calling thread. Fine-ish.

  Also: deleteAccountAndWipe path — check that acceptingSends = false precedes teardown there (line 1876). OK.

  C. quiesce vs mid-drain reconnect: reconnectTransport blocks the calling thread (holding transportLock) for up to 250ms. The swap runs on the worker under teardown lock. New sends over new socket admitted after. What about OkHttp: ws.disconnect then new dial — the drain emits on the old socket while live, then swapTransport disconnects. But emit() hands frames to OkHttp's async writer queue — "sendMessage hands the frame to OkHttp's asynchronous writer queue and returns" — so a drained cover frame may not actually be written to the wire before disconnect()! disconnect() presumably shuts down the websocket — queued-but-unwritten frames may be dropped. This is a real gap: drain emits gapless cover frames into OkHttp's queue, then immediately ws.disconnect() — OkHttp may discard queued messages on disconnect. The kdoc at 640 acknowledges OkHttp owns the writes for the gap floor, but for the DRAIN this matters more: the whole point of drain-then-disconnect is defeated if disconnect drops queued frames. Does WsClient.sendMessage actually enqueue async, and does disconnect flush? Need to check WsClient. If disconnect is graceful (sends close frame after queue drains?) — OkHttp's WebSocket.close() enqueues a close frame after pending messages, actually. OkHttp's `WebSocket.close(code, reason)` queues the close frame behind pending messages, so queued messages are written first. But `WebSocket.cancel()` aborts immediately. Which does WsClient.disconnect() call? Must check. Agent-0 flagged checking WsClient logging too. Let me read WsClient sendMessage/disconnect.

  D. The "no suspension point" claim: verify publishOutgoing and publishReceipt are non-suspending (private fun, not suspend). Line 2488 comment mentions it. Need to read them.

  E. `cover()` is called at 1210 etc. — inside what coroutine context? Confirm those sends run on confined.

  F. UnlockController closes vault runtime immediately after stop() returns — check ordering: stop() may return after fallback while the dispatched NonCancellable coroutine is STILL queued/running on the worker and will run coverTeardown later — concurrently with vault runtime close! coverTraffic.stop drains by calling emit → send → ws; and recipient()/sender() not used in drain. But wait — if both run: fallback thread runs coverTeardown (terminalTeardownDone set true), then the dispatched coroutine later runs coverTeardown → returns early due to terminalTeardownDone. OK. But the reverse race: fallback sets terminalTeardownDone=true then coverTraffic.stop runs; the queued coroutine runs coverTeardown → sees done → returns. Fine. But drain on calling thread emits frames via send() — WsClient thread-safe presumably.

  Also, after stop() fallback, UnlockController closes vault runtime — meanwhile the still-queued NonCancellable coroutine will run coverTeardown which early-returns. No vault touch. Fine.

  G. acceptingSends gate placement: line 1103-1106 deliverText checks acceptingSends before crypto/durable write; 1294-1295, 1523-1526 for receipts. Good. But acceptingSends is set false in stop() (778) and deleteAccountAndWipe (1876) and close (2284). 

  H. The prompt item 1 also asks: "teardown really is dispatched onto the same worker in every path (including deleteAccountAndWipe)". deleteAccountAndWipe does NOT dispatch — it's already on the worker (comment at 1882-1883). Verify deleteAccountAndWipe actually runs on confined.

  I. ZitroneApp wiring: applyTransportLocked uses reconnectTransport; stop wired to lock path; coverTraffic construction with recipient = DecoyAuthStore(rt).accountId etc. Also check that MessagingCoordinator.stop is called before runtime.close in UnlockController, and that stop is invoked while holding transportLock (prompt item 6). Let me read ZitroneApp wiring sections and UnlockController.

  Let me now read MessagingCoordinator sections: 770-910 (stop, coverTeardown, runTerminalTeardown, reconnectTransport), 1100-1260 (deliverText + publishOutgoing), 1290-1450 (attachment), 1520-1620 (receipts + publishReceipt), 1850-1910 (deleteAccountAndWipe), 2270-2320 (close), and WsClient sendMessage/disconnect.

• Now let me read the coordinator's teardown machinery and publish tails directly.

• Now read the publishOutgoing/publishReceipt tails, the other two send sites, deleteAccountAndWipe region, close() region, and WsClient sendMessage/disconnect. Also ZitroneApp wiring (applyTransportLocked, coverTraffic construction, stop ordering with runtime close in UnlockController).I still need: publishOutgoing/publishReceipt definitions (they're referenced; find them), the receipts site (~1520-1600), deleteAccountAndWipe (~1850-1900), close() (~2270-2300), and WsClient.sendMessage/disconnect semantics. Also ZitroneApp wiring + UnlockController.Now read publishOutgoing (399-470ish) and the WsClient sendMessage/disconnect. Also ZitroneApp wiring and UnlockController ordering. Also close() at 2270-2300.Note something at line 2300: the revoke path (onSessionRevoked presumably) does NOT call coverTeardown — it sets acceptingSends=false and calls onForcedLogout. The comment says "[onForcedLogout] below runs the real teardown" — so teardown happens via the app's lock path → stop(). OK, need to confirm the forced logout path leads to MessagingCoordinator.stop(). That's in ZitroneApp.

  Also check WsClient sendMessage/disconnect.Now read WsClient.send (private) to verify what sendMessage's Boolean means ("handed to relay" vs "queued to OkHttp") — important for the success-gate claim. The tail returns true when OkHttp accepted the frame into its queue, not when the relay took it. That's a wording/semantic gap: "handed to the relay" is really "handed to OkHttp's queue". But pre-existing behavior for the real send's SENDING tick — acceptable. The gate's purpose is distinguishing discarded/refused from accepted, which it does.

  Also read ZitroneApp sections 940-980 (lock path: coordinator.stop() then runtime close), 1500-1540 (applyTransportLocked + reconnectTransport), 1640-1770 (coverTraffic construction). And UnlockController close ordering.I'm mid-review. Let me set up a quick todo list mentally — actually let me just continue; I have a few more verification steps:

  1. ZitroneApp coverTraffic construction (1640-1770) — read.
  2. UnlockController — check lock()/lockIf re-entry and terminal wipe ordering (stopSession under transportLock, runtime.close in finally — confirmed at 961-968).
  3. Verify quiesce mid-drain reconnect attack: reconnectTransport blocks calling thread (holding transportLock) up to 250ms. swapTransport runs on worker. What if a send is in its gap when quiesce runs: drain emits cover frame on OLD socket, then swapTransport disconnects and redials. But emit → ws.sendMessage → OkHttp async queue; disconnect() calls webSocket?.close(CLOSE_NORMAL) — OkHttp close() enqueues a graceful close AFTER queued messages, so queued frames are written first. OK good — OkHttp's close() waits for queued messages to be transmitted (it enqueues the close frame). Actually OkHttp WebSocket.close() enqueues a close frame; pending messages are sent before the close frame. So drain-then-close preserves ordering on the wire. Good.

  But wait — in quiesce, swapTransport = { wsClient.disconnect(); wsClient.connect(token) }. disconnect() sets webSocket = null immediately after close() — the close is graceful, frames flush. Then connect() dials new socket. OK.

  Attack: "correctness against a socket that reconnects mid-drain" — e.g., the socket dies on its own during drain: emit returns false (dead socket) — contained. Then swapTransport disconnects and redials. Fine.

  What about a race: applyTransportLocked checks `connectionState != DISCONNECTED` before calling reconnectTransport. If socket is DISCONNECTED (e.g., mid-reconnect with auto-reconnect scheduled), the swap path skips reconnectTransport entirely — no drain! Pairing sleeping in its gap will wake and emit... wait, if connection dropped and WsClient auto-reconnects (scheduleReconnect), the socket underneath changes without any drain. A pairing in its gap then emits its cover frame over the NEW socket (or a later reconnect), while its real frame went out over the old (now-dead) connection. Is that a split pair? The real frame went on connection 1; connection 1 died (observer sees TLS teardown); the cover frame goes out 5-50ms later — if the reconnect happened within the gap, the cover goes over connection 2. That's exactly the split-pair scenario the quiesce fixes, but occurring on an *unplanned* reconnect (network flap), which the observer can see anyway. Hmm — but wait: if the socket died, did the real frame even reach the relay? The real frame was handed to OkHttp's queue; if the connection died, OkHttp may have failed to write it — but publish returned true (webSocket non-null at the time). OkHttp fails queued messages? On failure, queued messages are... OkHttp's WebSocket on failure discards the queue. So the real frame may never have hit the wire, while the cover goes out on the reconnected socket → lone decoy! 

  Sequence: publishOutgoing: ws.sendMessage → webSocket.send() enqueues → returns true. Connection drops before OkHttp's writer writes the frame (onFailure discards queue; the real frame never reaches the relay — relay never acks message.stored, message stays SENDING... wait, actually marked SENDING and retried? No — it returned true, so marked... the tick waits for message.stored; a lost frame means it stays SENDING forever? That's a pre-existing real-send issue, not U3). Cover: gap 5-50ms, then emit over a reconnected socket → decoy frame with no real frame behind it → lone decoy = marked frame. This is the same class as round-4's gated fix but via asynchronous socket failure rather than synchronous refusal. Is this in scope? R-U3-3: intermittent lone decoy. The gate can only check synchronous acceptance. The window is: OkHttp queue write failure after send() returned true. This is inherent to async writer — the kdoc acknowledges OkHttp owns writes. Is it a finding? The marked-frame property: a passive observer sees a lone decoy-length frame... but wait — the observer sees TLS records. If the connection died, the observer sees connection teardown; a lone frame on the new connection addressed to... recipient_id is only visible to relay, not passive observer. Passive observer sees sizes/timings: a single frame of the pair size after a reconnect. Given reconnects are independently observable and the real frame's loss means nothing was on the wire before, the pairing signal is weak: the passive observer can't distinguish "lone frame of pair-size after reconnect" from "ordinary single send". Hmm, but they could correlate: frame of exactly pair size... all message.send frames are padded to block multiples, so a single frame of that size is indistinguishable from any uncovered send. Actually a lone decoy is only "marked" relative to the expectation that all sends are paired. If some sends are unpaired (R-U3-4 accepted), the class "unpaired frames" exists. Intermittency is the issue: R-U3-3 says intermittent unpaired is worse than none. But this window (OkHttp queue dropped on connection failure within the 5-50ms gap) is a rare race, similar in kind to the accepted R-U3-4 residual. Also the relay-side observer: relay sees a decoy addressed to the synthetic account from the real account... relay sees sender+recipient. Relay knows synthetic accounts? The relay operator sees a frame from account A to synthetic account S with no preceding frame A→contact. Could correlate with a failed send. Bounded. I'd rate P2 at most, arguably inherent. Actually — hmm, wait. There's a subtlety: is this distinguishable from the already-accepted residual? Round-4 gate handles synchronous refusal. Async write failure within the gap → lone decoy. Also async write failure of the DECOY (emit returns true but write fails) → lone real frame. Both are inherent to OkHttp async semantics and equally affect the pre-U3 real send's durability story... no, pre-U3 had no pairing. I'll note it as P2/P3 — but must be candid: is it new machinery? It's inherent to the design of pairing over async sockets; the fix would require relay-level acks (message.stored) before cover — big design change. The spec §2.4 likely concedes relay-visible residuals. I could raise as P2 with concrete sequence. Actually let me reconsider: the cover frame emitted after a reconnect... hold on, does the reconnect even complete in 5-50ms? Tor/I2P reconnects take seconds. So typically emit returns false (socket down) → no decoy → lone real frame... but the real frame was dropped too (queue discarded on failure). Then NOTHING is on the wire — no marked frame at all! Because if the connection died before the real frame was written, the real frame never appeared. The only bad case: real frame written successfully (on wire, relay stored it), then connection dies, then cover emitted on new connection within 50ms. Reconnect within 50ms is essentially impossible (handshake + Tor). So the practical outcome of a mid-gap connection death: cover emit hits dead socket → returns false → dropped → real frame WAS on the wire (if written before death) → lone real frame, correlated with an observable connection death. That's the same class as the round-2/3 P1 (unpaired real frame correlated with teardown) but here the teardown is not user-correlated; it's network-flap correlated. A passive observer sees: frame, connection dies. Without cover, same observation. With cover intended, they might expect a pair... but pairing is only probabilistically observable anyway. Honestly this is minor — network death within a 50ms window, and the observer cannot know a send wasn't covered vs. wasn't a message.send. I'll mention as P3 or fold into a note. Hmm, actually there is one more: scheduleReconnect on connection death → new socket up seconds later → cover already decided (emit at 50ms hit dead socket → swallowed). No re-emit. Lone real frame. Bounded, rare, not user-action-correlated. P3.

  4. The `stop()` 250ms bound judgement — defensible: bounds waiting for worker, fallback degrades to round-3 behavior with the lock (teardown ReentrantLock) still serializing register access. Under fallback, can teardown land mid-slice? The slice is on the worker thread; teardown on calling thread. teardown.withLock in cover() admission and stop() — yes the lock serializes: if stop() runs during a send's slice, stop takes teardown lock after the send's admission (or before). Cases: (a) stop acquires before admission → admission sees transportInvalid → refuses → real frame unpaired. Real frame was already sent on a live socket → lone real frame, teardown-correlated. Window = duration of build (crypto) while worker stalled ≥250ms elsewhere... wait no — if the worker is executing the send's slice, the worker is not "blocked"; the dispatch would just queue behind the send's coroutine... Actually coroutines on limitedParallelism(1): the send coroutine's slice runs to its next suspension. The teardown task queues; when the send suspends at sleep(gap), teardown runs, drains (pairing admitted? admission happened before sleep — yes admitted) → drained → exactly once. Fine. The fallback only triggers when the worker doesn't become free for 250ms — i.e., a send coroutine or boot loop is doing blocking work >250ms without suspending (e.g., Argon2 PoW? That's on Dispatchers.IO for provisioning; signal crypto is fast; EncryptedSharedPreferences disk commits ms-scale; flush retries suspend). Rare. Then teardown on calling thread races the slice: protected by teardown lock → worst case lone real frame (case a) — round-3 race "back for that one teardown", as declared. Trade is defensible. 250ms: reasonable for a user-visible lock under transportLock; worst-case stall bounded. I'd accept with a note: the fallback path is untested behaviorally? Tests: "stop can't slip between CAS and assign" (932) tests blocking; fallback path — does any test exercise the 250ms timeout fallback? Agent-1 didn't mention one. Could be a test gap (P3).

  5. Re-entry (item 6): stop() blocks on worker while holding transportLock (stopSession at ZitroneApp 961-968 under synchronized(transportLock)). deleteAccountAndWipe → onConfirmed() → ... → lockIf → unlockController.lock() → stopSession → coordinator.stop() — called from the worker thread (since deleteAccountAndWipe runs on confined). stop() checks terminalTeardownDone (true, set by coverTeardown at 1885) → skips dispatch → no self-deadlock. Verified at code level: line 786 guard + 817-818. But wait: what if deleteAccountAndWipe's flow calls stop() BEFORE coverTeardown? Order in code: coverTeardown() at 1885, then onConfirmed() at 1890 — onConfirmed is what leads to lock. So teardown done first. Also stop() itself, when called on the worker thread in some other path where terminalTeardownDone is false — would dispatch and block 250ms then fallback: runTerminalTeardownOnConfinedWorker from the worker thread itself: launch queues behind... the current coroutine is running (blocking in done.await) — worker can't run the queued task → 250ms stall → fallback runs terminal() on calling thread (the worker) → fine, no deadlock, just 250ms. The comment at 781-785 acknowledges exactly this. OK.

  But hold on — there's a deadlock angle the other way: stop() is called under transportLock (ZitroneApp stopSession). runTerminalTeardownOnConfinedWorker blocks up to 250ms+. Meanwhile applyTransport holds transportLock too. Any path where the confined worker needs transportLock while stop() waits on the worker? Grep for transportLock uses: applyTransport (1506), publish (953), stopSession (962), reconnectTransport caller. Does the coordinator ever call something needing transportLock from the worker? If yes → deadlock bounded by 250ms then fallback — fine. If fallback runs terminal() which calls coverTraffic.stop { ws.disconnect() } — no transportLock needed. OK.

  Another: stop() waits `done.await()` unbounded in the `else` branch (line 875): if the worker claimed it in the same instant, wait unboundedly for the worker to finish — "what it is running is now bounded work with no wait of its own". But if the worker claimed ran=true and then the worker thread gets stuck before calling terminal()... e.g., the worker claimed it but is then... no — the claim happens inside the coroutine on the worker; after CAS, terminal() runs immediately on the worker; bounded (drain emits frames — emit is quick; ws.disconnect quick). But what if the worker coroutine, after CAS, is preempted forever (thread killed)? Threads aren't killed. Acceptable. However: what if the queued task after CAS... fine.

  Edge: `done.await()` unbounded in else branch — if the worker-claimed terminal() hangs (e.g., emit → ws.sendMessage blocks? OkHttp send() on a broken socket — non-blocking queue; fine). drainLocked loops inFlight — bounded by number of admitted pairings. Each send admits one pairing and removes after gap; max in-flight = sends in last 50ms. Bounded. OK.

  6. Item 2 — verify tails cannot suspend: publishOutgoing/publishReceipt are non-suspend private funs — confirmed. contactExists — need to check it's non-suspending. It's called within the tails. Let me check contactExists and messages.discard/markFailed — they're non-suspend (called from non-suspend fun, so compiler-enforced). Good. ws.sendMessage non-suspend. OK.

  The three sites: 1210, 1422, 1574 — all `if (publish…) cover(…)`. Confirmed.

  7. Item 3 — wall clock gone: DecoySendPairing has no deadline/condition variable/resolved flag; DRAIN_TIMEOUT_MS removed (comment at 651). Confirmed. Grep to be sure for "deadline|CountDown|resolved|DRAIN_TIMEOUT" in DecoySendPairing — from my full read: none. Confirmed. Note: runTerminalTeardownOnConfinedWorker has TEARDOWN_QUIESCE_MS — a wall clock, but on dispatch wait, declared residual.

  8. Now the remaining item: read ZitroneApp coverTraffic construction (1640-1770) to check sender lambda, DecoyEnvelopeBuilder usage, and check UnlockController lock path for isTerminalWipe (burn) — does burn path also stop coordinator? Also check deleteAccountAndWipe start: does it run on confined? Line ~1811 `scope.launch(confined + NonCancellable)`. Confirmed by earlier grep (1811).

  Also verify: is there any OTHER caller of coverTraffic.cover besides the 3 sites + NONE? Grep earlier showed only those. Also who calls coordinator.stop()? UnlockController stopSession. Also check the deleteAccountAndWipe head to see dispatch context.

  Let me also double check `cover()`'s first check `teardown.withLock { transportInvalid }` and admission — under confinement these are uncontended. Fine.

  One more attack on confinement (item 1): is `coverTraffic.cover(envelope)` at 1210 actually executed on the confined worker? deliverText is called from where? `send()` presumably does scope.launch(confined) { deliverText(...) } — line 529 area? Let me check the entry points: `fun sendText(...)` → scope.launch(confined)? Earlier grep showed line 529 scope.launch(confined), 1067, 1257, 1444, 1523... deliverText is suspend, called presumably within confined coroutine. Need to confirm quickly. Also retry() at 1444 launches confined → calls deliverText/deliverAttachment. OK likely all confined. Let me verify line 520-535 & the sendText entry.

  Also potential finding: `cover()` builds the decoy via `sender()?.let { builder.build(...) }` — builder.build does Signal encryption? No — U2: no SessionBuilder, no crypto; it mirrors. It needs Sender = local identity (account id + identity key?). Sender lambda at ZitroneApp:1722-1732 — need to read. It reads vault runtime — could that block? Non-suspending but takes vault locks — on the confined worker, fine (lock order preserved).

  Let me read ZitroneApp 1640-1770 and the sendText entry point, and UnlockController quickly. Then check `contactExists`. Then I can write the review.

• Let me finish verifying the wiring: the `DecoySendPairing` construction, send entry points, and the unlock/lock controller.

• sendText at 1066 — need to confirm it launches on confined. Line 1067 scope.launch(confined) per the earlier grep. Yes.

  Now check UnlockController's lock path quickly (lockIf re-entry), and confirm deleteAccountAndWipe start (line ~1811 launches confined+NonCancellable). Also check the WsClient diag logging: `diag` in WsClient logs to Log.w + bootDiagnostics — the sendMessage failure path doesn't log envelope (send(frame) just returns false silently; disconnect logs nothing). The coordinator's diag at publish failure logs connection state only, not recipient. So agent-0's finding 9 (unverified WsClient logging) — from my read of WsClient, diag lines are: "ws[N]: firing WS /ws handshake", "ws: connected", "ws: closed code=$code", handshake/stream failed with exception class+message+status. Exception message could contain... okhttp exception messages may include the URL? The URL is the relay ws URL, not vault-specific. No per-frame logging. So no decoy leak via WsClient logs. I can resolve finding 9 as clean: sendMessage failure path logs nothing per-frame; only connection-level lines.

  Now, UnlockController: check lock() and lockIf and isTerminalWipe, and where deleteAccountAndWipe's onConfirmed leads. Actually onConfirmed is a constructor hook — in ZitroneApp it probably triggers destroy + lock. The re-entry: onConfirmed → ... → unlockController.lock() → stopSession → coordinator.stop() — all while still on the confined worker thread (since deleteAccountAndWipe runs there). stop() skips dispatch via terminalTeardownDone. Verified at 786. But also: stopSession's synchronized(transportLock) — is transportLock held by anyone who waits on the worker? applyTransport holds transportLock and calls reconnectTransport which BLOCKS up to 250ms on the worker. If the worker (in deleteAccountAndWipe → onConfirmed → lock → stopSession) blocks on transportLock while applyTransport's reconnectTransport waits on the worker... deadlock scenario: applyTransportLocked holds transportLock, calls coordinator.reconnectTransport → runTerminalTeardownOnConfinedWorker → dispatch to worker, await 250ms. Worker meanwhile is in deleteAccountAndWipe → onConfirmed → lock() → stopSession → synchronized(transportLock) — blocks until applyTransport releases. applyTransport waits max 250ms for worker; worker task (quiesce) is queued behind deleteAccountAndWipe coroutine which is blocked on transportLock → worker never runs quiesce → timeout → fallback: `ran.compareAndSet(false,true)` → runs quiesce on calling thread (holding transportLock) → returns → releases transportLock → worker proceeds. Deadlock resolved by the 250ms bound. Good — the bound also breaks this lock cycle. That's another justification for it.

  Let me quickly check UnlockController for lock() semantics (cancels scope etc.) — mainly to verify "closes the vault runtime the instant stop() returns". ZitroneApp stopSession: coordinator.stop() then runtime.close() in finally — confirmed. Good enough.

  Now remaining checks for item 4 (quiesce correctness vs mid-drain reconnect):

  Attack: reconnectTransport is only called when `connectionState != DISCONNECTED`. Mid-drain, the OLD socket might die on its own (onFailure) — WsClient schedules auto-reconnect (scheduleReconnect). Then swapTransport runs wsClient.disconnect() (cancels reconnectJob — good, disconnect() cancels reconnectJob and nulls webSocket) then connect(token) → new socket. Fine.

  But: what if connection dies and auto-reconnects BEFORE applyTransportLocked runs? Then pairing gap frames emit over the new socket — split pair across unplanned reconnect, as analyzed. The drain can't help because the swap already happened underneath (WsClient auto-reconnect is internal). Hmm wait — this is important! WsClient auto-reconnects on its own (scheduleReconnect). So a pairing sleeping in its gap when the connection drops and quickly recovers (e.g., network blip, WebSocket ping timeout with fast redial) emits its cover frame over the NEW connection. The real frame went out over the old one (if it was written before the drop). That's a split pair across a connection boundary — the exact P1 class quiesce was built to prevent, but on the unplanned-reconnect path, which quiesce does not cover. Is this reachable and observable? Connection drops: passive observer sees TLS teardown. Reconnect seconds later (backoff base 1s). Cover frame sleeping in gap: gap ≤50ms. For the cover to land on the new socket, reconnect must complete within 50ms — implausible (WS handshake ≥1 RTT, Tor much worse; plus BASE_BACKOFF 1s). So in practice the cover emit hits a dead/CONNECTING socket: webSocket reference — during reconnect, openSocket() sets webSocket to the new WebSocket immediately (CONNECTING). OkHttp's send() on a connecting socket... OkHttp queues messages even before onOpen? Actually OkHttp's newWebSocket returns immediately and send() enqueues — messages sent before onOpen are queued and transmitted once connected? Let me think: OkHttp RealWebSocket.connect is async; send() before connection established... RealWebSocket.send checks... I believe OkHttp throws or returns false if the socket hasn't completed the handshake? Actually RealWebSocket.send enqueues into the queue regardless; the writer runs once connected. Hmm, I recall OkHttp allows send() before onOpen and queues it ("Messages are queued..."). Not 100% sure. If queued: cover frame goes out when the reconnect completes → split pair across the boundary, seconds later. Real frame: was it on the wire? If the connection died mid-write... the real frame's send() returned true at publish; if written before death, it's on the wire (observer saw it on connection 1); connection dies; 1s later new connection; cover frame (pair-sized, identical length) appears on connection 2. Observer links two identical-length frames across the connection boundary — the exact split-pair signal, modulo the delay being ~1s rather than ms. Identical-length frame pairs are rare by chance (padded to 256B blocks — actually many frames share block sizes, so identical length alone is weak; the linking signal is weak). And the observer can't tell it's a decoy vs another send. Honestly: the passive-observer signal here is weak; the relay operator sees sender/recipient and would see a frame to the synthetic account seconds after a reconnect — the relay already knows synthetic accounts (it issued them) so the relay learns nothing new: decoys addressed to synthetic accounts are inherently visible to the relay as decoy-like? Wait — does the relay operator know which accounts are synthetic? The synthetic account is registered like any other; the relay can't distinguish it from a real user account... except it never fetches prekeys except once, never sends... The relay sees a frame A→S. If the relay suspects S is synthetic, any frame to S is a decoy by definition, and the design accepts relay-visible pairing (spec §2.4 residuals). So the marked-frame concern is about the passive observer. For the passive observer, a frame seconds after a reconnect carries no pairing signal (gap is 5-50ms normally; a 1s+ gap breaks the pairing pattern — it looks like an ordinary single send). So the bad case is only reconnect completing within ~50ms of the drop — implausible as argued, and even then, identical-length frames across a boundary with weak distinctiveness.

  I'll fold this into a P3/P2 note. Actually, let me reconsider whether to report it at all. The prompt says attack quiesce "especially correctness against a socket that reconnects mid-drain." Mid-DRAIN: drainLocked emits frames then swapTransport disconnects+redials. If the socket dies mid-drain: emit returns false, swallowed; rest of drain also false; then disconnect/connect. Result: some cover frames lost → corresponding real frames unpaired → but the connection death is the cause, network-visible, not user-action. Same bounded class. OK.

  The more interesting mid-drain issue: during quiesce, swapTransport = { disconnect(); connect(token) }. connect sets intentionallyClosed=false and dials. New pairings after quiesce emit via ws.sendMessage — if the new socket is still CONNECTING, does send() return true and queue, or false? If OkHttp queues pre-open sends: cover frames emitted during CONNECTING flush on connect — fine. If returns false: cover dropped → unpaired real frames for sends in the CONNECTING window. Wait — but the REAL frame would also get false → publish returns false → no cover → marked failed, retried on reconnect. Consistent. And if send() returns true-but-queued pre-open: both frames queued, both written post-open, pairing preserved. Either way consistent. OK good.

  Now item 5 judgment and item 7 are covered by agent reports and my reads.

  One more thing to verify myself: DecoyState kdoc/codec key parts — agent-0 covered thoroughly with citations; I trust but should spot-check a couple of high-impact claims: (a) requireDecoyCredentialsPaired exists at VaultState.kt:761-768; (b) duplicate-tag rejection at :512-514; (c) wipe on decode failure :770-793. Also the prompt item "Check §4.3 and §5 against the code" — agent-2 flagged restatements: §5 U2 cell restates builder contract; §4.1 carries tag-write trigger table despite "not restated here deliberately" (that was §4's hazard block). These are doc-drift P3s. Also the capacity numbers disagreement 636–646 vs 635/641/645 and prompt's "640–643". The prompt itself says "claimed 640–643 B" — which matches neither doc! Code kdoc says 636–646 per agent-0 (VaultState.kt:355-373). So the review prompt's number is stale; the code's kdoc and spec §4 R5 agree (636–646); u1 table says 635/641/645. Minor doc drift, P3.

  Let me spot-check VaultState.kt decoy codec sections to confirm agent-0's key citations before finalizing. Read lines 140-250, 350-470, 500-530, 700-800 of VaultState.kt.All key codec claims verified directly. One subtlety in `decodeDecoy`: if `readNullableString(r)` for accessToken/refreshToken throws inside the DecoyState constructor args — that throw happens before the `try`? No — the constructor call is inside the try (line 779-785 are within try). The args are evaluated inside the try block since the constructor call is inside it. Yes, lines 779-785 inside `try {`. Good.

  Another subtlety: `readNullableString` for accountId at line 772 — if identityKeyPair read succeeds but accessToken read throws, identityKeyPair is wiped in catch. Good.

  Now, one more check on agent-0's candidate finding 2 (non-capacity encode throws leave mutated-unscheduled state with capacityExceeded=false). VaultRuntime.mutate: applies block, then encodes; if encode throws IllegalArgumentException (pairing check), the live state stays mutated but unscheduled and capacityExceeded unset → flushBeforeAck would flush stale scheduled payload → durability lie. Reachability: current writers can't produce it (DecoyAuthStore fail-closed). Invariant hole. P3 at most. Note: is it actually true that mutate applies the block before encoding and on throw the mutation stays live? Agent-0 cited VaultRuntime.kt:121-130. I'll trust with "per VaultRuntime.kt:122-130" — maybe spot-check quickly. Let me read VaultRuntime mutate.

  Also verify agent-0 finding 1 (challenge-phase 429 retires backoff). DecoyRelayApi registrationChallenge rethrows non-404 ApiException including 429; provision() treats failures before registrationSpent as spent-nothing → clearBackoff. That's a real

