• This is a large adversarial security review task. Read-only. The prompt mentions U3 round 4, with MessagingCoordinator, cover traffic pairing, teardown confinement, quiesce state, etc. But interestingly, the SCOPE section lists U1 files (VaultState.kt, DecoyAuthStore.kt, etc.) — the prompt seems to be a composite (says "What U1 is" but round 4 focuses on U3 send-pairing machinery). The files listed in scope are U1 files, but the attack points (confinement, MessagingCoordinator, publish tails, quiesce, stop(), tripwires) are U3 files. I need to actually explore the repo to find the relevant files.

  This is a big review task — good candidate for delegating exploration to subagents, but the analysis itself requires careful reading. Let me first explore the tree to find the relevant files: MessagingCoordinator, cover(), DecoySendPairingTest, UnlockController, etc.

  Let me start by locating files.

• I'll start by locating the relevant files for this round-4 U3 review.165abb37 U3 fix r4: the composed fix — a success signal, and teardown on the send worker
3d1b50d5 l00prite: roster change — Kimi K3 to main reviewer, Codex to tie-breaker
8986d7af l00prite: U3 round-3 adjudication — severity UP, and one P1 is the architect's
e60a7887 U3 fix r3: cover traffic is subordinate — it cannot precede or outlive the real send
b78ee9b7 l00prite: U3 round-2 adjudication + Kimi ruling on two severity disputes
5695d6d9 l00prite: record U3 fix round 2 — the ruling implemented, and what it deleted
7a798d17 U3 fix r2: real frame first, always — the ruling implemented as a simplification
75f1b68d l00prite: group four CX23 items for one trip; split the rate-limit problem in two
?? l00prite/.l00prite/reviews/decoy-0.10.0/u3-r4-grok.md
?? l00prite/.l00prite/reviews/decoy-0.10.0/u3-r4-kimi-high.md
?? l00prite/.l00prite/reviews/decoy-0.10.0/u3-r4-kimi.md
?? l00prite/.l00prite/reviews/decoy-0.10.0/u3-r4-review-prompt.md


• Let me read the core files. Key files: MessagingCoordinator.kt, DecoySendPairing.kt, plus the review docs (§4.3 and §5 referenced — likely in the u3 review notes). Let me check the diff and read files. .../java/com/zitrone/app/MessagingCoordinator.kt   | 254 ++++++++--
 .../src/main/java/com/zitrone/app/ZitroneApp.kt    |  13 +-
 .../java/com/zitrone/app/decoy/DecoySendPairing.kt | 325 +++++++-----
 .../java/com/zitrone/app/DecoySendPairingTest.kt   | 558 ++++++++++++++++-----
 docs/design/DECOY_TRAFFIC_0.10.0_SPEC.md           |  71 ++-
 l00prite/.l00prite/ledger.md                       |  63 +++
 l00prite/.l00prite/lock.json                       |  10 +-
 .../reviews/decoy-0.10.0/u3-fix-r4-composed.md     | 128 +++++
 l00prite/.l00prite/state.json                      |  38 +-
 l00prite/.l00prite/todos.md                        |  36 ++
 10 files changed, 1172 insertions(+), 324 deletions(-)
This is a substantial review task. I need to read MessagingCoordinator.kt (the key file for confinement), the spec §4.3/§5, the fix notes (u3-fix-r4-composed.md), the test file, and possibly ZitroneApp.kt. Given the size, I could delegate some reading to explore subagents, but the analysis needs to be done by me with actual evidence. Let me read MessagingCoordinator.kt myself — it's the heart of the review.The file is 2619 lines. Key parts: the confined worker, send paths (publishOutgoing, publishReceipt), stop(), deleteAccountAndWipe, quiesce usage, terminalTeardownDone. Let me grep for the relevant sections and read targeted regions.Now I need to check:
  1. The three call sites of cover — lines 1203-1210, 1416-1422, 1568-1574. Check whether the publish tail + cover call slice contains a suspension point. `cover` is a `suspend fun` — calling it from within the coroutine... The call to `coverTraffic.cover(envelope)` is a suspend call, so the coroutine CAN suspend inside cover (e.g., at `sleep(gapMs())`). But the claim is no suspension between `ws.sendMessage` and `inFlight.add`. The slice includes: `if (publishOutgoing(...)) coverTraffic.cover(envelope)`. Inside `cover()`: `teardown.withLock { transportInvalid }` — no suspend; `buildCover(real)` — non-suspend; `teardown.withLock { ... inFlight.add }` — non-suspend. So between publishOutgoing's ws.sendMessage and inFlight.add there's no suspension point — but wait, is there? Calling a `suspend fun` from another suspend function is not itself a suspension point unless the callee actually suspends. cover() doesn't suspend before admission — its first suspension is `sleep(gapMs())` after admission. But — hmm — `if (publishOutgoing(...)) coverTraffic.cover(envelope)`: entering a suspend function doesn't suspend. OK.

  But there's a subtlety: interface dispatch on a suspend function doesn't suspend. Good.

  However: is `ensureProvisioning()` inside `buildCover`? It calls `teardown.withLock` and `scope.launch` — non-suspending. Good.

  2. Key issue — the confinement claim: teardown is enqueued on the same confined worker. But `runTerminalTeardownOnConfinedWorker` has a fallback: if the worker doesn't run the teardown within 250 ms, teardown runs on the CALLING thread. Then the confinement guarantee is void — a send slice can run concurrently with teardown on the calling thread. That's the declared residual #5. Judge it.

  3. Check `stop()` blocking while holding `transportLock`? Point 6 says stop() blocks on the worker while holding transportLock, and deleteAccountAndWipe → lockIf can re-enter from the worker, handled by terminalTeardownDone skip. Need to see deleteAccountAndWipe (line 1800-1900) and where stop() is called with transportLock held (ZitroneApp?).

  4. Check reconnectTransport path — a socket that reconnects mid-drain. `quiesce` drains under the lock, then swapTransport. But WsClient — does swapTransport disconnect the old socket? What about the old socket's reconnect machinery dialing a new connection mid-drain? Need to read ZitroneApp.applyTransportLocked and WsClient.

  Also check: `reconnectTransport` uses `runTerminalTeardownOnConfinedWorker` — which has the 250 ms fallback running quiesce on the CALLING thread. For quiesce, fallback means drain runs concurrently with sends — exactly-once still holds via the lock (teardown lock in DecoySendPairing), so behavior is correct but degraded. OK.

  Another thing to attack: `terminalTeardownDone` in stop() — `stop()` reads `terminalTeardownDone` (volatile) and skips. But in deleteAccountAndWipe, coverTeardown runs on the worker; stop() is later called via lockIf on the worker? Let me read deleteAccountAndWipe.

  Also important confinement gap: **is every send actually running on the confined worker?** Check the three call sites are inside `scope.launch(confined)`. Lines 1067, 1257, 1444, 1523 launch confined. But the receipt path at 1568 — inside what? Also — are there OTHER paths that call ws.sendMessage, e.g. the reconnect flush of queued receipts (flushPendingReceipts), attachments? Cover applies only at three sites. What about `ws.sendMessage` calls elsewhere — e.g., retry of failed messages, sendAttachment? Line 1416 might be attachment. Retry path: messages marked FAILED get retried — does the retry path go through sendMessage -> publishOutgoing with cover? Let me check.

  Also the big one to check: **`cover()` is a suspend function called on the confined worker. After admission it calls `sleep(gapMs())` — delay — which SUSPENDS the confined worker coroutine.** Wait — if the send coroutine suspends at `sleep`, the confined worker thread is free to run other tasks, including teardown! Then teardown drains the pairing — emits immediately — and then the send coroutine resumes, `finish(pending)` finds it removed. That's fine — pairing emitted once, gapless. OK.

  But here's a subtle issue: while the send coroutine is sleeping in its gap (suspended), the confined worker runs the NEXT send. So multiple sends interleave — fine.

  But what about ordering on the wire: send N's cover frame is emitted after its gap; send N+1's real frame may be published before send N's cover frame. So wire order: real N, real N+1, cover N, cover N+1... An observer sees pairs not adjacent. The spec presumably accepts this. Not a new finding for round 4 presumably.

  Now, the deeper attack on confinement: is `ws.sendMessage` truly the last thing before cover? In publishOutgoing, after `ws.sendMessage` returns true, it returns true; then `coverTraffic.cover(envelope)` is called. In between, only the if-check. No suspension. Good. But wait — actually there's a suspension risk: `coverTraffic.cover` is an interface suspend call. The state machine... calling a suspend function does not suspend by itself. Right.

  BUT — one more: `publishOutgoing` is called... where exactly relative to the flush? The kdoc claims instruction sequence from durability barrier to ws.sendMessage is pre-U3. Fine.

  Now attack point 2: "Both publish tails return 'handed to the relay'". `ws.sendMessage` returns true if handed to OkHttp's queue — not actually to the relay. The claim "handed to the relay" is optimistic — OkHttp enqueue can fail later (socket dies after enqueue). Then cover was emitted but real frame never went out. That's inherent asynchrony; was it previously noted? The return value of WsClient.sendMessage — need to check semantics. Let me read WsClient.sendMessage.

  Also check point: the receipt path — `publishReceipt` on failure calls `queueReceipts`. On reconnect flush, queued receipts are sent — does the flush pair them with cover? If flush path calls ws.sendMessage directly without cover, those are unpaired real frames — a marked class (receipts sent after reconnect). Let me check queueReceipts/flush.

  Now point 5 residual: stop() blocks ≤250 ms. TEARDOWN_QUIESCE_MS = 250? Check the constant.

  Point 6: stop() holds transportLock? Where's transportLock — in ZitroneApp. Need to check: ZitroneApp calls coordinator.stop() while holding transportLock; the confined worker may need transportLock? And deleteAccountAndWipe → lockIf re-enters stop() from the worker. Let me read deleteAccountAndWipe and ZitroneApp.

  Given the volume, let me read targeted chunks: lines 1180-1260 (send site 1), 1400-1460 (site 2), 1520-1620 (receipt site), 1790-1920 (deleteAccountAndWipe), and the fields 270-380. Plus ZitroneApp diff and WsClient.sendMessage. And the test file (558 lines changed — the tripwires). Also the spec §4.3/§5 and u3-fix-r4-composed.md.

  This is a lot; I'll delegate the test-file review and spec cross-check to subagents while I read the main code myself. Actually the instructions say two blind reviewers; I'm one. Delegating is fine.

  Let me read the remaining key sections myself first.Now key details to verify:

  1. **The receipt path suspension gap**: In `sendReadReceipt`, the guard `if (!acceptingSends) return@launch` — fine. Note the receipt site: `flushSendRatchet` SUSPENDS (with backoff) BEFORE the tail; then publishReceipt + cover. OK.

  2. **Critical check — the receipt flow in `onMessagesSeen`**: `sendReadReceipt` launches on confined. Fine.

  3. **deleteAccountAndWipe teardown ordering**: `coverTeardown()` is called on the worker — good. But note: in deleteAccountAndWipe, coverTeardown runs BEFORE messages.clearAll() etc. And note the api.deleteAccount() call happens BEFORE coverTeardown — the server delete leaves the device while pairings might be mid-gap... Actually the DELETE request is an HTTP call, not a message.send envelope; the decoy pairing applies to message.send frames. Not relevant to framing.

  But wait — deleteAccountAndWipe suspends at api.deleteAccount() etc. During those suspensions, the confined worker can run other things — but sends are not yet refused (acceptingSends set false only at step 4). So during the server delete (network round trip), real sends can still be admitted on the worker, and pairings admitted. Then coverTeardown drains them. Fine.

  Re-entry (point 6): deleteAccountAndWipe → onConfirmed() → presumably UnlockController.lock() → coordinator.stop(). stop() reads terminalTeardownDone — which was set true by coverTeardown() — so skips runTerminalTeardownOnConfinedWorker. Good, no deadlock. But check: stop() also does linkJob?.cancel() — linkJob already cancelled — fine. And this happens on the worker thread (since onConfirmed is called from the worker coroutine). stop() then calls notificationScheduler.cancelAll() etc. on the worker. OK.

  But wait — point 6 says "stop() blocks on the worker while holding transportLock". Where is transportLock? Probably in ZitroneApp. Need to check ZitroneApp's applyTransportLocked and where stop() is called. Also check UnlockController.lock().

  4. **WsClient.sendMessage semantics** — need to check what a `true` return means (enqueued vs actually written).

  5. **The `cover` suspend call site**: `if (publishOutgoing(...)) coverTraffic.cover(envelope)` — inside runCatching. If cover throws (it mustn't), runCatching catches and marks FAILED an already-delivered message. cover is designed total. OK.

  6. **A subtle one**: `cover()` is called on the confined worker; the first thing after admission it does `sleep(gapMs())` — delay suspends the send coroutine, releasing the worker. Fine.

  7. **Check: does anything call `ws.sendMessage` for real envelopes OUTSIDE these three sites?** e.g. flushPendingReceipts → sendReadReceipt → covered. Retry path → deliverText/deliverAttachment → covered. Attachment redeem? `ws.sendReceived`, `sendBurn`, typing — uncovered control channel, declared. Let me grep ws.sendMessage usages.

  Also grep for other `coverTraffic.cover` calls — only three sites per the prompt.

  8. **TEARDOWN_QUIESCE_MS value** — check constant, claimed 250ms.

  9. **`runTerminalTeardownOnConfinedWorker` fallback analysis**: if `done.await(250ms)` expires, `ran.compareAndSet(false,true)` → run terminal() on calling thread. Meanwhile a send could be executing its publish+admit slice ON the worker concurrently — teardown on the calling thread now races the slice: the teardown lock in DecoySendPairing still serializes drain vs admit. Worst case: teardown runs `drainLocked` then sets transportInvalid, disconnects; a send's slice may have done ws.sendMessage just before disconnect and admitted just after transportInvalid → refused → unpaired real frame. That's the round-3 race, acknowledged as residual. Declared.

  But ALSO: the latch fallback — `else done.await()` — unbounded wait! If the worker claimed it (ran=true set by worker) but the worker's terminal() hangs (e.g., coverTraffic.stop blocks on the teardown lock held by... under confinement, the lock is uncontended; but `drainLocked` emits via send() → WsClient.sendMessage → could it block? It enqueues to OkHttp — non-blocking presumably). If the worker is stuck forever (blocked on disk), done.await() waits forever → stop() hangs → vault never wipes keys. That's the very thing the 250ms bound was to prevent — and the fallback reintroduces an unbounded wait in the case where the worker claimed the task but is wedged mid-teardown. Hmm, but if the worker claimed teardown, teardown work itself is "bounded work with no wait of its own" per the comment. Is it? coverTeardown → coverTraffic.stop → teardown.withLock → drainLocked → emit → send() → ws.sendMessage (OkHttp enqueue, fast) → ws.disconnect(). WsClient.disconnect — need to check blocking behavior (could do a blocking close?). If disconnect blocks (e.g., waits for close handshake), then done.await() hangs. Let me check WsClient.disconnect.

  Also stop() itself: it's called from UnlockController.lock() — on what thread? If stop() is called ON the confined worker (re-entry from deleteAccountAndWipe → onConfirmed → lock → stop), then runTerminalTeardownOnConfinedWorker is skipped due to terminalTeardownDone. But what if stop() is called on the worker without terminalTeardownDone set? E.g., onSessionRevoked path — check line 2296 area and onSessionRevoked. If stop() is invoked on the confined worker thread directly and terminalTeardownDone is false, runTerminalTeardownOnConfinedWorker would launch onto confined and block awaiting → deadlock until 250ms timeout → fallback runs teardown on the calling thread = the worker itself → runs fine. Actually the fallback runs terminal() on the calling thread, which is the worker — that works, just costs 250ms. OK.

  Let me now check: ZitroneApp changes (the diff), WsClient.sendMessage/disconnect semantics, TEARDOWN_QUIESCE_MS, acceptingSends gates in deliverText, and the UnlockController.lock path re transportLock.

• Now the surrounding machinery — ZitroneApp, WsClient semantics, and the constants.diff --git a/apps/android/app/src/main/java/com/zitrone/app/ZitroneApp.kt b/apps/android/app/src/main/java/com/zitrone/app/ZitroneApp.kt
index 12fba41e..14cce89e 100644
--- a/apps/android/app/src/main/java/com/zitrone/app/ZitroneApp.kt
+++ b/apps/android/app/src/main/java/com/zitrone/app/ZitroneApp.kt
@@ -1516,8 +1516,17 @@ class AppContainer(private val app: Application) {
         if (live != null &&
             live.wsClient.connectionState.value != WsClient.ConnectionState.DISCONNECTED
         ) {
-            live.wsClient.disconnect()
-            live.apiClient.accessToken?.let(live.wsClient::connect)
+            // 0.10.0 U3 fix round 4 — this used to disconnect and redial directly, which split any
+            // cover pair sleeping in its drawn gap across the TLS teardown and the reconnect. A
+            // split pair is a STRONGER signal than a missing cover frame (third-lens ruling,
+            // round 3): two identical-length frames milliseconds apart, straddling a connection
+            // boundary an observer can see, correlated with the user changing their anonymity
+            // transport. The swap now goes through the coordinator, which drains cover traffic on
+            // its confined worker first and keeps pairing afterwards over the new socket.
+            live.coordinator.reconnectTransport {
+                live.wsClient.disconnect()
+                live.apiClient.accessToken?.let(live.wsClient::connect)
+            }
         }
     }
 


• Note: `acceptingSends` is checked in deliverText/deliverAttachment/sendReadReceipt, but is the guard present at sendText entry? Line 1103-1106 is inside deliverText presumably. But — the check happens at the top of the coroutine; a send that passed the check and is mid-flight when stop() runs is drained by coverTeardown. Fine.

  Important gap: `acceptingSends` is set false at stop(), but the send coroutine checks it at the START. A send already in progress (suspended in flush backoff) when stop() sets acceptingSends=false will proceed to publish after its flush — and then cover — all on the worker, with teardown enqueued behind. Serialized, fine.

  Now WsClient.sendMessage + disconnect semantics.Let me set up a todo list and continue. Key remaining work:

  1. WsClient.send semantics: `webSocket?.send(...)` — OkHttp's WebSocket.send returns false if the queue is full or socket closing; true means enqueued, NOT transmitted. So "handed to the relay" is actually "enqueued to OkHttp". A `true` return followed by socket failure → real frame never reaches relay, but cover was emitted → lone decoy. Is that a finding? It's inherent asynchrony — the gap between enqueue and actual write. But note: cover is emitted after a 5-50ms gap, so in the normal case the real frame is written before the decoy. If the socket is dead but webSocket non-null and queue accepts, send returns true → cover emitted, real frame lost. The relay never got the real frame → lone decoy on... wait, does the decoy reach the relay either? If the socket is dead, the decoy enqueue also fails silently on the wire. So a lone decoy on the wire requires the real frame's write to fail but the decoy's write to succeed — with a reconnect in between (the decoy goes out on the NEW socket?). Hmm — after reconnect, OkHttp queue of the old socket is discarded; the decoy emitted 5-50ms later could go to a NEW socket if webSocket was replaced. Sequence: real send enqueued on old socket (returns true); old socket dies before writer thread flushes; reconnect dials new socket; cover emitted onto new socket → relay receives decoy but never the real frame. Lone decoy, marked. But is this distinguishable/observable? The observer sees a single frame to the synthetic account... the relay operator sees a decoy addressed to the synthetic account with no corresponding real frame — but the relay operator can't tell decoys from real frames except by the recipient being the synthetic account, which the relay knows (it assigned the account). Actually the synthetic account only ever receives decoys, so any frame addressed to it is known-cover. A lone decoy without a paired real frame nearby... the real frame went to a different recipient; relay operator sees all envelopes. A real frame missing while a decoy present — the relay operator can correlate timing. This is the same inherent enqueue-window race; previous rounds may have accepted "handed to OkHttp queue" as the handoff definition. The kdoc even documents OkHttp async writer. This is likely a known accepted residual (the round-3 doc discusses it). I should check the fix doc u3-fix-r4-composed.md and spec §4.3/§5 to see what's declared, so I don't re-report declared residuals.

  2. Verify test file (tripwires) — delegate to a subagent? The test file is 558 lines changed; DecoySendPairingTest. I should review it myself or delegate. Given context budget, delegate the test audit to an explore subagent, and also delegate the spec §4.3/§5 + u1-invariant-table cross-check... actually the invariant table is U1; scope lists U1 files but the round-4 focus is U3. The prompt is a template mixing U1 and U3. The seven attack points are all U3. I'll focus on U3 but give the U1 files a pass too? The SCOPE section lists U1 files explicitly. Hmm — "Review the WHOLE UNIT" — the unit being reviewed is U3 round 4 (title says Unit U3, ROUND 4). The SCOPE section's file list is stale from the U1 template. The required reading §4.3 and §5 of the spec — spec DECOY_TRAFFIC_0.10.0_SPEC.md. I'll check §4.3/§5 for stale parallel-copy recurrence.

  Let me read the fix doc and spec sections, then the test file. Delegate test review to a subagent to save context? The test file is ~1000+ lines. I'll delegate a focused audit of DecoySendPairingTest to an explore agent, asking: for each test, does the named property make it pass; would it still pass if the behavior were broken; do the rewritten tripwires pin what they claim. Meanwhile I read spec §4.3/§5 and the fix doc myself.

  Also remaining code checks:
  - `coverTeardown` in deleteAccountAndWipe: called while NOT holding transportLock; stop() re-entry handled.
  - transportLock: point 6 says stop() blocks while holding transportLock — find transportLock. Grep.
  - UnlockController.lock() — does it call coordinator.stop() then close vault runtime in finally?
  - onSessionRevoked path (line ~2296): sets acceptingSends=false then launches confined — does it call coverTeardown? Line 2284 acceptingSends=false. Let me read 2270-2330.

  Also potential re-entry deadlock: stop() called on main thread; runTerminalTeardownOnConfinedWorker launches on confined; if the confined worker is currently executing deleteAccountAndWipe which calls onConfirmed() → UnlockController.lock() → ... → coordinator.stop() — that's ON the worker, terminalTeardownDone already true → skip. Good. But what if deleteAccountAndWipe's onConfirmed → lock() → stop() runs while terminalTeardownDone is... it was set in coverTeardown before onConfirmed. Good.

  What about a DIFFERENT re-entry: stop() on main thread blocks up to 250ms waiting for worker; the worker is running deleteAccountAndWipe whose onConfirmed→lock()→stop()... stop() is being called on the worker now (reentrant call from onConfirmed). terminalTeardownDone is true → skip runTerminal... fine, returns. Then deleteAccountAndWipe finishes, worker frees, main thread's enqueued teardown runs → coverTeardown → terminalTeardownDone true → return. Fine.

  Dangerous case: stop() called while worker is busy with deleteAccountAndWipe BEFORE its coverTeardown (e.g., during api.deleteAccount() network call which suspends — actually suspension frees the worker). The network call suspends, so the worker picks up the teardown task → coverTeardown runs → drains, disconnects socket. Then deleteAccountAndWipe resumes and calls api.deleteAccount()... wait api is HTTP, not the socket. OK. But the socket is now disconnected while the session is still live — session continues after onNotConfirmed (delete failed) — socket stays disconnected? coverTeardown is terminal (transportInvalid=true, cover off forever) but if the delete is NOT confirmed, "the session stays live" — with cover traffic permanently torn down and the socket disconnected! After onNotConfirmed, the session resumes — does anything reconnect the socket? connectionState DISCONNECTED; does WsClient auto-reconnect? disconnect() sets intentionallyClosed=true, so no auto-reconnect. Hmm, but wait — can coverTeardown run before the delete outcome? In deleteAccountAndWipe, coverTeardown is only called at step 4, after CONFIRMED. The race I described: stop() (from a vault lock) runs coverTeardown while deleteAccountAndWipe is suspended awaiting the server response. Then the delete comes back NOT confirmed → session "stays live" but the socket was disconnected by the lock's teardown... but if stop() was called, the vault is locking anyway — UnlockController closes the runtime. Session doesn't actually stay live; onNotConfirmed "lifts the terminal-wipe gate and surfaces a retry". Hmm, if the user locked concurrently with a delete, and the delete failed, the intent marker persists and next unlock reconciles. Socket reconnect on next session start. Probably fine — and this is pre-existing machinery (round 13-15), not round 4.

  More pertinent: **stop() racing deleteAccountAndWipe's own coverTeardown**: both call coverTeardown; idempotent via terminalTeardownDone. Both set it — but note stop()'s check `if (!terminalTeardownDone)` is OUTSIDE the worker (on teardown thread), while the flag is set on the worker. Volatile, fine.

  Now the quiesce attack (point 4): "correctness against a socket that reconnects mid-drain". reconnectTransport dispatches quiesce onto the confined worker (blocking up to 250ms). The swapTransport lambda: `live.wsClient.disconnect(); live.apiClient.accessToken?.let(live.wsClient::connect)`. Inside quiesce, drainLocked() emits pending covers on the OLD socket, then swapTransport disconnects and reconnects. During the drain, could the old socket have been reconnecting on its own (WsClient reconnectJob)? WsClient has reconnectJob with backoff; if the socket was down and reconnectJob fires mid-drain, openSocket() replaces webSocket. Then drain emits on the NEW socket? The pairings' real frames went out on... hmm, if socket was down, real sends were refused (sendMessage false → no cover). Pairings in the register have real frames that were accepted (enqueued) earlier. If the socket died after enqueuing the real frame and reconnects mid-drain, the drain emits the cover on the NEW socket → split pair across connection boundary — the exact P1 quiesce was built to fix. But wait: quiesce is triggered by a Tor toggle; the swap disconnects anyway. The drain emits on whatever `webSocket` points to at emit time. If the old socket died and WsClient's auto-reconnect dialed a new socket just before the drain, the drain's covers go out on the new connection while their real frames were lost with the old — lone decoys on the new connection. Hmm, but that's the same enqueue-window class as above.

  But more concretely against the design: quiesce holds the pairing `teardown` lock and calls swapTransport which calls ws.disconnect() then ws.connect(). WsClient.connect → openSocket → dials new socket. Fine. Meanwhile, WsClient's OWN reconnectJob could fire concurrently (it's a coroutine, cancelled by disconnect()). disconnect() cancels reconnectJob. OK.

  Race: ZitroneApp.applyTransportLocked holds the app's transport lock and "re-points the clients the moment this returns". The reconnectTransport blocks the caller up to 250ms; if the bound expires, quiesce runs on the CALLING thread — the drain then races in-flight sends on the worker; the pairing lock keeps exactly-once. Then swapTransport runs on calling thread while a send on the worker calls ws.sendMessage concurrently — WsClient.send reads `webSocket` field (is it @Volatile? need to check). If webSocket isn't volatile, there could be visibility issues, but send just gets false or sends on old socket — a real frame could be emitted on the OLD socket after the drain, then disconnect kills it → the cover for it will be emitted 5-50ms later on the NEW socket → lone decoy. This is the fallback path though (250ms expiry), declared residual class.

  Wait — actually there's a subtler mid-drain issue in quiesce: drainLocked() → emit → send() → ws.sendMessage → webSocket?.send — OkHttp enqueue is async. Then swapTransport() → ws.disconnect() → webSocket?.close(). The enqueued-but-not-yet-written decoy frames may be discarded by close() before the writer thread flushes them! OkHttp's close() attempts a graceful shutdown: it enqueues a close frame after queued messages... Actually OkHttp WebSocket.close() queues the close frame behind pending messages, so queued messages are written first. But if the socket is unhealthy, they're dropped. Best-effort. The drain emits "while the socket is still live" then immediately closes — the writes race the close on OkHttp's writer thread. Graceful close should flush. Marginal.

  Now, the more important quiesce correctness check: **does the drain actually pair covers onto the same connection as their real frames?** The real frame was enqueued earlier (5-50ms+ ago) on the old socket; drain enqueues covers on the same old socket; then disconnect. OkHttp graceful close flushes queue. OK.

  But — during quiesce, the register stays open and transportInvalid stays false; new sends DURING the swap: sends run on the confined worker, quiesce also runs on the confined worker — serialized. A send after quiesce goes on the new socket. Good — that's the confinement point. On the fallback path (calling thread), a send's slice can interleave with swapTransport: publish on old socket concurrent with disconnect... racy but degraded-path.

  Now check UnlockController: does lock() call stop() then close vault runtime in finally? And where's transportLock held during stop? Let me grep.

• The core send-path code is mapped. Now the lock/re-entry side and the spec docs.

• Now I see the transportLock interaction:

  1. `stopSession` holds `transportLock` and calls `coordinator.stop()`, which blocks up to 250 ms waiting for the confined worker. The confined worker, meanwhile... does any confined-worker path need `transportLock`? `applyTransport` (transport state collector) takes transportLock and calls `applyTransportLocked` → `reconnectTransport` → blocks up to 250ms waiting for the confined worker. Deadlock scenario: applyTransportLocked holds transportLock and blocks waiting for confined worker; stop() called from... stopSession also takes transportLock — it would block on the monitor, not deadlock the worker. Does the confined worker ever need transportLock? The worker runs sends, teardown — no transportLock. So no lock-order inversion with the worker. But there IS a potential self-deadlock-ish scenario in reconnectTransport: applyTransportLocked is called from `onSessionPublished` (which is called from... where? afterPublish on UnlockController — probably called on the unlocking coroutine thread) and from the transportResolver collector. reconnectTransport blocks waiting for the confined worker. Meanwhile if the confined worker is executing deleteAccountAndWipe → onConfirmed() → UnlockController.lock() → stopSession → synchronized(transportLock) — blocks because applyTransportLocked holds transportLock and is waiting for the worker → DEADLOCK for 250ms, then fallback: runTerminalTeardownOnConfinedWorker times out and runs quiesce on the calling thread (the transport collector thread), releases, returns; applyTransportLocked returns, releases transportLock; worker proceeds. So the 250ms bound actually saves it. But wait — in that fallback, `ran.compareAndSet` fails? No: worker never picked up the task (it was blocked inside deleteAccountAndWipe→lock→stopSession waiting on transportLock), so ran is false, fallback runs terminal() on calling thread. OK, resolves.

  But hold on — worse: the re-entry in point 6. "stop() blocks on the worker while holding transportLock, and deleteAccountAndWipe → lockIf can re-enter from the worker." Sequence: main/UI thread calls lock() → stopSession (holding transportLock) → coordinator.stop() → dispatches teardown to worker, blocks ≤250ms. Worker is running deleteAccountAndWipe → onConfirmed() → which calls lock() again (lockIf?) → UnlockController.lock() → stopSession → synchronized(transportLock) — blocked (held by the first stop's thread). First thread's stop() waits for worker ≤250ms → times out → fallback: terminalTeardownDone? At this point deleteAccountAndWipe hadn't yet reached coverTeardown (it's blocked in onConfirmed→lock... wait, coverTeardown is called BEFORE onConfirmed in deleteAccountAndWipe — line 1885 coverTeardown(), line 1890 onConfirmed(). So by the time onConfirmed runs, terminalTeardownDone=true. First thread's stop(): `if (!terminalTeardownDone)` — read at stop() entry. If stop() was entered BEFORE the worker set terminalTeardownDone, it dispatches and waits; times out at 250ms; fallback ran.compareAndSet succeeds → runs coverTeardown() on the calling thread → sees terminalTeardownDone true → returns. Fine, no deadlock, no double-run.

  What if timing differs: stop() entered after flag set → skips dispatch entirely → proceeds → returns → runtime.close() in finally → releases transportLock → worker's re-entrant lock() proceeds → stopSession → coordinator.stop() again (idempotent-ish) → runtime.close() again?? Double close of runtime — presumably idempotent (pre-existing).

  OK. Now the declared residual (#5): stop() blocks ≤250ms. Trade: UnlockController closes vault runtime the instant stop() returns. If the 250ms expires and teardown falls back to the calling thread — the drain runs on the calling thread (still inside stop(), before runtime.close()). Wait, actually — the fallback runs terminal() synchronously on the calling thread, so the drain still completes BEFORE stop() returns and before runtime.close(). So the wipe-vs-build race is NOT what the fallback sacrifices; the fallback sacrifices the serialisation (teardown races the worker's send slice). The comment says exactly that. So the trade is: in the worst case, you get the round-3 race (possible unpaired real frame) rather than a hung lock. Is 250ms defensible? The worker's blocking work is "millisecond-scale disk commits" — but is it really? flushSendRatchet with transient-retry backoff SUSPENDS (frees worker). Disk commits (vault reseal, EncryptedSharedPreferences) — could take longer on a slow device, but 250ms is plausible. However — one scenario: the worker could be running a long NON-suspending stretch: e.g., Argon2id? No — PoW runs on Dispatchers.Default. Vault reseal (AEAD over a fixed region) is fast. OK.

  But there's a subtle hole in the fallback: `if (ran.compareAndSet(false, true)) terminal() else done.await()`. If the worker claimed the task in the same instant (ran=false→true by worker right after our CAS fails), we call done.await() — UNBOUNDED wait. The comment says "what it is running is now bounded work with no wait of its own". Is teardown bounded? coverTeardown → coverTraffic.stop → teardown.withLock. Under the fallback scenario the lock could be held by... who? cover()'s withLock sections are short, non-suspending, on the worker. drainLocked → emit → send() → OkHttp enqueue — non-blocking. invalidateTransport → ws.disconnect() → webSocket?.close() — OkHttp close is non-blocking (enqueues close frame). So teardown work itself is bounded, milliseconds. BUT the worker claimed the task — meaning the worker is FREE and running our teardown — so done will be counted down quickly. Except if the worker thread gets killed/stuck mid-teardown... edge. Acceptable.

  BUT: bigger hole — `done.await()` unbounded when the worker claimed it. What if the worker claimed the teardown task but is executing it while ALSO the quiesce path... no. Fine.

  Now the crucial question for point 1: "verify there is genuinely no suspension point between ws.sendMessage and inFlight.add". Slice: publishOutgoing → ws.sendMessage → return true → `coverTraffic.cover(envelope)` — entering suspend fun cover: the prologue is `teardown.withLock { transportInvalid }` — ReentrantLock — could BLOCK if contended (by teardown on calling thread in fallback mode), but blocking isn't suspension; the worker waits. Under confinement, uncontended. buildCover → recipient() (reads vault — DecoySectionLock, could block briefly), sender(), builder.build (crypto — could be slow but non-suspending). Then withLock { inFlight.add }. No suspension points (no delay, no withContext, no suspend calls). Verified at the source level — as long as none of recipient()/sender()/builder.build suspend. builder.build is non-suspend (DecoyEnvelopeBuilder — check). recipient/sender are `() ->` non-suspend types. Yes: `private val sender: () -> DecoyEnvelopeBuilder.Sender?` and `recipient: () -> String?` — non-suspending function types. Compiler-enforced. Good.

  BUT — one thing: `ensureProvisioning()` is called inside buildCover (when recipient()==null) — non-suspending, launches LAZY job, job.start() dispatches. Non-suspending. Good.

  And the kdoc's claim "the caller's publish tail, this build, the admission below is ONE uninterrupted slice" — but wait: between publishOutgoing returning and cover() being entered, is there anything? `if (publishOutgoing(...)) coverTraffic.cover(envelope)` — just a branch. But here's a subtlety: **calling a suspend function compiles to a state machine; entering cover does not suspend**. Correct.

  However — the REAL question: is teardown really "dispatched onto the same worker in every path"? Paths:
  - stop() → runTerminalTeardownOnConfinedWorker → confined (with fallback).
  - deleteAccountAndWipe → coverTeardown() directly on worker. ✓
  - reconnectTransport → runTerminalTeardownOnConfinedWorker with quiesce. ✓ (fallback exists)
  - onSessionRevoked (line ~2296) — need to check: does it tear down cover? It sets acceptingSends=false at 2284. Does it call coverTeardown or stop()? If onSessionRevoked disconnects the socket WITHOUT coverTeardown, that's a path where teardown isn't confined. Let me read 2270-2330.

  Also ZitroneApp line 1764 mentions "U3: wraps the publish tail of every outbound envelope" — the wiring of DecoySendPairing. Let me look at that too.

  And what about UnlockController.lock() → stopSession — always calls coordinator.stop() — covered.

  Other paths that disconnect the socket: grep ws.disconnect / wsClient.disconnect usages.

• Checking remaining teardown paths (onSessionRevoked) and socket-disconnect sites.

• onSessionRevoked → onForcedLogout → presumably UnlockController.lock() → stopSession → coordinator.stop() → coverTeardown. OK, so teardown flows through stop(). Good — only two socket invalidation sites, both through cover traffic.

  Now — point 6 details confirmed. Next: read the spec §4.3/§5 and the fix doc, and check for stale parallel-copy (DecoyEnvelopeBuilder canonical, VaultState codec kdoc, DecoyState kdoc — nothing may restate them). The class kdoc of DecoySendPairing is HUGE and restates a lot — but contracts in scope say nothing may restate DecoyEnvelopeBuilder construction rules, codec kdoc tag-write trigger, DecoyState TAG_DECOY field set. Check whether DecoySendPairing kdoc or MessagingCoordinator comments restate them. The kdoc mentions "DecoyState's kdoc is canonical for what the section holds" — a pointer, fine.

  Let me read the fix doc and spec §4.3, §5.1:# 0.10.0-beta — Decoy traffic: SPEC
6:### Maintainer rulings (2026-07-27)
41:## 0. Executive summary — what changed once the code was read
73:## 1. Threat model — stated before the mechanism
92:## 2. OPEN QUESTION 1 — envelope size and structure indistinguishability. **RESOLVED.**
94:### 2.1 The measured baseline
144:### 2.2 Resolution — size mirroring, and structure by instantiation
267:### 2.3 The ciphertext does not need to be a real ratchet output — and should not be
376:### 2.4 The uncovered channel — declared, not silently ignored
459:## 3. OPEN QUESTION 2 — idle-ping sizing. **MOOT — THE PING IS CUT.**
461:### 3.0 ⛔ CUT 2026-07-27 (maintainer decision). Everything below §3.0 is HISTORICAL.
492:### (HISTORICAL, superseded by §3.0) OPEN QUESTION 2 — idle-ping sizing
494:### 3.1 The premise correction — this is the finding that most changes §8
511:### 3.2 Resolution — reframe as in-session dead-air cover, and say so
526:### 3.3 Sizing — match the mode, do not sample a distribution
550:## 4. Durable state — WRITER/READER invariant table
560:### The signal
576:### WRITERS
589:### READERS, and what each assumes `TAG_DECOY` MEANS
641:### THE HAZARD THIS TABLE EXISTS TO CATCH
683:### 4.1 Storage-format-stability gate — ANSWERED, not deferred a third time
808:### 4.2 Account deletion and the synthetic account — RULED 2026-07-27 (raised by U1)
837:### CRASH ATOMICITY — to be verified, not assumed
851:### WHAT THIS WRITE MUST NOT DO
867:## 4.3 U3 — pairing. Stated as REQUIREMENTS, deliberately, and not as instructions
877:### R-U3-1 — A real send is never harmed by cover traffic. **Absolute.**
926:### R-U3-2 — ~~A covered send is two frames an observer cannot tell apart~~ **AMENDED: REAL-FRAME-FIRST, ALWAYS**
956:### (superseded) R-U3-2 — A covered send is two frames an observer cannot tell apart
962:### R-U3-3 — Failure is uniform, never intermittent. **This is the subtle one.**
974:### R-U3-4 — RULING on `build()` throwing: the real send proceeds, uncovered
984:### R-U3-5 — Nothing survives the vault
1046:### Open, and to be decided by evidence rather than by this document
1052:## 5. Implementation units — Rule of 6, hard cap at 6
1070:### The indicator (U6) — exact framing
1087:## 6. Dependencies and interactions the maintainer must rule on
1216:## 7. Out of scope for 0.10.0 — stated so it is not mistaken for coverage
1224:## 8. Still open from 0.9.4, tracked, not blocking
Now I should verify the claims in this doc against code. I've verified much of it. Remaining verifications:

  1. Spec §4.3 and §5 — check the spec amendments match code (spec was changed in the diff too — 71 lines).
  2. Tripwires in DecoySendPairingTest — need to audit the test file (~1100 lines?). Delegate to an explore subagent to audit the tripwires and behavioral tests. Actually, the tripwires are critical ("Judge whether they now pin what they claim") — I should verify key ones myself or delegate with precise instructions. Given context budget (~large already), delegate the test-file audit to an explore agent with detailed instructions.

  3. Some remaining code-level checks I can do quickly:
     - `acceptingSends` gate position in deliverText: line 1103-1106 — inside deliverText at top? Line 1103 is in deliverText presumably. sendText → scope.launch(confined) { ... }? Let me check where sendText launches and whether deliverText checks gate before crypto. Line 1103: "R-U3-5 step 1... Before any crypto, any durable write and any" — check location. Also is there a gate at flushPendingReceipts? sendReadReceipt checks.
     - The receipt gate nuance: `onMessagesSeen` → `sendReadReceipt` launches coroutine; gate checked inside. OK.
     - One more possible hole: `retry()` at 1443 → deliverText/deliverAttachment — gate checked inside deliver* (1103-1106, 1294-1295). OK.
     
  4. A deeper quiesce issue to consider: `reconnectTransport` is invoked from `applyTransportLocked` under `transportLock`. It blocks up to 250ms. Meanwhile stopSession also needs transportLock. If applyTransport runs (transport collector thread) and blocks 250ms while holding transportLock, stopSession waits — fine, monitor wait.

     But there's a re-entrancy deadlock variant: reconnectTransport dispatches quiesce to the confined worker and blocks. The confined worker is currently running... `deliverAttachment` which is suspended at `api.uploadBlob` — suspending frees the worker. OK. What runs on the confined worker that could need transportLock? Nothing found. What about `onSessionPublished` → applyTransportLocked — called from UnlockController afterPublish, which runs on whichever thread unlock happens. That path also calls reconnectTransport while holding transportLock — blocks up to 250ms. If at that moment the confined worker is executing deleteAccountAndWipe → onConfirmed → lock() → stopSession → synchronized(transportLock) → blocks → worker stuck → 250ms expiry → fallback runs quiesce on calling thread → OK. Recovers.

     But wait — worse: **stopSession holds transportLock while coordinator.stop() blocks on the worker; the worker is running deleteAccountAndWipe → onConfirmed → lockIf → lock() → stopSession → synchronized(transportLock)**. Deadlock: stop() waits ≤250ms for worker; worker waits on transportLock held by stopSession's thread. After 250ms, stop()'s await expires; fallback: `ran.compareAndSet(false,true)` — the worker hasn't claimed the task (it's blocked) → runs terminal() on the calling thread → coverTeardown → terminalTeardownDone already true (deleteAccountAndWipe called coverTeardown BEFORE onConfirmed) → returns immediately → stop() returns → runtime.close() → transportLock released → worker proceeds into stopSession → coordinator.stop() again → terminalTeardownDone true → skip → runtime.close() again. Is runtime.close() idempotent/thread-safe? Pre-existing concern, round 13-15 machinery; presumably idempotent ("onConfirmed's lock() is idempotent" per comment). OK.

     But hold on — actually there's a sharper problem in that sequence: the first stop() ran its teardown on the CALLING thread concurrently with the worker running deleteAccountAndWipe. coverTeardown returned early due to flag. Fine.

     What if stop() is called (vault lock by user) while worker is mid-deleteAccountAndWipe BEFORE its coverTeardown (e.g., suspended in api.deleteAccount())? Suspension frees worker → worker picks up stop()'s teardown task → coverTeardown runs → drains pairings, disconnects socket, transportInvalid=true, terminalTeardownDone=true. deleteAccountAndWipe resumes; server delete CONFIRMED; step 4: coverTeardown() → returns early (flag) → proceeds with wipe. Fine.
     If the delete comes back NOT confirmed: session "stays live" — but the socket was disconnected by the concurrent stop()'s teardown, and UnlockController.lock() proceeds to close the runtime anyway (stop() caller is locking). onNotConfirmed "lifts the terminal-wipe gate and surfaces a retry". Hmm — UnlockController.isTerminalWipe gate... this is pre-existing round 13-16 territory, out of round-4 scope.

  5. Now the **fundamental confinement gap** I should double-check: are there send paths that call ws.sendMessage NOT on the confined worker? grep ws.sendMessage / send( in coordinator: settlePostAck calls ws.sendReceived (control channel, declared uncovered). flushPendingReceipts — called from where? On CONNECTED transition — which thread? Let me check where flushPendingReceipts is invoked. If it's invoked on the socket callback thread, sendReadReceipt launches confined — fine.

  6. **Does `cover()` get called for a receipt that was queued then flushed on reconnect?** flushPendingReceipts → sendReadReceipt → builds a NEW envelope → publishReceipt → cover. Fine.

  7. Another check — **`emit` during drain while socket live but disconnect imminent**: quiesce drain emits, then swapTransport disconnects. OkHttp close flushes queue gracefully. Best effort, declared.

  8. **The `stop()` NonCancellable launch on `scope`**: "the session scope is cancelled immediately after this returns" — where is scope cancelled? UnlockController newSessionScope per session; lock() cancels it after stopSession? If the scope is cancelled BEFORE stop() is called... if scope already cancelled, `scope.launch` won't run the block — then done.await(250) expires → fallback runs terminal() on calling thread. Covered.

  9. **A subtle one in runTerminalTeardownOnConfinedWorker**: `scope.launch(confined + NonCancellable)`. If the scope is active, fine. The latch countdown is in finally. OK.

  10. **Now, a real candidate finding — the `onMessagesSeen` receipt path and `acceptingSends`:** fine.

  11. **Candidate finding — cover() called while holding runCatching: a CancellationException thrown from cover's sleep propagates through runCatching...** In deliverText, `runCatching { ... cover(envelope) }.onFailure { e -> if (e is CancellationException) throw e ... }`. If the send coroutine is cancelled during cover's sleep, cover's finally emits the cover frame (finish), then CancellationException propagates → rethrown. The real message was already sent. But `messages.markFailed(messageId)` is skipped (rethrow). Good.

  12. **What about ` Pending` exactly-once when stop() runs drain concurrently (fallback path)**: removal under lock, fine.

  13. **provisionJob race**: ensureProvisioning holds teardown lock; stop() cancels under same lock. But `provisioning` AtomicBoolean is reset in the job's finally — after cancellation, finally sets provisioning.set(false). Fine. But note: after stop(), transportInvalid=true so ensureProvisioning never relaunches. OK.

  14. **A missed path: does `stop()` get called when the scope is already dead AND terminalTeardownDone false, with a pairing mid-gap?** Fallback runs teardown on calling thread; the sending coroutine sleeping in its gap on the confined dispatcher — cancelled with the scope; its finally → finish(pending) → inFlight.remove → false (already drained) → no double emit. Or if the coroutine never runs again (dispatcher thread fine, but scope cancelled → finally still runs? When a coroutine is cancelled while suspended in delay, it resumes with CancellationException and runs finally — yes, finish runs. OK.)

  15. **THE key remaining thing: verify `deliverText` gate at 1103 and sendText entry; and whether the gate check for the receipt path `sendReadReceipt` also applies to `flushPendingReceipts`-triggered sends (it re-enters sendReadReceipt, so yes).**

  16. **Check the wiring in ZitroneApp line ~1764** — how DecoySendPairing is constructed: sender/recipient/provision lambdas. Verify recipient reads DecoyState credentials (presence≠readiness), sender etc. Quick read.

  17. **VaultState.kt codec / DecoyState etc. (U1 files)** — the prompt's scope lists them, and says check §4.3/§5 against code, and stale parallel-copy recurrence. These files weren't changed in round 4 (diff shows only MessagingCoordinator, ZitroneApp, DecoySendPairing, tests, spec). But "review the WHOLE UNIT" — the unit is U3; U1/U2 files were reviewed in their own rounds. Given limited budget, I'll do a light sweep: check that nothing in round-4 changes restates DecoyEnvelopeBuilder/codec contracts, and verify spec §4.3/§5 consistency. The tripwire test file may also pin codec stuff.

  Let me now:
  - Read ZitroneApp 1740-1800 (wiring).
  - Read MessagingCoordinator 1090-1115 (deliverText gate).
  - Then read spec §4.3 (867-1050) and §5.
  - Then delegate test file audit to an explore subagent.Now — a potentially significant observation regarding the **receipt path and `acceptingSends`**: `sendReadReceipt` gate is checked inside the coroutine — but `onMessagesSeen` calls `notificationScheduler.onConversationRead` and `messages.markRead` BEFORE launching. Fine.

  Let me now check the wiring just above 1740 — the DecoySendPairing construction (sender, recipient, provision, scope). Read 1690-1743.Wiring looks sane. `recipient = { DecoyAuthStore(rt).accountId }` — reads account id (presence≠readiness: accountId only, need to check DecoyAuthStore.accountId returns only when credential pair present — U1 scope; check quickly).

  Now read spec §4.3 and §5 to check against code, and look for stale parallel copies. Read spec lines 867-1087.Spec §4.3/§5 is consistent with the code. Now the test file. It's large; delegate the audit to an explore subagent with precise questions. Also ask it to check the tripwires' claims (fix doc §"tripwires re-derived") against the actual test code, and each behavioral test: does the named property make it pass.

  Also, I still want to check a few code points myself:

  A. **`emit` in drainLocked happens under the `teardown` lock, and `send` = `wsClient::sendMessage` → `webSocket?.send(...)` — OkHttp WebSocket.send can BLOCK?** OkHttp's WebSocket.send(String) — it enqueues; if the queue is full (queued byte count exceeds 16MiB) it returns false (in newer versions it throws? Actually `WebSocket.send` returns boolean: false when the message couldn't be enqueued because queue size limit; it doesn't block). OK, non-blocking.

  B. **`stop()` — the `if (!terminalTeardownDone)` check on the calling thread + `runTerminalTeardownOnConfinedWorker` CAS**: two concurrent stop() calls: both pass the check; both dispatch; CAS ensures one runs terminal(); done latch counts. Fine.

  C. **A genuine potential finding — `quiesce` via `runTerminalTeardownOnConfinedWorker` shares `ran`/dispatch logic; the `terminalTeardownDone` guard doesn't apply to quiesce (good), but quiesce dispatched while a terminal teardown is QUEUED**: Tor toggle dispatches quiesce; concurrently a lock dispatches coverTeardown. Order on the worker decides. If quiesce runs first: drains, swaps transport (disconnect+connect — new socket!). Then coverTeardown runs: drains (empty), disconnects new socket. Fine. If teardown first: transportInvalid=true, socket disconnected; then quiesce runs: drainLocked (empty), swapTransport() → **disconnect() then connect() — RE-CONNECTS THE SOCKET AFTER TERMINAL TEARDOWN!** The session is being locked; UnlockController closes the runtime right after stop() returns. But quiesce re-dials the socket — a live socket for a locked vault! Who disconnects it? applyTransportLocked's reconnectTransport returns after quiesce; the socket stays connected (connectionState CONNECTED, with access token). The vault is locked; the socket lingers — R-U3-5 "A vault that is locked emits nothing" — the socket will send pings? Does WsClient send periodic pings? OkHttp may send ping frames (pingInterval if configured). At minimum, a TLS connection to the relay persists after lock, authenticated with the token — it may receive deliveries and invoke callbacks (onMessageDeliver → scope.launch(confined) on a cancelled scope — no-op). So no frames emitted perhaps, but the connection persists until process death or next transport event. Is that a real scenario though? Sequencing: stop() runs teardown on worker (task A). applyTransport dispatches quiesce (task B). If A runs first, then B: swapTransport re-connects. But wait — would applyTransportLocked even call reconnectTransport after the socket is DISCONNECTED? Check: `if (live != null && live.wsClient.connectionState.value != DISCONNECTED)`. The state check happens BEFORE dispatching (reading current value). Race: state read as CONNECTED (before teardown's disconnect), then reconnectTransport dispatches quiesce; teardown task A was already queued and runs first, disconnecting; quiesce task B then re-connects. Concrete interleaving:

  1. User locks vault (thread L): stop() → dispatches coverTeardown (task A) to worker, blocks.
  2. Concurrently, Tor toggle resolves (thread T): applyTransportLocked → reads connectionState = CONNECTED → calls reconnectTransport → dispatches quiesce (task B) to worker, blocks.
  3. Worker runs A: drain, transportInvalid=true, ws.disconnect() → state DISCONNECTED. done.countDown → L's stop returns → runtime.close() (wipes keys).
  4. Worker runs B: quiesce → drain (empty) → swapTransport → ws.disconnect(); api.accessToken?.let(ws::connect) → **re-dials the relay with the vault's access token after the vault is locked and wiped.**

  Hmm — but is api.accessToken still present? apiClient.clearTokens happens on revoke, not on lock. On lock, tokens are vault-backed (VaultAuthStore(rt)) — rt is closed; reading accessToken from a closed runtime might throw or return null. VaultAuthStore(rt).accessToken getter — after rt.close(), mutate/read probably throws IllegalStateException (check(!closed)). Then `live.apiClient.accessToken` — accessToken is a property on ApiClient delegating to the auth store — could THROW inside swapTransport, inside quiesce's finally, on the worker — exception propagates out of the quiesce task (runTerminalTeardownOnConfinedWorker's launch has try/finally countDown; the exception propagates to the coroutine — unhandled, logged). connect() not called. So probably it throws rather than reconnects. Or returns null → no connect. Either way no reconnect? If accessToken read succeeds (token cached in ApiClient field?), connect() dials a socket for a locked vault. Need to check ApiClient.accessToken implementation.

  Also note: even absent a lock, this race shows quiesce doesn't check transportInvalid before swapping. If terminal teardown ran, quiesce should be a no-op. The CoverTraffic.quiesce in DecoySendPairing doesn't check transportInvalid — it just drains and swaps. But swapTransport is the CALLER's lambda (disconnect+connect) — DecoySendPairing can't skip it. The coordinator could guard reconnectTransport with terminalTeardownDone — it doesn't.

  Is this P1? Consequence: after vault lock, a new authenticated WebSocket to the relay is dialed. WsClient scope = session scope, cancelled after lock → reconnectJob cancelled; but the socket itself — OkHttp socket with listener callbacks on a dead scope. It will stay connected (OkHttp manages it) emitting nothing except maybe pings (does the OkHttp client configure pingInterval? need check). An observer sees a TLS connection established right AFTER the lock/background event — an infrastructure anomaly correlated with locking. Also R-U3-5: "A vault that is locked emits nothing" — the WS handshake sends the access token (Sec-WebSocket-Protocol header). That IS an emission post-lock, and an authenticated one.

  Severity: requires Tor toggle racing a lock within ~250ms. Concrete, plausible (backgrounding triggers auto-lock; transport state can flip when Orbot starts/stops — applyTransport fires on transportResolver.state collection, which happens on network changes! Not just user toggle. TorIntegration.requestOrbotStart, connectivity changes — these happen around backgrounding/lock naturally). I'd call it P2 (real defect, bounded blast radius: authenticated reconnect after lock; framing-wise it can also produce unpaired frames? No — sends are refused). Actually more precisely the harm: an authenticated session socket lives after the vault is locked — receives messages? onMessageDeliver launches on cancelled scope — dropped, un-acked → relay redelivers... to a dead scope. It also keeps the user's "online" presence on the relay after lock — a state distinguisher. I'll verify ApiClient.accessToken behavior before claiming.

  Let me also reconsider: is this reachable? `applyTransport` fires on ANY transportResolver state change, including network callbacks during backgrounding. And `onSessionPublished` also calls applyTransportLocked. The race needs the state read to see CONNECTED before the teardown disconnect. The window is real but narrow (~ms). P2 seems right, maybe P1 given "a lock that leaves an authenticated socket dialing the relay" — but it needs api.accessToken to still resolve. Check ApiClient.

  D. Also check `reconnectTransport`'s use of runTerminalTeardownOnConfinedWorker — its kdoc says "Run TERMINAL teardown..." but is used for quiesce too; cosmetic.

  E. Check `WsClient.updateTransport` — applies new transport without reconnect? Fine.

  Next steps: check ApiClient.accessToken + VaultAuthStore closed-runtime behavior; check OkHttp pingInterval; then delegate test audit; then sweep U1 files quickly (DecoyAuthStore.accountId presence≠readiness).

• Spec matches code. Now chasing a suspected quiesce-after-teardown race — need `ApiClient.accessToken` semantics.

• ApiClient.accessToken → VaultAuthStore(rt).accessToken → runtime.read { ... }. What does runtime.read do after close()? Check VaultRuntime.read/close — does read throw after closed or return stale? Let me check VaultRuntime quickly.So after `rt.close()`, `runtime.read` throws `IllegalStateException("vault runtime is closed")`. So in my suspected race:

  Sequence B (teardown runs first, then quiesce): stop() returns → stopSession's finally runs `rt.close()` — but wait, that happens on the LOCK thread, concurrent with the worker running task B (quiesce). Timing: worker runs task A (teardown, disconnect) → done.countDown → lock thread proceeds to runtime.close(). Meanwhile worker proceeds to task B (quiesce): drainLocked (empty), then swapTransport → ws.disconnect() (no-op) → `live.apiClient.accessToken` → authStore.accessToken → runtime.read → **throws IllegalStateException if close already happened**, or returns the token if close hasn't happened yet (race between worker's task B and lock thread's close()).

  - If it throws: exception propagates out of the `terminal()` call inside the launched coroutine (`try { if (ran.compareAndSet...) terminal() } finally { done.countDown() }`) → uncaught in coroutine → scope's handler. reconnectTransport's caller (applyTransport thread) — if it was still waiting on done (≤250ms), it gets countDown and returns normally. No reconnect. Socket stays disconnected. OK-ish, just an unhandled exception in a dead scope.
  - If it returns the token (close hasn't landed): `ws.connect(token)` — dials a new authenticated WebSocket. Then rt.close() lands. The socket is now live with a valid token, session scope cancelled (reconnectJob etc. dead), delivering inbound envelopes to a cancelled scope (no-op), relay sees the user online after lock. Socket stays open indefinitely (until process death or network loss; OkHttp won't close it; WsClient's auto-reconnect may even keep it alive? reconnectJob was tied to scope — cancelled, so no auto-reconnect, but the existing socket stays). Also `onOpen` callback → does it send anything (e.g., flush, auth)? Check WsClient socketListener onOpen.

  Is this plausible timing? Task B runs immediately after task A on the same worker; the lock thread wakes from the latch and calls runtime.close() — a race measured in microseconds-to-ms. The window is small but real, and there might be an even cleaner variant: quiesce dispatched BEFORE teardown (Tor toggle first, then lock): task B (quiesce) runs first — drains, swapTransport: disconnect + connect (session live, fine) — then task A (teardown): drains again (empty), disconnects. Fine. So the bad ordering is only teardown-then-quiesce.

  Also another variant without a lock: teardown via deleteAccountAndWipe? deleteAccountAndWipe's coverTeardown runs on the worker; a queued quiesce after it → swapTransport → api.accessToken — rt not yet closed (destroy happens later via onConfirmed → lock → ...) — token still readable → reconnects socket AFTER terminal teardown (transportInvalid=true, cover dead forever) → then messages.clearAll, onConfirmed → lock() → stopSession → stop() → skip (flag) → runtime.close(). Socket reconnected and never disconnected again! In this variant there's NO race at all — the quiesce task was queued behind the delete's coverTeardown, and deterministically re-connects the socket after terminal teardown, and nothing ever disconnects it (stop()'s teardown is skipped as done). The vault is wiped, account deleted server-side, and the client dials a fresh authenticated WebSocket to the relay. Deterministic given a Tor toggle / transport state change landing in the delete window.

  Actually wait — also simpler: after ANY terminal teardown (stop()), a queued quiesce re-connects. stop() path: stop() blocks until task A completes, then returns, runtime.close() follows immediately (microseconds-ms later on the same thread, but the worker runs task B concurrently). The quiesce's accessToken read races close(). In the deleteAccountAndWipe path, there's no tight race: coverTeardown is followed by more worker work and onConfirmed → lock — the window between coverTeardown and rt.close() includes messages.clearAll(), conversations.clearAll(), and the whole lock() round trip — milliseconds at least, likely much longer. A quiesce queued behind would very likely read a valid token and reconnect. Then the session is torn down with a live socket.

  Hold on — but is a quiesce ever actually queued in that window? applyTransport fires on transportResolver state changes (Tor up/down, network callbacks). During account deletion (user-initiated, seconds-long PoW/network) a transport flip is plausible. During a routine lock (backgrounding), transport flips also happen on network callbacks. So the interleaving is reachable.

  What's the actual harm? A WebSocket authenticated with the vault's access token connects to the relay after the vault is locked/account deleted. Emissions: the WS handshake itself (with token). Any frames after? Check socketListener.onOpen — does it send anything (e.g., flush pending)? Also OkHttp ping config. And for the delete case, the account is server-side gone — the token is invalid → server may reject/close → onAuthExpired? → onSessionRevoked? Callbacks on cancelled scope... onSessionRevoked runs on socket callback thread and does real work: `api.clearTokens()` → authStore.clearTokens() → runtime... on a closed runtime → throws? VaultAuthStore.clearTokens → probably mutate → throws IllegalStateException on closed runtime. Uncaught on OkHttp callback thread — could crash the app (OkHttp listener exceptions). Hmm, but scope cancelled doesn't stop onSessionRevoked (it's called on the socket thread directly, not via scope). Actually onSessionRevoked checks deleteInFlight/intentMarkerPresent → intentMarkerPresent() reads... a persisted marker — from where? If vault-backed and closed → throw. This cascades.

  But careful: is this round-4-introduced? Yes — quiesce is NEW in round 4; before round 4, applyTransportLocked disconnected and redialed DIRECTLY (same reconnect-after-lock behavior existed!). Hmm — before round 4: applyTransportLocked → `live.wsClient.disconnect(); live.apiClient.accessToken?.let(live.wsClient::connect)` — executed inline on the applyTransport thread, NOT serialized with teardown at all. The same reconnect-after-lock race existed before (worse, actually — no drain). So this specific hole is not a regression; but round 4 claims to fix the transport-swap path and introduces quiesce as "correct". The review says "attack quiesce hard, especially correctness against a socket that reconnects mid-drain". The finding: **quiesce does not check terminal state — a quiesce queued behind a terminal teardown re-dials the socket after teardown**, leaving an authenticated socket live after lock, and `DecoySendPairing.quiesce` doesn't consult `transportInvalid`, nor does `reconnectTransport` consult `terminalTeardownDone`. That's a genuine defect in the new machinery. Severity: P2 (bounded: requires transport flip racing teardown; result is an authenticated connection post-lock, potentially unhandled exceptions on a dead runtime; not key leak, not deniability break on disk). Could argue P1 under R-U3-5 "A vault that is locked emits nothing" — the reconnect emits an authenticated handshake after lock. Given the round-4 claim "quiesce is non-terminal and correct", and the framing: after terminal teardown, the socket it re-dials is unpaired (cover dead — transportInvalid=true, so cover() returns early) — wait, but sends are also refused (acceptingSends=false, and session is torn down). No messages flow. The emission is just the WS connection + token. I judge P2, leaning high. Actually — let me think about deniability: the adversary observing forced unlocks sees a connection established after lock... it's an infrastructure anomaly but not vault-content dependent. P2.

  Hmm wait, actually let me double-check the pre-round-4 code also had it: yes (diff shows the old code did disconnect+connect directly). So it's a pre-existing defect that round 4's new machinery retains and claims to have fixed the path. The review prompt says round 4 "transport changes now drain and keep pairing over the new socket" — the terminal-state check is missing. Report as P2 (not newly introduced, but in-scope new machinery fails to handle it, and the fix doc claims the path is now fixed).

  Second candidate finding — **`reconnectTransport` blocking while holding `transportLock` interacts with `stopSession`**: stopSession holds transportLock and calls stop() which blocks up to 250ms on the worker. If the worker is idle, teardown runs in ms. Fine. Conversely applyTransportLocked holds transportLock and blocks up to 250ms on the worker — if the worker is simultaneously trying to acquire transportLock... does the worker ever? Searched: transportLock only in ZitroneApp (publish, stopSession, onSessionPublished, applyTransport). publish is called by UnlockController after building session — on the unlock thread, not the worker. deleteAccountAndWipe → onConfirmed → lock() → stopSession → transportLock: the WORKER (deleteAccountAndWipe runs on confined worker) tries to acquire transportLock while applyTransportLocked holds it and blocks waiting for the worker (reconnectTransport). DEADLOCK until the 250ms expiry: applyTransportLocked's wait expires → fallback runs quiesce on the calling thread → releases transportLock → worker proceeds. So it self-resolves in 250ms. But wait — the fallback: `if (ran.compareAndSet(false, true)) terminal() else done.await()`. The worker never claimed the task (it's blocked on transportLock), so CAS succeeds, terminal() runs on the calling thread. Resolves. OK — the 250ms bound earns its keep here. Not a finding beyond noting it works.

  Third candidate — **the unbounded `done.await()` fallback**: if the worker claimed teardown but then... the worker runs terminal() which calls coverTraffic.stop → teardown.withLock → drain → emit → ws.sendMessage (non-blocking) → invalidateTransport → ws.disconnect() (non-blocking). Bounded. Unless a disk commit on the worker... no, teardown itself does none. done.await() fine.

  Fourth — **`stop()`'s skip when `terminalTeardownDone` is true but the socket disconnect hasn't happened**: flag is set inside coverTeardown BEFORE coverTraffic.stop runs (line 817-819: terminalTeardownDone = true; then coverTraffic.stop). So if coverTeardown is RUNNING on the worker (flag set, drain in progress) and stop() is called concurrently on another thread: stop() sees flag true → SKIPS runTerminalTeardownOnConfinedWorker → returns immediately → UnlockController closes the vault runtime WHILE the drain is still emitting covers and before ws.disconnect() runs! The kdoc on coverTeardown says "the flag exists so the dispatch can be skipped, not the drain" — but skipping the dispatch also skips WAITING for the drain. stop() returning early → runtime.close() races the drain's emit (emit doesn't touch vault — OK) and races... ws.disconnect happens after drain on the worker. runtime.close() doesn't touch the socket. So the socket gets disconnected a few ms later by the worker. Consequence: stop() returns before the transport is invalidated; vault wiped while socket still live for a few ms — "a session whose socket outlives its own lock is worse than any framing defect" — the kdoc's own stated reason for blocking! The window: between flag set and invalidateTransport() — the drain, which emits up to |inFlight| frames (each an OkHttp enqueue, fast). Small but nonzero. When can this happen? Two teardown paths racing: deleteAccountAndWipe's coverTeardown (worker) + a concurrent lock's stop(). The delete flow will call lock() via onConfirmed anyway. Or onForcedLogout → lock and a user lock simultaneously? UnlockController.lock() is probably idempotent/serialized (lockIf). Given UnlockController serializes locks, concurrent stop() calls are unlikely except delete+lock. In delete+lock: delete's coverTeardown runs on worker; the user-initiated lock's stop() sees flag → skips → returns → runtime.close() — but the delete flow is mid-flight (messages.clearAll etc. on worker, using... messages repo is RAM; conversations.clearAll uses vault roster store → VaultRosterStore(rt) → mutate on a closed runtime → throws!). Hmm — that's the pre-existing round 13-16 delete machinery with isTerminalWipe gates; lockIf... The delete flow's onConfirmed → lock() is supposed to be THE lock. A concurrent user lock during delete — UnlockController.lock() presumably has a guard (isTerminalWipe / lockIf). Can't fully verify without reading UnlockController. This is getting deep into pre-existing machinery. The flag-before-drain ordering is worth noting as a P3/P2: setting terminalTeardownDone before the drain completes means a concurrent stop() skips waiting for a teardown that hasn't finished — contradicting "blocking until they have run". Severity: bounded (delete racing lock; the drain is ms-scale; socket disconnect completes anyway on the worker; runtime.close doesn't invalidate the socket). I'll report as P3/P2 with the concrete interleaving. Actually — the concrete bad outcome: stop() returns, runtime.close() completes, and THEN the worker's drain emits cover frames — cover frames emitted after the vault is locked. R-U3-5: "A vault that is locked emits nothing." Emitting cover frames post-lock! But wait — the drain exists precisely to emit covers before disconnect; if a lock raced a delete, the delete's drain emitting after the user's lock() returned... the session was already being destroyed by the delete. The emission is of frames whose real counterparts went out pre-lock. Pairing-wise fine. The "lock emits nothing" is about the locked vault — here the delete flow legitimately owns teardown. Honestly this is marginal. P3.

  Better: move flag set AFTER coverTraffic.stop? Then re-entry deadlock: stop() (from delete's onConfirmed → lock) would see flag false → dispatch teardown to worker and block 250ms → the worker is blocked in onConfirmed→lock→stopSession→transportLock? No wait, stopSession acquires transportLock first, THEN calls stop(). The worker (in delete flow) calls onConfirmed → lock() → stopSession → synchronized(transportLock) — if free, proceeds → coordinator.stop() ON THE WORKER THREAD → flag false → runTerminalTeardownOnConfinedWorker → dispatches to confined (itself) and blocks 250ms → timeout → fallback CAS succeeds (worker can't claim, it's blocked) → runs terminal() on calling thread (the worker itself) → coverTeardown → flag false → set true → coverTraffic.stop runs AGAIN (second drain — empty, transportInvalid already true → invalidateTransport runs again → ws.disconnect() again — harmless). So with flag-after-stop ordering, the re-entry costs 250ms and a redundant teardown — which is exactly why they put the flag first. The current ordering is defensible; the skip-wait race is the cost. P3 it is.

  Now, fifth — verify the confinement claim about **no suspension point between ws.sendMessage and inFlight.add** — one more subtlety: `cover()`'s prologue calls `teardown.withLock { transportInvalid }`. ReentrantLock.lock() can BLOCK (if teardown fallback on another thread holds it). Blocking isn't suspension — the worker thread waits, teardown can't be "inside" the slice anyway since it's the thing holding the lock. Fine.

  And **buildCover calls recipient() → DecoyAuthStore(rt).accountId → runtime.read → stateLock.withLock** — could block on vault lock held by a flush... bounded. Fine.

  Sixth — **point 3: no timing dependence remains.** In DecoySendPairing: gapMs uses SecureRandom; sleep seam. No deadlines. Verified by reading the class: no DRAIN_TIMEOUT. ✓. But MessagingCoordinator has TEARDOWN_QUIESCE_MS=250 — a wall clock on waiting for the worker, declared. ✓ declared.

  Seventh — **point 2: tails cannot suspend**: publishOutgoing/publishReceipt are non-suspend private funs. ✓ Verified. And cover guarded at all three sites. ✓ (1210, 1422, 1574).

  But wait — the success signal semantics: `ws.sendMessage` returns OkHttp enqueue result. "handed to the relay" is overstated: it's "accepted into OkHttp's queue". The frame can still die in the queue (socket failure before writer flush), while the cover goes out 5-50ms later — possibly on a RECONNECTED socket (WsClient auto-reconnect replaces webSocket). Then the relay sees a lone decoy. Window: socket death in the 5-50ms gap. This is the same class the W1 fix claims to close ("no real frame behind it"). The guard closes the *detectable-at-enqueue* cases; the enqueue-to-write asynchrony remains. The kdoc is actually honest about OkHttp's writer elsewhere (GAP_MIN discussion), but the W1 fix claim "handed to the relay" overstates. Is it a finding? Round 4 explicitly claims "a decoy with no real frame behind it" is closed. The residual: real frame enqueued (true), socket dies before flush, cover emitted after gap — where? emit → send → ws.sendMessage → if webSocket still the dead one → false (dropped silently); if reconnect already replaced it → goes out on the new socket → lone decoy on the wire. WsClient reconnect with backoff (1s+ base) — the gap is 5-50ms, so reconnect within the gap is impossible (reconnect backoff ≥1s? check WsClient reconnect delay — BASE_BACKOFF 1s). So the cover would be emitted to the dead/null socket → dropped → the pair just vanishes: real frame lost in queue + no cover → observer sees nothing. That's not a marked frame (nothing on the wire). Actually wait — if the real frame DID get written before the socket died, then cover dropped → unpaired real frame — but that's a connection-death event, normal jitter, declared ("missing frame is one low-grade anomaly"). So the lone-decoy-on-new-socket requires reconnect inside 5-50ms — not possible with ≥1s backoff. OK, not a finding. Good.

  Eighth — **R-U3-1 durability**: between flushSendRatchet and ws.sendMessage — pre-U3 sequence. The cover call happens after. ✓. But note: `if (publishOutgoing(...)) coverTraffic.cover(envelope)` — cover is suspend; after admission it SLEEPS 5-50ms on the confined worker coroutine (suspending, worker free). deliverText's coroutine is occupied but worker free. No real-send delay. ✓.

  But — hmm, one more: **the cover() call holds the send coroutine alive for the gap; message state stays SENDING until relay ack regardless. Fine.

  Ninth — **R-U3-1 reordering**: cover frames and real frames of subsequent sends interleave — declared.

  Tenth — **`ensureProvisioning` under teardown lock; `provision()` runs on Dispatchers.IO** — the provision job touches vault runtime (provisionIfNeeded → runtime.mutate). If stop() runs while provisioning is mid-flight: stop cancels provisionJob under the lock → coroutine cancelled → but provision may be blocked in a non-cancellable HTTP call... it checks cancellation at suspension points. If it's mid-mutate (committing decoy credentials) when stop() drains and disconnects — the mutate completes (RAM), then UnlockController closes runtime → final reseal persists it. Fine — durable state consistent.

  But: **stop() cancels provisionJob, but `provisioning` AtomicBoolean is reset in the job's finally — fine. And after stop, ensureProvisioning refused. OK.

  Eleventh — **`DecoySendPairing.stop` runs `invalidateTransport` inside the `teardown` lock (finally)** — ws.disconnect() under lock; cover()'s emit also under lock (finish) — no deadlock (same thread or fallback thread; ReentrantLock not held by others across suspension). ✓

  Twelfth — **Quiesce mid-drain reconnect correctness (point 4)**: "correctness against a socket that reconnects mid-drain". During quiesce's drainLocked, emit sends to `webSocket` — the CURRENT socket. If WsClient's auto-reconnect fires mid-drain (reconnectJob was armed from an earlier drop), openSocket() replaces webSocket — covers emitted after that point go out on the NEW socket while their real frames went on the old → split pair — the exact P1 quiesce was created to prevent! Interleaving: socket dropped 1s ago (reconnect backoff pending); real send before the drop was paired, sleeping in gap; Tor toggle → quiesce on worker → drain starts; reconnectJob (on session scope, some dispatcher) fires concurrently → openSocket replaces webSocket → drain's emit → new socket. Split pair. Hmm — but wait: does reconnectJob fire while connectionState... the drain is fast (ms). The reconnect fires on its own schedule independent of quiesce. The window is the drain duration (sub-ms per frame). Small but the class of race is exactly what quiesce claims to eliminate: quiesce serializes against SENDS (worker) but NOT against WsClient's own reconnection machinery, which runs off-worker. The real fix would be to cancel reconnectJob before draining (like disconnect does). Actually the swapTransport lambda calls ws.disconnect() AFTER the drain — disconnect cancels reconnectJob — but a reconnect that fired DURING the drain (before disconnect) slips through. Concrete sequence:
  1. Socket drops at t=0; WsClient schedules reconnect at t=1000ms (backoff).
  2. t=990: real send → ws.sendMessage → webSocket null → false → markFailed, NO cover (guard works). Hmm — sends during a down socket are refused. So pairings in the register at drain time all had their real frames accepted by a LIVE socket. For a split pair, need: real frame accepted on socket S1 (live), pairing sleeping; S1 drops (unexpectedly — network), reconnect scheduled; reconnect fires and dials S2; THEN quiesce drains → emits cover on S2 → split pair (real on S1/lost, cover on S2). But also — even WITHOUT quiesce: pairing wakes from gap at t+30ms and emits cover on S2 — same split. That's the inherent gap-vs-reconnect race, present since round 3, independent of quiesce. The only way to fully close: emit cover only if the socket identity matches the one that took the real frame. Not implemented anywhere. Is this declared? The round-3 ruling declared "split pair" P1 for the Tor toggle; the reconnect-mid-gap variant is the same signal via a different trigger. The docs claim quiesce fixes split pairs for transport swaps, but the general case (auto-reconnect mid-gap) remains — cover emitted on a different connection than its real frame. Hmm, is this reachable? Auto-reconnect backoff base 1s; gap is 5-50ms. For the cover to land on S2, the reconnect must complete within the 5-50ms gap AND the socket must have dropped within that same gap (real frame accepted → socket dies → reconnect → all in ≤50ms). Reconnect backoff starts at 1s, so no. Unless reconnectAttempts>0 shrinks? Backoff grows, never below 1s. So the sequence requires S1 to die ≥1s before... but then the pairing would have woken from its 50ms gap long before the reconnect. So NOT reachable via auto-reconnect timing. Good — no finding.

  But the quiesce-specific one: quiesce drains covers onto S1 (still connected), then swapTransport disconnects S1 and dials S2. The drained covers were enqueued to S1's OkHttp queue; disconnect → close() → graceful close flushes queue... OkHttp close(): "Attempts to initiate a graceful shutdown... the queue of messages is transmitted before the close frame". So covers go out on S1 before close. But grace: if the socket is unhealthy, close may not flush; the covers are dropped → unpaired real frames correlated with Tor toggle. Best-effort; the wire-level guarantee depends on OkHttp flush before TCP close. Marginal; the alternative (wait for flush confirmation) isn't available in OkHttp. I'd call it declared-adjacent; not reporting as separate finding, maybe a P3 note.

  Now — U1 file sweep. The scope says review U1 files too, but those were reviewed over six rounds and MERGED per the spec table. This is U3 round 4; the prompt's file list is stale U1 boilerplate. Budget-wise I'll do a quick targeted sweep: VaultState.kt TAG_DECOY codec + DecoyState isEmpty + wipe — but honestly, that was U1's six rounds. The prompt's invariant list (register-before-commit, counter reservation — note: counter reservation was DELETED per §3.0, so invariant #2 in the prompt is stale too) confirms the prompt template is stale. I'll note that and focus findings on U3. Quick spot checks only.

  Now the tests. Delegate to an explore subagent: audit DecoySendPairingTest.kt (35 tests), especially the four tripwires and the two confinement behavioral tests, answering per-test "would this still pass if the behavior it names were broken?" Also check the tripwire claims in u3-fix-r4-composed.md (W6 list) against the test source.

  Let me launch that subagent now, and meanwhile do the U1 spot check myself.

• The code side is mapped. Now I'll delegate the test-file audit while I spot-check the codec wiring.

