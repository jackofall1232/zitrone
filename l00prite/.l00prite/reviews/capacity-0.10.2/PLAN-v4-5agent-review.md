{
  "summary": "Five-agent DESIGN review of plan v4 (single-writer abandon) — v1, v2, v3 all rejected",
  "agentCount": 6,
  "logs": [],
  "result": {
    "lenses": [
      "enqueued",
      "composition",
      "routes",
      "arithmetic",
      "threat"
    ],
    "synthesis": "# BLOCKING\n\nRanked. Everything below was re-derived at HEAD `700794fb`, not accepted from a lens.\n\n**B1 — P1 — `ENQUEUED` + terminal flush can permanently destroy a delivered attachment. Four lenses, independently.**\nVerified at `/root/zitrone/apps/android/app/src/main/java/com/zitrone/app/net/WsClient.kt:165-171`:\n\n```kotlin\nfun disconnect() {\n    intentionallyClosed = true\n    reconnectJob?.cancel()\n    webSocket?.close(CLOSE_NORMAL, \"client closing\")   // OkHttp GRACEFUL — queued frames are written\n    webSocket = null                                    // …and the identity guard now rejects the ack\n    _connectionState.value = ConnectionState.DISCONNECTED\n}\n```\nplus `socketListener.onMessage` at `:258-261` — `if (webSocket !== this@WsClient.webSocket) return`. The two compose into the worst case: the teardown **makes delivery more likely and the acknowledgement impossible**. So `ENQUEUED`-at-flush-time is systematically populated with entries the relay already owns, and `CONFIRMED` can never arrive for them. Recipient redeems → 404 → `MessageRepository.attachmentUnavailable` (`:202-210`, sets `bytes = null`, terminal). Sender's repository is RAM-only and dies with the lock. Silent to both parties.\n**Must change:** the flush must never abandon `ENQUEUED` (see the ruling), and the plan's sentence \"Neither is data loss in the destroy-a-live-blob sense, provided `CONFIRMED` is excluded\" must be deleted, not softened.\n\n**B2 — P1 — the terminal reclaim flush cannot complete. Five lenses.**\n`CoverTrafficWorker.runTerminalConfined(teardown: () -> Unit)` (`CoverTrafficWorker.kt:140-156`): the body is **non-suspending**, so a flush inside it cannot await an abandon at all — it can only `scope.launch`. It waits `TERMINAL_TEARDOWN_WAIT_MS = 250L` (`:201`), then at most 250 ms more, and **returns while the teardown is still running** (`:152-155`). `stopSession` (`ZitroneApp.kt:963-971`) is `try { coordinator.stop() } finally { it.runtime.close() }`; `VaultRuntime.read` is `check(!closed)` (`VaultRuntime.kt:105-106`); `ApiClient` reads the bearer live per call (`ApiClient.kt:104-105, 443` → `VaultAuthStore.accessToken` → `runtime.read`). A Tor/I2P abandon RTT is seconds. Result: the flush reclaims at most what completes inside ~500 ms; everything else throws `IllegalStateException` into `runCatching`. This is v3's decisive defect by a new route.\n**Must change:** mechanism 2 as specified is not implementable. Either the flush gets a bearer/scope that outlives `runtime.close()` (which the plan names as the thing it does not need), or it is deleted (recommended — see v5).\n\n**B3 — P1 — \"duress wipe does not run the reclaim flush, by design and by construction\" is false as a construction claim. Four lenses, with one important correction.**\n`AppContainer.runTerminalBurnLocked` (`ZitroneApp.kt:607-621`) opens with `unlockController.lock()` → `lockCurrent` → `stopSession` → `coordinator.stop()`, and then *asserts* the quiesce (`if (session.value != null) throw …step(\"session-quiesce\")`). Construction routes duress straight through the flush host.\n**Correction the majority missed** (arithmetic lens got this right): `lockCurrent` opens `val session = current ?: return` (`UnlockController.kt:124`). On the dominant production path (auto-lock → lock screen → duress credential) `current` is already null, so `stop()` — and the flush — already ran at the *earlier ordinary lock*, and the burn itself emits nothing new. The claim is therefore not \"false on every duress\" but **\"true only by a state accident, on a path where the code is written assuming the opposite\"**: `MainActivity.kt:1143` reaches the lock screen with a session already live (\"A refused build (a session already live) — route to the lock gate\"), the byte-for-byte gate calls `runTerminalBurn` over a real published session, and round 6 added the `lock()` precisely to quiesce a live session.\n**Must change:** delete \"by construction\" and either gate the flush on `isTerminalWipe()` explicitly — which re-imports the duress-gating complexity mechanism 2 claims to dispose of — or remove the flush.\n\n**B4 — P1 — step 1 deletes the only network-independent bound, and the plan never enumerates the two non-throwing `publishOutgoing == false` exits. Three lenses.**\n`publishOutgoing` (`MessagingCoordinator.kt:480-505`) has exactly two `return false` routes that do **not** throw, so `runCatching{}.onFailure` (route (c)) never sees them:\n- `!contactExists` → `messages.discard(messageId); releaseDeposit(messageId); return false` — **no abandon**;\n- `ws.sendMessage` false → `messages.markFailed(messageId); return false` — **no abandon**.\n\nNeither appears anywhere in v4. Step 1's only owner rule is \"`STORED`/abandon-on-failure after\", and a `false` return is not a failure. Meanwhile deleting `reuseToken`/`reuseKey` removes the stable `blobId` that `ON CONFLICT (blob_id) DO NOTHING` (`server/internal/db/store.go:283-292`) uses to cap a message at one row regardless of k retries and regardless of the network.\n**Must change:** enumerate every `publishOutgoing` exit inside the reversal commit — and see B4′.\n\n**B4′ — P1 — the atomic remove-and-decide destroys the token *before* the abandon is known to have succeeded. Two lenses (threat F4, routes F2); this is the strongest single finding in the pass and neither lens named it as a mechanism.**\n`abandonBlobQuietly` swallows every failure (`MessagingCoordinator.kt:435-441`), and mechanism 1 hands the entry to exactly one party by `remove`. So an abandon that 429s, 401s or dies with the link leaves a blob with **no token anywhere in the process**. In the current tree the memo survives, so a later attempt (or a later abandon) can still reclaim it. Combined with fresh-secrets-per-attempt this converts a self-healing 1-row residual into k permanent 8 MiB rows, on the exact failure the retry button exists for, where the abandon's failure is *correlated* with the send's.\n**Must change:** removal must be on **confirmed 204**, not on decision.\n\n**B5 — P2 — abandon shares one 60/min bucket with deposit and redeem; step 7 makes it worse. Five lenses.**\n`handlers.go:83` `blobLimit: ratelimit.New(60, time.Minute, …)`, consumed by `DepositBlob` (`blobs.go:93`), `AbandonBlob` (`:152`) and `RedeemBlob` (`:172`); `handlers.go:87-91` records that behind the overlay sidecars per-IP degrades to **one global bucket**. `AbandonBlob` is `RequireAuth` (`cmd/server/main.go:120`). So \"costs nothing\" is false: a flush burst can 429 the user's next upload *or a third party's redeem*, which surfaces as terminal `attachmentUnavailable` for a live blob.\n**Must change:** a separate abandon limiter server-side (v1 mandated it, v4 dropped it) before any bounded-concurrency flush exists.\n\n**B6 — P2 — the flush never runs on account delete and is unauthenticated on revoke. Two lenses.**\n`deleteAccountAndWipe` calls `coverWorker.runTerminalHere(::coverTeardown)` (`MessagingCoordinator.kt:1942`), winning the `terminal` CAS; the later `stop()` → `runTerminalConfined` returns at `if (isTerminal) return` (`CoverTrafficWorker.kt:141`). `onSessionRevoked` calls `api.clearTokens()` (`:2380`) **before** `onForcedLogout?.invoke()` (`:2394`), so every abandon on that path 401s. Two of five session ends get nothing; neither is in the residual list.\n\n**B7 — P2 — commit boundary: step 2 lands the owner-exit abandon while mechanism 3's table is only `UPLOADING`/`STORED`.**\nOne lens (composition CX-2). Not forced by step 1's \"abandon-on-failure\" wording, but the step list invites the reading \"any surviving entry at the owner's exit is abandoned\", which at that commit destroys the blob of every *successful* send — and nothing in the suite can construct `MessagingCoordinator` (`AttachmentDepositReuseTest.kt:110-118` says so in its own kdoc), so no gate would catch it.\n**Must change:** `ENQUEUED`/`CONFIRMED` must land in the same commit as any owner-exit abandon rule.\n\n**B8 — P3 — `CONFIRMED` has no removal rule.** Two lenses. The tree's `releaseDeposit` kdoc (`:412-419`) states the trade explicitly (\"Without this the map grows for the process's lifetime\"), and `AttachmentDepositWiringTest` pins `releaseDeposit(messageId)` at exactly 3 call sites with the rationale \"The trade this fix must NOT make: disk orphans for unbounded heap\" — the class step 1 deletes. Under v4 each successfully-sent attachment retains a live 32-byte redemption capability for the process lifetime.\n\n**B9 — P3 — step 8 misfiles the one unambiguous improvement as a comment fix.** `ApiClient.abandonBlob`'s kdoc (`ApiClient.kt:280-282`) claims \"a contact deleted mid-send\" is covered; source shows that branch abandons nothing. An implementer told to \"fix the kdoc\" will edit the comment and leave the route leaking. This route is fixable **today**, with one `abandonBlobQuietly` call, needing none of mechanisms 1–3.\n\n# THE ENQUEUED RULING\n\n**Ruling: the terminal flush must NOT abandon on `ENQUEUED`. Not as a default, not as a tunable — as an invariant, in the same class as \"never abandon `CONFIRMED`\".**\n\n**Yes, one branch causes DATA LOSS, and it is not symmetric with the other.**\n\n- **Flush abandons (Branch A): DATA LOSS, not an orphan.** Proven at source, not inferred. `disconnect()` closes gracefully, so the frame the flush is deciding about has *probably already been written*; `webSocket = null` plus the `onMessage` identity guard then makes the relay's `message.stored` unobservable **by construction**, so those entries can never be `CONFIRMED` and are indistinguishable at flush time from entries that were never written. Excluding `CONFIRMED` therefore excludes nothing: the teardown *manufactures* the ambiguity and Branch A resolves it destructively. The blob dies; the envelope is store-and-forwarded to the recipient; the recipient gets a terminal `UNAVAILABLE`; the sender's copy died with the RAM-only repository at lock. Unrecoverable, silent to both parties.\n- **Flush declines (Branch B): ORPHAN ONLY.** Up to 8 MiB of relay disk for ≤96 h — the same category the design already concedes for crash, duress and unreachable network, and exactly equal to what the current tree does today. It cannot destroy anything.\n\nFrequency does not rescue Branch A. Its error fires whenever a lock lands within one relay RTT of a send (Tor 0.3–2 s, I2P 1–5 s) — \"send a photo, press the power button\" with immediate auto-lock is a routine gesture, not a corner. Branch B's error is commoner but costs only disk. Wrong-but-cheap at higher frequency beats wrong-and-unrecoverable at lower, and the value ordering of the product (\"a capacity fix must not destroy delivered content\") settles it before any frequency argument.\n\nBranch A has a second, independent cost the plan never considers: it puts an authenticated `POST /blobs/abandon` **after** a `message.send` on the same authenticated channel, letting the conceded relay label that envelope as the attachment envelope for that blob — precisely what `mediaType = MEDIA_TEXT` exists to prevent (`MessagingCoordinator.kt`, the envelope construction comment: \"NEVER MEDIA_IMAGE/MEDIA_FILE — the relay must not be able to tell an attachment from conversation text\"). Today an abandon is only ever emitted on routes where **no envelope was published**, so this channel does not exist. Branch B preserves that property for free.\n\n**Is there a third option — information the flush lacks?** Partly, and it must be honest about its price:\n1. **The only sound signal is negative and must be captured *before* `ws.disconnect()`.** `CoverTraffic.stop` drains admitted pairings **while the socket is still live** and only then runs the disconnect lambda (`MessagingCoordinator.kt:880-901`), so a live-socket window does exist inside the terminal task. Snapshot a monotonic socket epoch at `send() == true` plus `WsClient.outboundQueueBytes()` (`WsClient.kt:218`, OkHttp `queueSize()`); an entry may be abandoned **only** on positive evidence of non-delivery — that socket ended in `onFailure` (abrupt) with its outbound queue non-empty. Everything else stays `ENQUEUED` and is never abandoned. Note the trap: reading `connectionState`/`outboundQueueBytes()` *at flush time* always returns `DISCONNECTED`/`0` because `disconnect()` nulled the socket, so a naive check concludes \"discarded\" for every entry and takes Branch A universally.\n2. **The relay already answers the question**, and a bounded inbound wait for outstanding `ENQUEUED` acks inside that live-socket window would convert them to real `CONFIRMED`. Its honest price is **a new explicit bound inside a teardown whose existing bound was justified on the premise that it contains no work to bound** (`CoverTrafficWorker.kt:125-132`: \"the alternative to a bound here is a vault lock that can hang and never wipe its keys\"). That is a real cost and the plan must state it rather than claim the bound is free.\n\nNeither third option changes the ruling; they only shrink Branch B's residual. **Ship Branch B; treat (1) as optional later refinement; do not ship (2) without pricing the bound explicitly.**\n\n# VS CURRENT TREE\n\nReclaimable orphans per attachment message, after all client action. A = flush abandons `ENQUEUED`, B = it does not. \"v4 as written\" means the plan's own text, including the two `publishOutgoing` exits it never enumerates.\n\n| Route | Current tree | v4 as written | Verdict |\n|---|---|---|---|\n| R1 non-durable ratchet flush | 0 — route (a) abandons | 0 | **equal** |\n| R2 throw after deposit (incl. lost response after commit) | 0 — route (c), memo | 0 | **equal** |\n| R3 contact deleted mid-send | **1**, permanently (`discard` + `releaseDeposit`, token gone) | 0 | **v4 better** |\n| R4 socket down at hand-off (`sendMessage` false), no retry | 1 | plan is silent (non-throwing `false` ≠ \"failure\"); 0 only if the exit is enumerated | **v4 better only after B4 is fixed** |\n| R5 socket down, k retries, abandons landing | ≤1 forever (stable `blobId`; retry #1 409s → route (c) deletes it, #2 re-deposits) | 0 | **v4 better** |\n| R6 socket down, k retries, **abandons failing** (correlated) | **≤1**, network-independently, and self-healing | **k**, permanently unreclaimable (token destroyed by remove-on-decide, B4′) | **v4 WORSE, by factor k** |\n| R7 lock/logout while `UPLOADING` | 1 | 1 (flush inert past ~500 ms, B2); plus the plan's own flush-then-late-deposit | **equal at best** |\n| R8 `ENQUEUED`, frame discarded unwritten (the headline route) | 1 | A: 0 *only as often as B2 permits*; B: 1 | **A marginally better, B equal** |\n| R9 `ENQUEUED`, frame written + committed, lock inside one RTT | 0 orphans, 0 loss | A: 0 orphans, **content destroyed**; B: 0 orphans, 0 loss | **A strictly worse in a category the tree does not have** |\n| R10 account delete / revoke | 1 | 1 (flush skipped / 401, B6) | **equal** |\n| R11 duress burn | 0 relay traffic | flush runs when a session is live (B3) | **v4 worse on the security property** |\n| R12 crash | 1 | 1 | **equal** |\n| R13 `CONFIRMED` then burn/TTL/contact-delete | 1 | 1 (`CONFIRMED` excluded) — step 5's eight hooks buy nothing here | **equal** |\n| Retry UX (not an orphan) | first retry after a landed deposit **always** 409-fails and deletes the row; a live cross-attempt destruction race exists | fixed by construction | **v4 better — and this is the reversal's real justification** |\n\n**Net.** v4 is unambiguously better on **R3 and R5**, equal on six routes, and **worse on R6, R9 (Branch A) and R11**. On the route it is built to fix — R8 — it is *equal to the tree under Branch B*, and under Branch A its improvement is throttled by B2 while its cost (R9) is not.\n\nv3 died on \"it loses a reclaim the tree already performs\". **v4 does not lose one — it loses the tree's only network-independent *bound*.** The tree caps a message at one row by construction (`ON CONFLICT` over a stable `blobId`); v4 replaces that with a best-effort authenticated DELETE whose failure mode is silence, whose failure is correlated with the failure that triggered it, and which destroys its own retry capability when it fails. On the metric item 5 exists to bound — bytes on CX23 — **v4 as written is worse than the current tree**, and the fix for that (B4′, confirmed-removal) is one line of semantics, not a redesign.\n\nThe honest argument for the reversal is not orphan arithmetic, which v4 currently loses. It is that the memo carries a **live cross-attempt data-destruction race**: attempt 1's `abandonBlobQuietly` launches unordered on `scope`, the user taps retry, attempt 2 re-deposits the same `blob_id` and publishes, and attempt 1's in-flight abandon then deletes the row attempt 2 just published. Fresh secrets per attempt is the only thing that removes it. v5 should be argued from that.\n\n# CONVERGENCE\n\nIndependent agreement, in order of weight:\n\n1. **`ENQUEUED` + flush is data loss, and the plan's \"neither is data loss\" is false — 4 of 5 lenses** (enqueued F1, composition CX-3, routes F3, arithmetic F1), reached by three different routes (`disconnect()` nulls the socket so the ack is dropped / `close(1000)` is graceful so the frame *is* written / the relay store-and-forwards to an offline recipient). **All three mechanisms are real and they compound.** Confirmed at `WsClient.kt:165-171, 258-261`.\n2. **The terminal flush cannot complete — 5 of 5 lenses.** Same three source facts every time: the 250 ms×2 bound that returns while the task runs, `runtime.close()` in `stopSession`'s `finally`, and the live per-call bearer read. Composition additionally caught the decisive one nobody else stated: the teardown body is `() -> Unit`, so **the flush cannot await an abandon at all**.\n3. **The duress claim is unsupported — 4 of 5 lenses**, each independently walking `runTerminalBurnLocked` → `lock()` → `lockCurrent` → `stopSession` → `stop()`.\n4. **The shared 60/min `blobLimit` makes \"costs nothing\" false — 5 of 5 lenses**, and all five note step 7's bounded concurrency makes it worse rather than better.\n5. **Deleting the memo without confirmed-removal multiplies orphans on the correlated-failure retry path — 4 of 5 lenses** (composition CX-4, routes F2, arithmetic F2, threat F4), all with the same k-vs-1 arithmetic.\n6. **The `cleanupRequested` bit has almost no reader — 2 lenses independently** (composition CX-5, arithmetic F7): for `ENQUEUED`/`CONFIRMED` entries the owner has already exited, so step 5's eight hooks on the hardened burn surface change no outcome on any route the plan can name.\n7. **The flush never runs on account delete (`runTerminalHere` wins the latch) — 2 lenses** (composition CX-6, routes F4).\n\n# WRONG CLAIMS\n\nPlan claims falsified against source, with the correct behaviour.\n\n| Plan claim | Correct behaviour |\n|---|---|\n| \"**no `markFailed` exists on any disconnect path**\" (mechanisms 1 and 3, load-bearing for the whole `ENQUEUED` story) | **FALSE.** `publishOutgoing` calls `messages.markFailed(messageId)` on the `ws.sendMessage == false` route, and `deliverAttachment` calls it on routes (a) and (c). The true, narrow statement: *nothing marks failed when `send()` returned **true** and the frame is later discarded.* Building on the broad version is **why v4 misses the `send() == false` route entirely** (B4). Caught by one lens only (arithmetic). |\n| \"Neither is data loss in the destroy-a-live-blob sense, **provided `CONFIRMED` is excluded — which it is**\" | **FALSE.** `CONFIRMED` means the ack *arrived*; relay ownership is the ack *sent*. `disconnect()` structurally severs the two, so the teardown systematically populates a would-have-been-`CONFIRMED` class recorded as `ENQUEUED`. Branch A destroys those. |\n| \"**duress wipe does not run the reclaim flush, by design and by construction**\" | **FALSE as a construction claim.** `runTerminalBurnLocked`'s first statement is `unlockController.lock()`. It is saved only by the *state* fact `current == null` at `UnlockController.kt:124`, which holds on the common path and demonstrably fails on the refused-build route (`MainActivity.kt:1143`) and in the load-bearing burn gate. |\n| \"`coordinator.stop()` … dispatches the terminal task onto the confined worker, **blocking until that task runs**\" | **Imprecise in the load-bearing direction.** Bounded at `TERMINAL_TEARDOWN_WAIT_MS = 250` per wait, returns **with the task still running**, may run teardown on the calling thread inside `synchronized(transportLock)`, and **returns immediately without dispatching** when `isTerminal` is already set. It bounds *scheduling*, per its own kdoc — \"It bounds scheduling, not cover-side work\". |\n| \"a terminal reclaim flush riding that existing bound needs **no `NonCancellable`**, no process scope, no captured credential\" | **FALSE twice over.** The helper *already* launches with `confined + NonCancellable` (`CoverTrafficWorker.kt:145`) — so the claim misdescribes the construction it rides — and the flush needs strictly *more* than `NonCancellable`: a deferred `runtime.close()` or a captured bearer, else every abandon started after ~500 ms throws on the live `accessToken` read. |\n| \"`AbandonBlob` … **costs nothing and reveals nothing**\" | **Half false.** 204-either-way and reveals nothing about liveness: confirmed (`blobs.go:151-169`). But it is `RequireAuth` (401 on the revoke path) and spends a slot in a 60/min bucket **shared with deposit and redeem**, degrading to one global bucket behind the overlay sidecars. |\n| \"`lockCurrent` already carrying a bounded drain\" (offered as budget for the flush) | **True but inert.** `runBlocking { withTimeoutOrNull(drainTimeoutMs) { job.join() } }` runs *after* `stopSession`, i.e. after `runtime.close()`; the `NonCancellable` teardown coroutine is not a child of the session job, so the `join()` never waits for it. Zero budget. |\n| \"`AbandonBlob`'s **three-route undercount**\" (step 8) | **Mis-described.** `abandonBlobQuietly` has exactly **two** call sites. The kdoc's defect is not an undercount but a **false claim**: it names \"a contact deleted mid-send\", which is not implemented. An implementer told to widen \"two\" to \"three\" leaves the false claim standing and the route leaking. |\n| \"`STORED` … the deposit returned success (**or 409**)\" | **Vestigial after step 1.** With fresh secrets per attempt, no two attempts of this client share a token, so 409 `blob_exists` becomes unreachable from this client. (In the *current* tree it is reachable and is a live defect: the first retry after a landed deposit always 409s, lands in route (c), and deletes the blob it was reusing.) |\n| step 8's \"`sendAttachment`'s '1 week' (actual 96 h)\" | **Enumeration incomplete.** Two sites in `main`: `MessagingCoordinator.kt:1282` and `AttachmentCrypto.kt:23`. (One lens claimed a third in `ApiClient.redeemBlob`; that one does not exist — lens error, not a plan error.) |\n\nVerified correct and not to be re-derived: `ws.sendMessage` is an enqueue receipt; `stop()` joins no in-flight sends and `limitedParallelism(1)` serialises slices not coroutines; `markFailed` accepts `SENDING || SENT`; `MessageRepository.exists` is a flatten-and-scan returning `Boolean`; `burn` flips to `BURNING` before invoking hooks; `onMessageBurned` is `notifyPeer`-gated and the vault contact-delete passes `notifyPeer = false`; `clearAll` needs explicit iteration; `abandonBlob` has no `callTimeout` while both clients set `readTimeout(0)`; the never-a-standalone-`OkHttpClient` rule; `AttachmentDepositReuseTest.kt` holds both test classes and deleting the reuse params reds the whole test source set (v1's ordering error is genuinely fixed); the atomic `remove` genuinely yields one decider per entry.\n\n# CONFLICTS\n\n1. **Does the duress path run the flush?** Three lenses: outright FALSE. Arithmetic: unsupported, but saved by `current == null`. **Checkable** at `UnlockController.kt:124` (`val session = current ?: return`) against `MainActivity.kt:1143` (lock screen reachable with a live session). **Adjudicated:** arithmetic is precise, the other three overstate frequency — but the plan's claim is still false and unusable, because the safety rests on a state accident the code itself does not assume.\n2. **Was the enqueued frame written or discarded?** Enqueued lens: `disconnect()` \"sets `webSocket = null` **before** closing\" → ack dropped. Routes lens: `close(CLOSE_NORMAL)` is graceful → frame written. **Checkable** at `WsClient.kt:165-171`. **Adjudicated:** the enqueued lens has the *ordering backwards* (close is line 168, the null is line 169) but its conclusion survives — the ack is dropped by the identity guard regardless. Routes is right about the write. They are not alternatives; they compose into B1's worst case.\n3. **Is the tree better or worse on the retry route?** Arithmetic/routes: the tree converges to ≤1 row via the 409-forced abandon, so v4 regresses. Composition/threat: the tree's same mechanism is a live cross-attempt destruction race and a guaranteed wasted retry. **Checkable** at `deliverAttachment` route (c) + `store.go:283-292`. **Adjudicated:** both are true and not in tension. The tree buys its disk bound with a data-destruction race and a broken first retry; v4 buys correctness with a worse disk bound. **Only confirmed-removal (B4′) gets both.**\n4. **Does mechanism 2 need `NonCancellable`?** Composition says it already has it; arithmetic/threat say it needs it. **Checkable** at `CoverTrafficWorker.kt:145`. **Adjudicated:** it has it and it is not sufficient — the binding constraint is `runtime.close()`, exactly as v4 itself diagnosed for v3, and then reproduced.\n\n# VERDICT\n\n**DO NOT SHIP** — the `ENQUEUED` branch v4 leaves open is not a symmetric tradeoff but a choice between an orphan and permanent destruction of delivered content; the flush that is supposed to make the design work cannot complete, cannot even suspend, and does not run on two of five session ends; the duress claim is false as stated; and as written v4 is worse than the current tree on the disk metric item 5 exists to bound.\n\n# IF REJECTED — WHAT v5 MUST BE BUILT AROUND\n\nFour rounds have now failed the same way: **the client tries to decide, at teardown, using state it cannot observe, with a call it cannot complete, after destroying the record that would let it try again.** Patch any one of those and the other three kill the plan. v5 must be built around three mechanism changes, not fixes.\n\n**M1 — CONFIRMED-REMOVAL, NOT DECIDED-REMOVAL.** The registry entry is retired when the abandon returns **204**, not when a party wins the `remove`. Until then the token stays and the abandon is retryable and idempotent (`AbandonBlob` is an unconditional DELETE, so retries are free and reveal nothing). This is the single change that makes fresh-secrets-per-attempt safe: a failed abandon stops being a permanent orphan, k retries stop multiplying, and the tree's `ON CONFLICT` bound stops being something v4 has to give up. It also preserves mechanism 1's real property — one decider per attempt — because deciding and retiring become different events.\n\n**M2 — RECLAIM IS A LIVE-SESSION ACTIVITY, NOT A TEARDOWN ACTIVITY. DELETE MECHANISM 2.** Every defect in B2, B3, B6 and the whole flush-then-late-deposit residual is a consequence of doing authenticated network I/O inside a synchronous vault-key-wipe teardown. Replace the terminal flush with a **session-scoped reclaim queue** that drains continuously while the session is live: an entry that fails its abandon is re-queued with backoff and retried against a live socket, live bearer, open vault, and a rate limiter it can respect. At lock there is then normally nothing to flush; whatever remains is TTL, declared as a residual rather than raced for. This removes the duress question entirely (nothing runs at teardown, so nothing must be gated), removes the account-delete latch collision, removes the revoke-401 window, and removes the need for `NonCancellable`, a process scope or a captured credential — the properties v4 claimed without earning.\n\n**M3 — `ENQUEUED` IS NEVER ABANDONED, AND THE ONLY ADMISSIBLE EVIDENCE IS CAPTURED BEFORE THE SOCKET CLOSES.** Ruled above. If v5 wants to reclaim the enqueue-then-drop route, the signal must be **positive evidence of non-delivery** (socket ended in `onFailure` with a non-empty outbound queue, snapshotted per socket epoch at `send() == true`), recorded while the socket is live, and consumed by M2's queue. Absent that evidence: TTL. Any wait for `message.stored` inside teardown is a **new explicit bound on the vault key wipe** and must be priced as such, not hidden inside \"rides the existing bound\".\n\nGiven M1–M3, the parts of v4 that survive are exactly the parts every lens said to keep: the **per-attempt registry shape**, **single-writer abandon**, and **states that name observables**. Mechanism 2 does not survive.\n\n**Order of work for v5 (each independently shippable):**\n1. **Now, independent of everything:** add `abandonBlobQuietly` to `publishOutgoing`'s contact-deleted branch and correct `ApiClient.abandonBlob`'s kdoc. This is the one route where v4 is unambiguously better than the tree, and it needs none of the design.\n2. **Server:** separate limiter for `AbandonBlob` (v1 mandated it; v4 dropped it). Nothing that bursts abandons may land before this.\n3. **Registry + reversal in one commit**, with M1's confirmed-removal semantics, `ENQUEUED`/`CONFIRMED` in the **same** commit as any abandon rule (B7), an explicit removal rule for `CONFIRMED` (B8), and **every** `publishOutgoing` exit enumerated in the commit message and in code (B4).\n4. **M2's live-session reclaim queue**, replacing the terminal flush; `stop()` does nothing but stop accepting.\n5. **Optional, priced separately:** M3's epoch/queue evidence for the enqueue-then-drop route.\n6. **Out of scope but now blocking the honest story:** `markFailed` on disconnect. v4 is right that it is a worse user-facing bug than the orphan; it is also the only thing that would ever make `ENQUEUED` genuinely conditional. v5 should stop claiming conditionality until it exists."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design review"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesis"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "v4:enqueued",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a2fcb5127090bff08",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785417159148,
      "queuedAt": 1785417149077,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v4.md in full, then READ THE SOURCE and check its claims.\n\nNOTE ON CITATIONS: v4 deliberately contains NO line numbers. Three prior plans cited offsets that were\nwrong (v3's were uniformly 8 lines low — correct for a commit two befor…",
      "lastProgressAt": 1785417862009,
      "tokens": 136641,
      "toolCalls": 43,
      "durationMs": 702861,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — mechanism 3 / `ENQUEUED`, the undecided branch (flush-abandons)**\nSequence: `deliverAttachment` → `api.uploadBlob` OK → `flushSendRatchet` durable → `publishOutgoing` → `ws.sendMessage` returns true → entry marked `ENQUEUED`; owner proceeds into `coverTraffic.cover` (suspends in `sleep(gapMs())`, yielding `confined`). OkHttp writes the ~1–3 KB frame within ms; the relay com…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "v4:composition",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "ac6bf87726633ad21",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785417155453,
      "queuedAt": 1785417149078,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/server && grep -rn \"blobs/abandon\\|Abandon…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v4.md in full, then READ THE SOURCE and check its claims.\n\nNOTE ON CITATIONS: v4 deliberately contains NO line numbers. Three prior plans cited offsets that were\nwrong (v3's were uniformly 8 lines low — correct for a commit two befor…",
      "lastProgressAt": 1785417843275,
      "tokens": 141230,
      "toolCalls": 42,
      "durationMs": 687822,
      "resultPreview": "# FINDINGS\n\n**CX-1 — P1 — mechanism 1 (owner-exit abandon) × mechanism 2 (terminal reclaim flush): mutual backstops, both inert after `stop()` returns.**\nSequence: a send is suspended in `api.uploadBlob` (legitimately for minutes — `BLOB_UPLOAD_TIMEOUT_MS` = 10 min, and its own kdoc budgets 80–160 s of transfer). `VaultLockManager` fires → `UnlockController.lock()` → `lockCurrent()` → `stopSession…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "v4:routes",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a354c589ebaf1d8ec",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785417848527,
      "queuedAt": 1785417149078,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"onMessageBurned\" --include=*.kt apps/android/app/…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v4.md in full, then READ THE SOURCE and check its claims.\n\nNOTE ON CITATIONS: v4 deliberately contains NO line numbers. Three prior plans cited offsets that were\nwrong (v3's were uniformly 8 lines low — correct for a commit two befor…",
      "lastProgressAt": 1785418413264,
      "tokens": 131379,
      "toolCalls": 50,
      "durationMs": 564737,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — mechanism 2 / step 4 (\"a duress wipe simply does not run it\")**\nSequence: coercer forces an unlock → user enters the Pucker Burn slot-0 credential → `MainActivity.onBurn` → `AppContainer.runTerminalBurn` → `runTerminalBurnLocked` → `unlockController.lock()` → `UnlockController.lockCurrent` → `stopSession` → `MessagingCoordinator.stop()`. The duress wipe reaches `stop()` thr…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "v4:arithmetic",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a5047ef9f1e9cf235",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785417867189,
      "queuedAt": 1785417149078,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/test/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v4.md in full, then READ THE SOURCE and check its claims.\n\nNOTE ON CITATIONS: v4 deliberately contains NO line numbers. Three prior plans cited offsets that were\nwrong (v3's were uniformly 8 lines low — correct for a commit two befor…",
      "lastProgressAt": 1785418647079,
      "tokens": 136264,
      "toolCalls": 57,
      "durationMs": 779889,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — mechanism 3 / step 4 (`ENQUEUED` + terminal flush) — \"neither option is data loss\" is false; flush-abandon on `ENQUEUED` destroys a live attachment**\nSequence: user sends an 8 MiB photo → `api.uploadBlob` returns 201 (row live) → `publishOutgoing` → `ws.sendMessage` returns true → entry = `ENQUEUED` → the frame **is** written and the relay accepts it → the relay's `message.…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "v4:threat",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a17182c4f463f5268",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785418417915,
      "queuedAt": 1785417149078,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v4.md in full, then READ THE SOURCE and check its claims.\n\nNOTE ON CITATIONS: v4 deliberately contains NO line numbers. Three prior plans cited offsets that were\nwrong (v3's were uniformly 8 lines low — correct for a commit two befor…",
      "lastProgressAt": 1785418986293,
      "tokens": 142752,
      "toolCalls": 44,
      "durationMs": 568378,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — step 4 / MECHANISM 2 (\"riding the existing bounded dispatch-and-block\") — `CoverTrafficWorker.runTerminalConfined`, `TERMINAL_TEARDOWN_WAIT_MS`, `ZitroneApp.stopSession`, `VaultRuntime.close`**\nSequence: user locks (`VaultLockManager` → `unlockController.lock()`) → `UnlockController.lockCurrent` → `stopSession` = `synchronized(transportLock){ try { coordinator.stop() } fina…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesis",
      "phaseIndex": 2,
      "phaseTitle": "Synthesis",
      "agentId": "a8a639b70795eed21",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785419005251,
      "queuedAt": 1785418988445,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"fun attachmentUnavailable\" -A 8 /root/zitrone/app…",
      "promptPreview": "READ-ONLY. Five lenses reviewed DESIGN PLAN v4 at l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v4.md. Its three predecessors were all rejected\nDO NOT SHIP. Reports follow verbatim. SYNTHESIS, not re-review. ADJUDICATE AGAINST SOURCE.\n\n# BLOCKING\nRanked most severe first. Which lens(es), AGREE or CONFLICT, and exactly what must change.\n\n# THE ENQUEUED RULING\nThe design's one open …",
      "lastProgressAt": 1785419450407,
      "tokens": 123534,
      "toolCalls": 40,
      "durationMs": 445155,
      "resultPreview": "# BLOCKING\n\nRanked. Everything below was re-derived at HEAD `700794fb`, not accepted from a lens.\n\n**B1 — P1 — `ENQUEUED` + terminal flush can permanently destroy a delivered attachment. Four lenses, independently.**\nVerified at `/root/zitrone/apps/android/app/src/main/java/com/zitrone/app/net/WsClient.kt:165-171`:\n\n```kotlin\nfun disconnect() {\n    intentionallyClosed = true\n    reconnectJob?.canc…"
    }
  ],
  "totalTokens": 811800,
  "totalToolCalls": 276
}