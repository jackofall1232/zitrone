{
  "summary": "Five-agent DESIGN review of plan v5 (live-session reclaim) — v1-v4 all rejected",
  "agentCount": 6,
  "logs": [],
  "result": {
    "lenses": [
      "storm",
      "composition",
      "routes",
      "arithmetic",
      "threat"
    ],
    "synthesis": "# SYNTHESIS — PLAN v5, five lenses, adjudicated against source at `feat/0.10.2-capacity-fixes`\n\nVerdicts in: **DO NOT SHIP ×3** (routes, arithmetic, threat), **SHIP WITH CHANGES ×2** (storm, composition) — and both of those two explicitly refuse work item 3, which is the plan. There is no lens that endorses v5's core. Every source claim below I re-derived myself; where a lens was wrong I say so.\n\n---\n\n# BLOCKING\n\n**B1 — P1 — The reversal (item 3) trades a DB-enforced cap for a best-effort network one. ALL FIVE LENSES, INDEPENDENTLY, SAME MECHANISM.**\nVerified: `AttachmentCrypto.encrypt` sets `blobId = sha256(token)`; `Store.StoreBlob` is `INSERT … ON CONFLICT (blob_id) DO NOTHING` → `ErrBlobExists`; `MessagingCoordinator` memoizes `attachmentDeposits[messageId]` and passes `memo?.token, memo?.key` on every attempt. So on the current tree, **k retries of one message can only ever occupy one row, with no network call, no bearer, no limiter budget and no surviving session.** v5 deletes that and replaces it with one row per *attempt*, reclaimed only if k authenticated POSTs each get a 204 before the session ends. On the metric item 5 exists for (~2,079 orphans exhaust CX23), v5 lowers the exhaustion bar by a factor of k **in exactly the correlated-outage regime that produces retries across the user base at once**.\n*Must change:* the reversal cannot ship on the capacity argument. Either drop it (see v6 below) or re-argue it explicitly as a trade, with the k-multiplier priced.\n\n**B2 — P1 — M1 × M3 do not close: `ENQUEUED` and `CONFIRMED` have no retirement rule. FOUR LENSES INDEPENDENTLY** (storm F2, composition F3, arithmetic F7, threat F3).\nM1: retired *only* on a 204, \"never when a party wins the `remove`\". M3: `ENQUEUED`/`CONFIRMED` are never abandoned. Composed, the two states covering **every successful send** never retire. The tree has exactly the retirement path v5 deletes — `releaseDeposit(messageId)` at `onMessageStored`, `onMessageDelivered` and `publishOutgoing`'s contact-deleted branch, whose kdoc says \"**Without this the map grows for the process's lifetime**\", and whose count-of-3 is pinned by `AttachmentDepositWiringTest`. Threat adds the part that matters most for this product: each retained entry is the blob **redemption token**, and `RedeemBlob` is deliberately unauthenticated — the token *is* the capability. v5 holds, for the whole session, proof that an attachment was sent plus the live capability to fetch-and-burn it, after the tree would have dropped both. **M1 as written is not implementable.**\n\n**B3 — P1 — Item 6's mark has no named consumer: it is either dead code or a NEW destroy route. TWO LENSES INDEPENDENTLY** (routes F2, threat F2; arithmetic F8 reaches \"vestigial\" from the other side).\nVerified: `burnAll` filters only `state != BURNING`, so an in-flight `SENDING` message **is** burned; `burn` flips to BURNING then fires the `notifyPeer`-gated hook; `publishOutgoing` checks `contactExists` only, **never message existence**; `redeemAttachment`'s `onFailure` → `attachmentUnavailable` is terminal with no retry. So under M2's continuously-draining queue, a burn-all during an 8 MiB Tor upload marks the entry, the queue 204s the row, and `deliverAttachment` then publishes the envelope anyway → the recipient gets a permanent UNAVAILABLE for a blob that was live, silent to both parties. Under the other reading (mark read only by the owner-at-exit) the item is inert. **The plan does not say which.** This is the same shape as the finding that killed v2: the defect is in the step added for tidiness.\n\n**B4 — P2 — M2's queue admission rule is unwritten, and one reading destroys a live blob mid-send** (composition F4, routes F2 second half).\nNothing in v5 says only an entry whose owning attempt has *terminated* may be drained. `flushSendRatchet`'s transient-retry backoff suspends by design; a drain that takes a `STORED` entry during that window destroys the row and the owner then hands off the envelope. Same terminal outcome as B3. Write the admission rule; do not leave it to be inferred (v4 died on an unstated step boundary).\n\n**B5 — P2 — Item 2 is a hard prerequisite AND is what makes B1 easy to reach** (storm F3, composition F2, threat F5, arithmetic F6).\nVerified: `blobLimit = ratelimit.New(60, time.Minute, …)` gates `DepositBlob`, `RedeemBlob` **and** `AbandonBlob`, and `handlers.go` documents the sidecar collapse to one global bucket for all overlay users. Two consequences the plan does not price: (i) composition's sharp point — **today the shared bucket is accidentally self-limiting** (an abandon storm 429s the deposit too, so no extra row is minted); splitting the buckets creates the \"deposits land while abandons 429\" state that manufactures k rows; (ii) a separate abandon bucket **still collapses to one global bucket per sidecar**, so it stops abandons 429ing a third party's redeem (which is terminal `attachmentUnavailable` for a live blob) but does not stop abandons 429ing each other and stalling the drain. Also: item 2 must be **deployed** to CX23 and every relay peer before any client that bursts abandons ships, not merely merged.\n\n**B6 — P2 — M2 specifies \"re-queued with backoff\" and nothing else** (storm F4, plus storm F5 / routes F6 on the dispatcher).\nMissing and required: connectivity gate on `ws.connectionState == CONNECTED`, full jitter, capped ceiling, a **queue-wide** pause on 429 (not per-entry), a client ceiling well under 60/min, and an explicit statement that the drain does **not** run on `confined`. Verified: `confined = Dispatchers.IO.limitedParallelism(1)` carries the publish tail and the terminal cover-traffic path, and the tree's only flush-on-CONNECTED precedent (`flushPendingReceipts` at the `connectionState` collector) is on it — copying that idiom puts cleanup I/O in front of real sends and can push `runTerminalConfined` past `terminalWaitMs` into the unpaired-real-frame fallback. Item 5's \"bounded concurrency + `Call.timeout()`\" bounds one call, not the loop.\n\n**B7 — P2 — \"Retired on 204\" is a confirmed *request*, not a confirmed *removal*. ROUTES F4 ONLY — and it is correct.**\n`AbandonBlob` answers 204 whether or not a row existed. An `UPLOADING` entry whose abandon reaches the relay before the deposit's `INSERT` commits retires on a false 204, and the row then commits with **no token anywhere in the process** — the exact condition M1 exists to prevent, and *worse than the tree*, where the memo survives and the next retry's 409 fires a second abandon of the same id that does delete it. M1 needs: an entry whose deposit outcome was never observed does not retire on the first 204.\n\n**B8 — P2 — Item 1's ordering trap. THREE LENSES INDEPENDENTLY** (storm F6, composition F5 sub-note, threat F8).\nVerified at `publishOutgoing`: the contact-deleted branch is `messages.discard(messageId)` → `releaseDeposit(messageId)` → `return false`. The new abandon **must read `attachmentDeposits[messageId]` before `releaseDeposit`**; placed after, `abandonBlobQuietly(null)` returns at its first line and the whole item is a silent no-op that no test can catch (nothing in the suite constructs `MessagingCoordinator` — confirmed, and the wiring test's own kdoc says so).\n\n**B9 — P3 — M2 assumes a live bearer it cannot get** (threat F4). The only re-auth trigger is a WS handshake answered 401/403 → `onAuthExpired` → `start()`. Nothing refreshes the JWT proactively and nothing can while the socket is down — which is the failure mode the queue exists for. Past ~15 min every abandon 401s, and 401 ≠ 204, so under M1 those entries survive to session end and feed B1's multiplier.\n\n**B10 — P3 — the reversal deletes the only automated pin and replaces nothing** (arithmetic F5). All three of `AttachmentDepositWiringTest`'s assertions die with it, including `releaseDeposit(messageId)` count == 3. v1 required inverting the tripwire; v5 dropped that requirement while keeping the invariants that need it. **Adjudication of a lens dispute here — see W6.**\n\n---\n\n# THE STORM RULING (single ruling)\n\n**M1 and M2 are NOT jointly implementable as written — and the reason is not the storm.** The storm is a non-problem; the joint failure is B2.\n\n1. **The registry cannot grow to a size that matters.** Verified: `retry(messageId)` has exactly one call site, `MainActivity` → `MessageBubble`'s retry tap. There is no auto-retry of sends anywhere; the only automatic reconnect work is `flushPendingReceipts`. Growth is human-thumb-paced. Storm and arithmetic independently sized it at ~250–290 B/entry (not the plan's 96 B) → ~125 KB for a pathological session, ~2 MB for the fastest humanly reachable one — against the 8 MiB of plaintext the tree *already* retains per FAILED attachment in `Message.attachment.bytes`. The registry OOMs three orders of magnitude after the retained bytes do.\n2. **Therefore: no size bound, and NO DROP POLICY.** A drop trades ~290 B of RAM for 8,454,180 B of relay disk — 29,000 : 1 against — and is decided-removal wearing a different hat. Routes is the one lens that would allow a cap with oldest-first eviction; it is out-argued on arithmetic, and its own premise (\"a drop is observationally identical to session end\") is exactly why the drop buys nothing.\n3. **What actually retires an entry, none of which is a decision:**\n - (a) `abandonBlob` returns 204 **and** the deposit outcome for that attempt was observed (B7);\n - (b) **terminal success — `CONFIRMED`** (`message.stored` / `message.delivered`). Nothing will ever abandon it; this is precisely where the tree calls `releaseDeposit`. Retiring a capability nobody will use is not giving up on a blob;\n - (c) **the clock**: deposit time + `BLOB_TTL_HOURS` (verified 96 in `config.go`). The row is gone; the token is a liability.\n `ENQUEUED`-without-confirmation retires at session end, to TTL, never by drop.\n4. **The bound that IS required is on RATE, not size** — B6's governor. And threat F6 adds a cost no other lens priced and that this project has already ruled on once: an exponential-backoff abandon train is a deterministic, recognisable REST cadence correlated with send failure, firing while the user is idle, outside the `CoverTraffic.cover` seam entirely (verified: pairing covers `message.send` frames only). `coverTeardown`'s own kdoc rejects that class.\n5. **The unbounded thing in v5 is not the registry — it is the relay rows.** With fresh secrets per attempt, growth moved to the server, where the only bound that ever existed is the `ON CONFLICT` cap the reversal deletes. The plan's least-confident question is a symptom of B1, not an independent question.\n6. **Do not persist the queue.** Its contents are blob redemption capabilities; a durable outbox writes destruction-capable tokens plus the fact that attachments were sent, to disk. RAM-only is correct.\n\n---\n\n# VS CURRENT TREE — the headline claim\n\n> \"v5 reclaims strictly more than the tree does and **never less** … **No route in v5 destroys a blob the tree would have kept.**\"\n\n**First half: REFUTED, unanimously, and I confirm it at source.** Second half: **NOT PROVEN** — it holds only for a v5 that has B3's consumer and B4's admission rule written; as the plan stands, both are unstated and both have a reading that destroys a live blob.\n\nPer outgoing attachment message, k = retry taps, rows still live at session end:\n\n| route | current tree | v5 | delta |\n|---|---|---|---|\n| contact deleted mid-send | **1 orphan, guaranteed** — no abandon exists on that exit | 0 (item 1) | **v5 better** — needs none of the reversal |\n| flush-not-durable (route a) | abandon fired once, failure swallowed → 0 or 1 | retried to 204 → 0 | **v5 better** |\n| any throw after deposit (route c) | 0 or 1 | retried to 204 → 0 | **v5 better** |\n| `ws.sendMessage == false` | no abandon on that route; **the next retry 409s and route (c) abandons** → ≤1 | 0 if the abandon lands | **v5 better per attempt** |\n| **throw after `send()==true`** (`coverTraffic.cover` is `suspend` and sits *inside* `deliverAttachment`'s `runCatching`) | **live blob DESTROYED**, recipient terminal UNAVAILABLE | `ENQUEUED` never abandoned → 1 orphan | **v5 better (data loss → orphan)** |\n| **stale abandon racing a retry** — `abandonBlobQuietly` launches on bare `scope` while `sendAttachment`/`retry` launch on `confined` | **reachable data loss** on the shared `blobId` | impossible (per-attempt secrets) | **v5 better — the real motive for the reversal, never stated** |\n| **k retries, abandons unresolved at session end / lock / kill** | **1 row. Hard cap. No network needed.** | **k rows** | **v5 WORSE, linear in k** |\n| crash / process death after k attempts | 1 row | up to k rows | **v5 worse** |\n| deposit `callTimeout` with a late commit | reclaimed by the next retry's 409 + second abandon | permanent (false-204 retirement, B7) | **v5 worse** |\n| successful send, then TTL/read-burn | `releaseDeposit` drops the token | entry never retires (B2) | **v5 worse on capability lifetime** |\n| burn / burn-all mid-upload | no hook → recipient redeems fine | mark + live queue may abandon; envelope still publishes (B3) | **v5 worse, silently, for a third party** |\n\n**Honest summary: better on six routes, worse on five, equal on the rest. A trade, not a dominance.** The plan's two supporting sentences are false: \"Session ends with entries still queued — TTL. **Same as the current tree**\" and the same wording on crash. The tree's residual is one row per **message**, invariant in k and independent of the network; v5's is one row per **attempt**, conditional on k successful authenticated round-trips.\n\n**What the plan under-sells, and it is real:** the tree has two live-blob-destruction routes (rows 5 and 6 above) that v5's M3 + per-attempt secrets genuinely close. That, not capacity, is the strongest argument for anything in v5 — and it does not require the reversal.\n\n---\n\n# CONVERGENCE — independent agreement, most prominent first\n\n- **5/5 — the headline \"never less\" is false, by the identical mechanism** (`ON CONFLICT` cap vs per-attempt rows). Five lenses, five separate derivations, no shared wording. This is the finding.\n- **5/5 — \"Same as the current tree\" (residual + crash lines) is wrong in magnitude, not kind.**\n- **5/5 — items 1 and 2 are correct, dependency-free, and shippable today.**\n- **5/5 — M3 is correct at source and must be kept regardless of what happens to the reversal.** All five independently verified `disconnect()` closes gracefully then nulls `webSocket`, and every listener callback opens with `if (webSocket !== this@WsClient.webSocket) return` — delivery more likely, acknowledgement impossible.\n- **4/5 — `ENQUEUED`/`CONFIRMED` have no retirement rule (B2).**\n- **3/5 — item 1's `releaseDeposit`-ordering silent no-op (B8).**\n- **3/5 — item 6 has no consumer: destructive or dead (B3).**\n- **3/5 — the queue's dispatcher is unnamed and must not be `confined` (B6).**\n- **2/5 — `abandonBlob` needs a `callTimeoutMs` parameter it does not have** (composition F7, storm): `execute` supports it, `uploadBlob` passes `BLOB_UPLOAD_TIMEOUT_MS`, `abandonBlob` passes nothing, the shared client sets `readTimeout(0)` and no `callTimeout`. On a half-open circuit an abandon never resumes. Not an item in the plan.\n- **2/5 — the 409-as-success alternative dominates the reversal on the plan's own metric** (storm, composition, arrived at independently). I verified the safety condition end to end: `encrypt` computes `digest = sha256(plain)` and `size = plain.size` — **plaintext-derived and attempt-independent** — the nonce travels inside the box, the key is memoized, and `AttachmentDepositReuseTest`'s \"either attempt's stored bytes open under the shared key\" already pins it.\n\n---\n\n# WRONG CLAIMS (a lens showed a claim about behaviour false; correct behaviour given)\n\n**W1 — \"M1 is the single change that makes fresh-secrets-per-attempt safe.\"** False. Keeping the token preserves the ability to **retry the DELETE**; it does not delete a row. The cap came from a stable `blobId` meeting `ON CONFLICT DO NOTHING` — no network, no bearer, no limiter, no session. M1's mitigation is void precisely in the correlated-failure case that creates the risk.\n\n**W2 — \"Session ends with entries still queued — TTL. Same as the current tree.\"** False. Per-message vs per-attempt. Same for the crash line.\n\n**W3 — \"No route in v5 destroys a blob the tree would have kept.\"** Not proven; refuted under the plain reading of item 6 + M2 (B3/B4).\n\n**W4 — \"M1 … preserves single-writer's real property — one decider per attempt.\"** Asserted, not held: v4's only exclusion mechanism was the atomic `remove`-and-decide, which M1 explicitly abolishes and does not replace. The consequence is benign (duplicate idempotent DELETEs, extra limiter spend) — so say *\"duplicate abandons are harmless because the DELETE is idempotent\"*, which is true, instead of claiming exclusion, which is not.\n\n**W5 — \"retries are free and reveal nothing.\"** Idempotent and opaque: verified (`DELETE … WHERE blob_id = $1`, `SendStatus(204)` regardless). **Not free:** `h.blobLimit.Allow(...)` runs *before* the delete in `AbandonBlob`, so every retry spends a token from a fixed window that behind the sidecars is global. And 429/401/500/400 are all non-204, so under M1 each means \"retry forever\".\n\n**W6 — \"deleting `reuseToken`/`reuseKey` breaks the reuse tests' *compilation*\" — the plan is RIGHT and arithmetic's correction is wrong here, though its underlying point stands.** Arithmetic says `AttachmentDepositWiringTest` is a source-text scan that \"compiles fine and fails at runtime\". Verified: that class lives **inside `AttachmentDepositReuseTest.kt`**, the same file whose `encrypt(plain, first.token, first.key)` calls break compilation — so both die in one compilation unit and the plan's one-commit conclusion is correct for the reason it gives. **What survives from arithmetic F5:** the reversal deletes all three wiring assertions (including `releaseDeposit(messageId)` count == 3) and v5 mandates no replacement pin, for invariants no behavioural test can reach.\n\n**W7 — \"the duress question disappears … because there is no teardown-time network activity.\"** Conclusion right, reason wrong. Verified: `ZitroneApp.runTerminalBurnLocked` calls `unlockController.lock()` **first**, then hard-fails `if (session.value != null) throw VaultImageException.DestroyFailed.step(\"session-quiesce\")` before the first destructive step. It is an **enforced ordering with a runtime assertion**, not an absence — and a burn from a live session cancels in-flight requests on the wire (`execute`'s `invokeOnCancellation { call.cancel() }`). State it as the ordering property and cite the assertion.\n\n**W8 — \"96-byte entries.\"** Raw key material only; real JVM cost ~250–290 B. Immaterial to the conclusion, but the plan reasons about growth from it.\n\n**W9 — kdoc overclaims: the plan names one, there are two.** Verified: `ApiClient.abandonBlob`'s kdoc claims \"a non-durable ratchet flush, and a contact deleted mid-send\" — the second is false. **`server/internal/db/store.go`'s `AbandonBlob` kdoc makes the same false claim (\"three routes … a contact deleted mid-send\")** and is not named in item 7.\n\n**W10 — dropped from v4 without being fixed** (threat): `markFailed`'s precondition is `SENDING || SENT` — verified — so a relay-ACKed message can be flipped to FAILED and retried, minting a second blob whose `CONFIRMED` sibling M3 forbids ever abandoning.\n\n**W11 — stale drift the plan inherits:** `blobs.go` says \"default 1 week\", `packages/protocol/src/attachments.ts` exports `BLOB_TTL_HOURS = 168`, `AttachmentCrypto`'s kdoc says \"1-week fallback TTL\" — the deployed default is 96 h.\n\n---\n\n# CONFLICTS\n\n**C1 — Is a bounded queue with a drop policy required?** routes says yes (plain cap, oldest-first eviction, because nothing reads an entry after handover). storm/arithmetic/composition say no. **Ruled for the majority:** no size bound, no drop. Checkable arithmetic: ~290 B of heap vs 8,454,180 B of relay disk per dropped entry.\n\n**C2 — Is the re-queue storm a real risk?** The plan and routes treat registry growth as the danger; storm and arithmetic quantify it as irrelevant; threat relocates the cost to traffic analysis. **Ruled:** the cost is **rate** (limiter + observable cadence), never size. Checkable by counting `retry()` call sites (one, user-tap) and any auto-retry of sends (none).\n\n**C3 — item 6: destructive or inert?** routes and threat say ambiguous-and-one-reading-destroys; arithmetic says purely vestigial. **Ruled:** unresolvable *by the plan*, which is the finding. Checkable by naming the consumer — if the answer is \"the live queue\", threat's burn-all sequence lands.\n\n**C4 — arithmetic F3: does item 1, shipped alone on the memoized tree, destroy attempt 1's live blob?** Every other lens calls item 1 strictly safe. **Adjudicated against source, and arithmetic is mostly refuted:** I enumerated every `markFailed` call site — 501 (`sendMessage` false, no handoff), 1239/1254 (text paths), 1461 (route a, pre-handoff), 1479 (route c), 1504/1511 (retry preconditions). The **only** way a handed-off attachment becomes retryable is route (c), which *already* fires `abandonBlobQuietly` on the same memoized token. So item 1's abandon is a second DELETE of a row the tree already decided to destroy — **not a new capability**. It is reachable **only when route (c)'s fire-and-forget abandon failed** (429/offline), in which case item 1 gives that destruction a second chance. P3, pre-existing in kind. Ship item 1; record the exposure; the right fix is the `published`/`ENQUEUED` bit in v6, not v1's guard bolted onto one branch.\n\n**C5 — which alternative dominates?** storm/composition: treat 409 on a memoized-token deposit as success. threat: keep the memo, cancel the queued abandon on `confined` when a retry starts. **Both are right and they compose** — see v6 below. Checkable and I verified both premises: the 409-safety condition (plaintext-derived verifiers, key memoized, nonce inside the box) and the serialisation (`sendAttachment` and `retry` both `scope.launch(confined)`, while `abandonBlobQuietly` launches on bare `scope` — which is exactly why the stale-abandon race exists today).\n\n---\n\n# VERDICT\n\n**DO NOT SHIP.**\n\n---\n\n# IF REJECTED — WHAT v6 MUST BE BUILT AROUND\n\nNot a patch. The reframing: **stop building a new ownership structure for a capability the tree already owns correctly.** `attachmentDeposits` is already a per-message registry with three retirement points, released on every terminal outcome, pinned by a test. v1–v5 each proposed to replace it with a state machine, a flush, a queue, or a registry — and each died on a route the replacement could not see. **v6 adds two bits to the existing memo and changes one branch. No registry, no queue, no state machine, no teardown, no storm.**\n\n**Mechanism 1 — IDEMPOTENT REDEPOSIT: treat a 409 on a memoized-token deposit as SUCCESS.**\nOne branch in `deliverAttachment`'s `upload-blob` stage. The safety condition is verified end to end: our own 256-bit token ⇒ our own row; `digest`/`size` are **plaintext**-derived and attempt-independent; the nonce travels inside the box; the key is memoized. This single change fixes the live UX defect nobody has named (**today the first retry tap of every attachment is guaranteed to fail on a 409**), keeps the server-enforced one-row-per-message cap, and deletes the entire motivation for the reversal, M1, M2, the drain, the backoff, the governor and the storm.\n\n**Mechanism 2 — A `published` BIT, SET NON-SUSPENDING, READ AT EVERY ABANDON SITE.**\nThis is M3 made structural instead of documentary. Set it inside `publishOutgoing` at `ws.sendMessage(envelope) == true` (the compiler already enforces non-suspension there — the one invariant v5 had for free). No abandon may fire for a message whose bit is set. This closes the tree's two real live-blob-destruction routes — the `coverTraffic.cover` throw after handoff, and item 1's second-chance destruction (C4) — and it is the only enforcement mechanism in the whole design that is not a comment.\n\n**Mechanism 3 — CANCEL-ON-RETRY, NOT RACE-ON-RETRY.**\nMake the queued abandon a per-`messageId` `Job` that a new attempt cancels, and move it onto `confined` where both attempt entry points already run. That dissolves the stale-abandon race the reversal was chosen to fix, without fresh secrets.\n\n**Mechanism 4 — BOUNDED RETRY OF THE ABANDON, ATTACHED TO THE MEMO.**\nM1's one genuine win (a swallowed abandon failure is currently lost forever) survives, hung on the structure that already exists and already has three retirement points. Bounded attempts, jittered, gated on `CONNECTED`, queue-wide 429 pause, off the send-critical path, with a `callTimeoutMs` added to `ApiClient.abandonBlob`. Retirement rules: 204-with-observed-deposit, `CONFIRMED`, or deposit + `BLOB_TTL_HOURS`.\n\n**Also required in v6, and not negotiable:** the mark-hook consumer must be named or the hooks must not exist; a replacement source pin for whatever the change deletes, since **nothing in the suite can construct `MessagingCoordinator`** and this is the third round in a row where an invariant would ship enforced only by prose.\n\n## Shippable TODAY, independent of all of the above\n\n1. **Item 1** — abandon in `publishOutgoing`'s contact-deleted branch. **Read `attachmentDeposits[messageId]` BEFORE the existing `releaseDeposit(messageId)` call** (B8), or it is a silent no-op. Strict gain: that route is a guaranteed orphan today.\n2. **Item 2** — a separate `AbandonBlob` limiter. Server-side, sized for the new volume, and **deployed to CX23 and every peer**, not merely merged. Document honestly that it still collapses to one global bucket per overlay sidecar.\n3. **Item 7 kdoc corrections — both files**: `ApiClient.abandonBlob` *and* `server/internal/db/store.go`'s `AbandonBlob`, which carries the identical false three-route claim (W9). Optionally the 96 h/1-week drift in `blobs.go`, `AttachmentCrypto` and `packages/protocol/src/attachments.ts` (W11).\n\nEverything else waits for v6."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design review"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesis"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "v5:storm",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "ae62c0c73444fd7b7",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785420378455,
      "queuedAt": 1785420372268,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/root/zitrone/apps/android/app/src/main/java/com/zitrone/ap…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v5.md in full, then READ THE SOURCE and check its claims.\n\nCITATIONS: v5 contains NO line numbers, deliberately — three earlier plans cited offsets that were\nwrong (v3's uniformly 8 lines low; two of its deletion ranges would have de…",
      "lastProgressAt": 1785421059725,
      "tokens": 128333,
      "toolCalls": 41,
      "durationMs": 681270,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — step 3 (delete `reuseToken`/`reuseKey`) vs `Store.StoreBlob`'s `ON CONFLICT (blob_id) DO NOTHING`**\nSequence, no network failure required: user sends a photo; `api.uploadBlob` returns 201; `ws.sendMessage` returns false (socket reconnecting) → `publishOutgoing` marks FAILED; the owner decides ABANDON and hands the entry to M2. The user taps retry twice before the abandon re…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "v5:composition",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a27118b43efaf374b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785420378231,
      "queuedAt": 1785420372269,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v5.md in full, then READ THE SOURCE and check its claims.\n\nCITATIONS: v5 contains NO line numbers, deliberately — three earlier plans cited offsets that were\nwrong (v3's uniformly 8 lines low; two of its deletion ranges would have de…",
      "lastProgressAt": 1785420989094,
      "tokens": 110752,
      "toolCalls": 40,
      "durationMs": 610863,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — M1 (retire only on 204) × M2 (session-scoped queue) × the reversal in item 3 (deleting `reuseToken`/`reuseKey`)**\n\nSequence: user sends an 8 MiB photo; `publishOutgoing` hits the `ws.sendMessage`-false branch (socket down, HTTP up) or `flushSendRatchet` returns false. User taps retry 4 more times over a few minutes. `POST /api/v1/blobs` succeeds each time (fresh `blobId` pe…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "v5:routes",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "aa6e505a456575aa4",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785420994526,
      "queuedAt": 1785420372269,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/root/zitrone/apps/android/app/src/main/java/com/zitrone/ap…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v5.md in full, then READ THE SOURCE and check its claims.\n\nCITATIONS: v5 contains NO line numbers, deliberately — three earlier plans cited offsets that were\nwrong (v3's uniformly 8 lines low; two of its deletion ranges would have de…",
      "lastProgressAt": 1785421779111,
      "tokens": 154347,
      "toolCalls": 50,
      "durationMs": 784584,
      "resultPreview": "I have read the plan and verified its symbols against `feat/0.10.2-capacity-fixes` @ `534d4dfa`.\n\n# FINDINGS\n\n**F1 — P1 — the reversal itself (step 3: delete `reuseToken`/`reuseKey`) + M2's session-scoped queue.**\nSequence (no exotic timing): vault unlocked, 8 MiB photo, flaky link. Attempt 1: `api.uploadBlob` returns 201 (entry `STORED`, token T1) → `flushSendRatchet` true → `publishOutgoing` → `…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "v5:arithmetic",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a6565a048429d014f",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785421064358,
      "queuedAt": 1785420372269,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"encrypt(\" /root/zitrone/apps/android/app/src/test/…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v5.md in full, then READ THE SOURCE and check its claims.\n\nCITATIONS: v5 contains NO line numbers, deliberately — three earlier plans cited offsets that were\nwrong (v3's uniformly 8 lines low; two of its deletion ranges would have de…",
      "lastProgressAt": 1785421795011,
      "tokens": 135507,
      "toolCalls": 34,
      "durationMs": 730652,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — headline claim / M1 + the reversal (work item 3).** *Sequence:* user sends an 8 MiB photo. Attempt 1: `api.uploadBlob` succeeds (row 1 on the relay), then `publishOutgoing` hits `ws.sendMessage == false` (or `flushSendRatchet` returns false) → `messages.markFailed`. v5 queues the abandon; the overlay is congested and it 429s / times out, so the entry is re-queued with backo…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "v5:threat",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a39dc66d352b6bd5b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785421786559,
      "queuedAt": 1785420372269,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"publishOutgoing\\|abandon\\|non-suspend\" apps/androi…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v5.md in full, then READ THE SOURCE and check its claims.\n\nCITATIONS: v5 contains NO line numbers, deliberately — three earlier plans cited offsets that were\nwrong (v3's uniformly 8 lines low; two of its deletion ranges would have de…",
      "lastProgressAt": 1785422479744,
      "tokens": 139571,
      "toolCalls": 41,
      "durationMs": 693185,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — Step 3 (the reversal: deleting `AttachmentCrypto.encrypt`'s `reuseToken`/`reuseKey`) + M1.**\nv5 trades a **DB-enforced, network-independent** cap for a **best-effort network** one, and then claims the result is \"never less\".\n\n*Sequence (all symbols at HEAD):* Alice sends an 8 MiB attachment. `deliverAttachment` deposits (`api.uploadBlob`), then `flushSendRatchet` returns fa…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesis",
      "phaseIndex": 2,
      "phaseTitle": "Synthesis",
      "agentId": "ae5ac42582eea2485",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785422488447,
      "queuedAt": 1785422481849,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone; sed -n '305,325p' server/internal/db/stor…",
      "promptPreview": "READ-ONLY. Five lenses reviewed DESIGN PLAN v5 at l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v5.md. Its four predecessors were all rejected\nDO NOT SHIP. Reports follow verbatim. SYNTHESIS, not re-review. ADJUDICATE AGAINST SOURCE.\n\n# BLOCKING\nRanked most severe first. Which lens(es), AGREE or CONFLICT, exactly what must change.\n\n# THE STORM RULING\nv5's open tension: M1 retires …",
      "lastProgressAt": 1785422825634,
      "tokens": 94730,
      "toolCalls": 23,
      "durationMs": 337186,
      "resultPreview": "# SYNTHESIS — PLAN v5, five lenses, adjudicated against source at `feat/0.10.2-capacity-fixes`\n\nVerdicts in: **DO NOT SHIP ×3** (routes, arithmetic, threat), **SHIP WITH CHANGES ×2** (storm, composition) — and both of those two explicitly refuse work item 3, which is the plan. There is no lens that endorses v5's core. Every source claim below I re-derived myself; where a lens was wrong I say so.\n\n…"
    }
  ],
  "totalTokens": 763240,
  "totalToolCalls": 229
}