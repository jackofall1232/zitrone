{
  "summary": "Five-agent DESIGN review of plan v3 (per-token registry) — v1 and v2 were both rejected",
  "agentCount": 6,
  "logs": [],
  "result": {
    "lenses": [
      "window",
      "composition",
      "routes",
      "arithmetic",
      "threat"
    ],
    "synthesis": "# BLOCKING\n\nRanked most severe first. Line numbers below are HEAD (`31770972`) of `feat/0.10.2-capacity-fixes`; every one was read at source.\n\n**1 — `HANDED_OFF` is defined by a fact the client cannot observe, and rule 3 turns that into a permanent orphan. (window F1, routes F3, arithmetic F2, composition claim-check — 4 lenses, AGREE; threat did not contest.) CONFIRMED.**\n`WsClient.sendMessage` (`:176-177`) → `send` (`:222-223`) → `webSocket?.send(...)`, an OkHttp **enqueue** receipt. The tree says so in its own words in three places: `WsClient.kt:210-216` (\"Bytes handed to the socket and **not yet written**\"), `MessagingCoordinator.kt:493-495`, and `:2296-2300` — \"the FIRST honest proof the message reached the other device, so it — **not ws-enqueue** — advances the tick\". Relay ownership is `message.stored` (`blobs`/`hub` → `MessagingCoordinator.kt:2289`). Consequence: on the ordinary mobile socket drop the frame is discarded unwritten, and **no `markFailed` exists on any disconnect path** — all seven call sites (`:501,1239,1254,1461,1479,1504,1511`) are on send paths — so the bubble sits at SENDING, `retryable`'s CAS (`MessageRepository.kt:142-147`) refuses a retry, and rule 3 forbids ever abandoning the blob. Up to 8 MiB unreclaimable for the full TTL, on the most frequent failure in the product, and it is absent from the list the plan calls \"exhaustive\".\n*Must change:* delete \"the relay owns it now\". The state may only assert what `send()==true` proves: *a frame naming this token entered a buffer we cannot recall*. \"Never abandon\" must be **conditional on relay confirmation**, not permanent — which means step 5's blanket rule has to be narrowed (see BLOCKING 4).\n\n**2 — Step 3 is inert on the teardown it exists to fix: the abandon cannot authenticate. (composition F2, routes F2 — independent, AGREE.) CONFIRMED, deterministic, not a race.**\n`UnlockController.lockCurrent()` runs `stopSession(session)` at `:126` **before** `sessionScope?.cancel()` at `:135`. `stopSession` is `ZitroneApp.kt:964-971`: `coordinator.stop()` then **`runtime.close()` in a `finally`**, and `close()` sets `closed = true` (`VaultRuntime.kt:197-203`). `ApiClient.request` reads the bearer **live on every call** (`:434-444`) → `VaultAuthStore.accessToken` (`AuthStore.kt:144-145`) → `VaultRuntime.read` → `check(!closed)` (`:105-106`) → `IllegalStateException`, swallowed by `abandonBlobQuietly`'s `runCatching` (`:438-440`). So at every lock — the plan's own headline case — a `NonCancellable` process-scope abandon dies before a byte hits the network. **The binding constraint is vault closure, not scope cancellation**, and step 3 addresses the wrong one.\n*Must change:* the reclaim must happen **inside** the teardown order while the vault is open — a bounded terminal flush in `coordinator.stop()`, which already has the exact pattern for it (`coverWorker.runTerminalConfined(::coverTeardown)`, `:869`, dispatch-and-block on the confined worker) — or with the credential captured at deposit. Not by escaping the scope.\n\n**3 — `RESERVED` unreclaimable makes v3 *worse than the current tree* on the metric item 5 exists to bound. (composition F1, routes F1, arithmetic F1 — three independent, AGREE.) CONFIRMED.**\nToday the memo is written at `:1346`, **before** the upload at `:1428`, and route (c) at `:1483` abandons unconditionally — so a response lost after the relay committed the row *is* reclaimed today. Under rule 3 it is not. `AbandonBlob` is an unconditional `DELETE` answering 204 either way (`blobs.go:151-169`, `store.go`), so abandoning an unknown-state token costs nothing and reveals nothing. Worse: step 1 deletes the token memo, so each retry is a **new** `blobId` — today k retries leave one blob (`ON CONFLICT (blob_id) DO NOTHING`), under v3 they leave k. `BLOB_UPLOAD_TIMEOUT_MS` (`612b6bd2`) makes this route *more* frequent, not less.\n*Must change:* `RESERVED` means \"we do not know\", and must be abandoned. The plan's own dissolution property is the proof it is safe.\n\n**4 — Step 4's messageId-keyed sweep falsifies the dissolution property the design is built on, and hands the abandon trigger to a relay frame. (window F4, arithmetic F3, threat B3 — three independent, AGREE on the defect, differ on framing.) CONFIRMED.**\n\"A stale cleanup from attempt N can only ever name attempt N's own token\" is false for half the design by construction: step 4 is specified as \"remove **every** registry entry whose `messageId` matches\". `message.burned` is a plain unauthenticated, unratcheted WS frame (`WsClient.kt:316-317` → `MessagingCoordinator.kt:2274-2275` → `MessageRepository.onRemoteBurn:306-308`), and `ConcurrentHashMap` iteration is weakly consistent. What actually protects attempt N+1 is step 6's guard plus the `HANDED_OFF` exclusion — **guards, not dissolution** — which is exactly the reasoning error v1 was rejected for.\n*Must change:* the sweep must **remove/mark only, never abandon**. Its abandon is also redundant: `DEPOSITED` means the upload returned, so the owning coroutine is alive and must reach an exit. (Severity note: against a *conceded* relay the incremental destructive power is small — it can 404 any blob already. The point is that item 5 exists to stop **self-inflicted** deletion, and step 4 reintroduces it.)\n\n**5 — `abandonBlob` has no call bound, so under `NonCancellable` a wedged cleanup never returns and step 7's single-flight queue stalls for the process lifetime. (threat B1, alone.) CONFIRMED.**\n`abandonBlob` is `execute(post(...))` with **no `callTimeoutMs`** (`ApiClient.kt:292-295`); both clients set `readTimeout(0)` (`CertificatePinning.kt:74,130`); `execute`'s only reaper for a half-open circuit is `continuation.invokeOnCancellation { call.cancel() }` (`:479`) — the mechanism step 3 deliberately removes. `ApiClient.kt:453-459` documents this exact failure mode for the deposit and does not apply it to abandon. One worker + one unresumed continuation = every later orphan in the process is never reclaimed.\n*Must change:* a per-call deadline on abandon, and bounded concurrency rather than a single worker (threat B1 + arithmetic F10 agree on the queue shape).\n\n**6 — Step 6's justification is false in three ways. (all five lenses: window F3, composition F5, routes F5, arithmetic F5, threat B8 — AGREE.) CONFIRMED.**\n`MessageRepository.exists` (`:363`) → private `find` (`:365-366`) = `_messages.value.values.asSequence().flatten().firstOrNull { it.id == messageId }`: a flatten-and-scan over every message in RAM. Not O(1). `exists` returns `Boolean`, so `present && state != BURNING` **cannot be expressed through it**, and widening it silently changes its one live caller — the incoming duplicate staleness gate at `MessagingCoordinator.kt:589`. And the cited rule is the wrong rule: **R-U3-1 is the cover-traffic subordination requirement** (\"cover traffic must never compete with a real send for any resource\", `CoverPressure.kt:16-21`, `DecoySendPairing.kt:219-235`), not a prohibition on send-path work. The rule that governs the tail is *no suspension between the check and the send*, compiler-enforced by the non-suspending `fun` (`MessagingCoordinator.kt:456-470`).\n*Must change:* a new bounded accessor (index by id), and the constraint restated as the no-suspension rule it actually is.\n\n**7 — Every `MessagingCoordinator.kt` citation is exactly 8 lines low, and two step-8 deletion ranges destroy live code. (threat named the cause; window F5, composition F6, routes, arithmetic independently found the same wrong lines — AGREE.) CONFIRMED by diff.**\nI checked `c781d2ac`: at that revision `:484` is `if (ws.sendMessage(envelope)) {`, `:1454-1457` is the ROUTE (a) comment, `:1330-1334` is the \"ONE BLOB PER MESSAGE\" comment, `:1727` is `burnAll(..., notifyPeer = false)`. **The plan's line numbers are correct for `c781d2ac` — two commits before the baseline it states (`612b6bd2`)** — and `dc145ad5` shifted everything by +8. Executed literally against HEAD, step 8 deletes `if (!acceptingSends) return` (R-U3-5's send-admission gate, `:1334`), the `flush = flushBeforeAck / onNotDurable` argument list (`:1454-1457`), and — worst — `@OptIn(...)` + `private val confined = Dispatchers.IO.limitedParallelism(1)` (`:386-387`), the single-worker dispatcher the entire D2c argument rests on. `AttachmentCrypto.kt:74-99` is likewise wrong as a range: the reuse text is `:76-86`; `:88-92` (fresh-nonce / catastrophic GCM reuse) and `:94-97` (token must never be derived from anything the relay sees) survive the parameter deletion and must not go.\n*Must change:* re-cite every range against HEAD, and express deletions as anchored text, not line ranges. Third consecutive version with this defect.\n\n**8 — Step 1 deletes the only behavioural pin on the design's premise, and the only tripwire on its load-bearing invariant. (threat B5+B6, alone — and it contradicts window/composition's \"step 1 is sound\".) CONFIRMED.**\n`AttachmentDepositReuseTest.kt:28-36` (`a fresh encrypt draws new secrets every time`) and `:91-105` (`the token is never derivable from anything the relay sees`) use only the **1-arg** `encrypt(plain)` form — they compile unchanged after the parameters are deleted, and they are the only behavioural assertions that the token is a fresh, non-derived draw, which **is** the dissolution premise. Deleting the class makes freshness comment-only. Separately, `AttachmentDepositWiringTest:149-159` asserts the **count** of release points precisely so \"a release point was lost\" fails the build; step 1 deletes it and explicitly declines a replacement, leaving \"all five exits remove their entry\" with zero automated signal — and nothing in the suite can construct a `MessagingCoordinator` (`:111-114`).\n*Must change:* keep those two tests; replace the counting tripwire with one that counts the new exits.\n\n**9 — Nothing ever removes a `HANDED_OFF` entry, and the map key is the redemption capability. (window F6, composition F9ii, routes F6, arithmetic F9 — four independent, AGREE.) CONFIRMED.**\nToday's only happy-path release is `releaseDeposit` from `onMessageStored` (`:2293`) / `onMessageDelivered` (`:2305`); step 5 forbids exactly that. Step 4's hooks fire only on burn/remove/discard, so a successfully sent attachment with no TTL and no burn keeps its entry for the whole session — the growth the existing kdoc at `:412-417` says the release exists to prevent — and the retained key **is** the 32-byte redemption token, held as an unwipeable `String`.\n\n**10 — A sixth exit, of exactly the shape that killed v1. (routes F4, composition F9i — AGREE.) CONFIRMED.**\n`} ?: return` at `MessagingCoordinator.kt:1397` (contact deleted during prekey fetch, or pinned-identity mismatch) is a **plain return inside `runCatching`** — `onFailure` never fires — and it sits after the token is drawn (`:1344`) and before the upload (`:1428`). \"All five exits\" is a count of `publishOutgoing` exits (of which there are three: `:485-491`, `:492-497`, `:498-502`); routes (a), (c), (e) are exits of `deliverAttachment`. That mis-framing is what hides this one. Related: `coverTraffic.cover(envelope)` at `:1475` is **inside** the same `runCatching`, so a throw there reaches route (c) *after* a genuine handoff → `markFailed` on a handed-off message plus an abandon of a live blob.\n\n**11 — \"all TTL-bounded at 96 h\" is false for the relay as deployed. (arithmetic F7, alone.) CONFIRMED.**\n`git show main:server/internal/config/config.go` → `BlobTTLHours: envInt(\"BLOB_TTL_HOURS\", 168)`. The 96 h default is branch-only. The effective value is also `max(BLOB_TTL_HOURS, MESSAGE_TTL_UNDELIVERED_HOURS + 24)` (`config.go:172-173`), i.e. deployment-raisable, plus up to one janitor period. `config.go:87` and `blobs.go:135` still say \"default 1 week\" and are not on step 8's list.\n\n**12 — Step 3's duress asymmetry is backwards. (composition F3, threat B2, arithmetic F8 — three independent, AGREE.)**\nA `NonCancellable` process-scope writer is uncancellable **by construction**, so it defeats the quiesce whose stated purpose is that \"a surviving session here means the quiesce did not happen\" (`ZitroneApp.kt:587-592`), and the byte-for-byte terminal-burn gate checks `current == null`, so it stays GREEN while the invariant breaks. Meanwhile the burst step 3 fears cannot arise where it claims: `PassphraseOutcome.Burn` is reachable only at the lock screen with no live session and therefore no registry. The gate suppresses an unreachable case and creates the reachable one.\n\n**13 — Step 4's `clearAll` iteration is dead on every path it could fire on. (composition F4, routes F7 — AGREE.) CONFIRMED.**\n`messages.clearAll()` has exactly two call sites: `:1943` (account-delete, on the confined worker) and `:2391` (`onSessionRevoked`, after `api.clearTokens()` at `:2380`). Both are inside step 3's **disabled** set. A plain lock never reaches `clearAll` at all. The only non-revoke logout in the tree is `onForcedLogout`, invoked from `onSessionRevoked` — so step 3's \"enabled for lock / logout\" reduces to lock alone.\n\n---\n\n# THE NAMED WINDOW — RULING\n\n**Framing 4 is correct: the third state is MISPLACED, not missing. But it is misplaced in its *definition* and in *who is allowed to act on it* — not in its position. Do not move it, do not add a lifecycle state, do not fuse it with the send.**\n\nThe reasoning, against source:\n\n1. **The state exists to answer exactly one question:** *may cleanup destroy this blob?* The answer must become NO the moment a frame naming the token could reach a recipient, because destruction after that is unrepairable — redemption is one-shot, `attachmentUnavailable` is terminal (`MessageRepository.kt:196-215`), and a `markSent` message cannot be retried.\n2. **That moment is precisely `send() == true`.** Before the call, impossible. If `send()` returns `false` — no socket, or OkHttp refusing a closing/failed socket or an over-cap buffer (`WsClient.kt:222-223`) — the frame was **never enqueued**, so it is still impossible. The plan's chosen position (immediately after the send returns true) is therefore the *only* correct position for this predicate. **Candidate (i) is wrong**: moving it earlier requires a revert-on-false path to avoid leaking the highest-frequency route (socket-down), and once you add the revert you are back to two computes and have bought nothing. (Note: the plan's *reason* for hesitating about (i) is also wrong — a non-suspending `compute` before the send violates nothing. R-U3-1 is a cover-traffic rule; the tail's rule is no-suspension, compiler-enforced.)\n3. **Candidate (iii) is expressible but bad.** `publishOutgoing`, `publishReceipt` and `ws.sendMessage` are all non-suspending, so `registry.compute(token) { ... ws.sendMessage(...) ... }` compiles and keeps the compile-enforced contract at `:463-470`. It holds a `ConcurrentHashMap` bin lock across `JSONObject.toString()` and `RealWebSocket.send` — which `compute`'s own contract forbids — and it closes only the *post*-send half of the window.\n4. **Candidate (ii) is the wrong answer to the right instinct.** A fourth lifecycle state promoted on `message.stored` is unresolvable *as the plan is written*, because step 5 forbids relay frames from touching entries — routes is right that v3 cannot express its own correctness condition. But the missing thing is not a state, it is a **narrowing of step 5**: relay frames must be barred from causing an **abandon**, not from resolving an entry. `message.stored` arriving is proof the relay has it, so letting it release the entry is safe and also fixes BLOCKING 9.\n5. **What actually dissolves the window is single-writer abandon.** Make the depositing coroutine the only party in the process that ever calls `abandonBlob`; every foreign-stack cleanup **marks** and returns no-abandon, and the owner resolves the mark at its own exit (`marked + handoff-true` → remove, no abandon; `marked + send-false` → remove and abandon). Then nothing can abandon inside the window, because nothing but the owner can abandon at all. This is threat B7's construction stripped to its load-bearing property: **it is one extra bit on the entry, not a fourth lifecycle state**, and it simultaneously kills BLOCKING 4 (relay-forged burn cannot cause a destroy) and arithmetic F3.\n6. **On the harm the plan feared, it overestimated:** every cleanup trigger reachable in that window is a *destroy instruction* for that very message — main-thread `burnAll` (`MainActivity.kt:1677`), relay-routed `onRemoteBurn`, read-burn/reveal-burn, or `discard` — and sender-side TTL **cannot** fire there at all, because `scheduleTtl` is armed only from `markDelivered` (`MessageRepository.kt:104-114`). Composition is right that abandoning in that window is aligned with intent, so the window itself is P3. The P1s are the state's definition (1), the issuer (2), `RESERVED` (3) and the sweep (4).\n\n**Ruling in one line: keep the transition where it is, rename it to what `send()==true` actually proves, make the depositing coroutine the sole party that may abandon, and let relay confirmation resolve the entry.**\n\n---\n\n# CONVERGENCE\n\nIndependent agreement, strongest signal first:\n\n- **`ws.sendMessage` is enqueue, not receipt — 4 lenses (window F1, routes F3, arithmetic F2, composition claim-check).** Each traced `:176-177 → :222-223` on its own. The tree agrees with them in its own comments (`WsClient.kt:210-216`; `MessagingCoordinator.kt:2296-2300`).\n- **`RESERVED`-no-abandon regresses the metric versus today — 3 lenses (composition F1, routes F1, arithmetic F1),** all three reaching it via the same \"response lost after the row committed\" sequence and all three noting today's `:1483` reclaims it.\n- **Step 6's `exists` is not O(1) and cannot express the predicate — 5 of 5 lenses.** Unanimous, and trivially checkable at `MessageRepository.kt:363-366`.\n- **Step 3 cannot authenticate after `runtime.close()` — 2 lenses (composition F2, routes F2),** independently walking `UnlockController.kt:126` → `ZitroneApp.kt:964-971` → `VaultRuntime.kt:105-106`. Both call it deterministic. This is the finding that says step 3 needs redesign, not adjustment.\n- **Step 4's messageId sweep breaks the dissolution property — 3 lenses (window F4, arithmetic F3, threat B3)** by three different routes (structural claim, local race, relay-forged frame).\n- **Nothing removes a `HANDED_OFF` entry — 4 lenses (window F6, composition F9ii, routes F6, arithmetic F9).**\n- **The `MessagingCoordinator` citations are systematically wrong — 5 of 5 lenses,** independently, and threat alone diagnosed *why* (they describe `c781d2ac`, +8 lines behind the stated baseline). I verified the diagnosis by reading both revisions.\n- **Step 3's duress gate is backwards — 3 lenses (composition F3, threat B2, arithmetic F8),** each noting that `NonCancellable` is exactly what the quiesce forbids.\n- **Step 4's `clearAll` hook is dead code — 2 lenses (composition F4, routes F7).**\n- **The sixth exit at `:1397` — 2 lenses (routes F4, composition F9i).**\n- **Step 9's open question is answered the same way by all five:** `ApiClient.kt:448-451` uses `body.toString().toRequestBody(...)`, a byte-backed body, `isOneShot()==false`, so OkHttp *will* replay; and `store.go` `ON CONFLICT DO NOTHING` → `ErrBlobExists` → `blobs.go:116-118` **409**. Step 9's conclusion (\"409 ⇒ our bytes are stored ⇒ `DEPOSITED`\") is confirmed, not assumed — it is the only step no lens faulted on substance. (Caveat all five miss the same way: the `callTimeout` path throws *without* replay, which is BLOCKING 3's route.)\n\n---\n\n# WRONG CLAIMS\n\nv3 assertions shown false, with the correct fact (all verified at HEAD):\n\n| v3 says | Correct |\n|---|---|\n| \"route (d), the socket-down exit at `MessagingCoordinator.kt:484-494`\" | Socket-down exit is **`:498-502`** (`markFailed` `:501`). `:485-491` is the *contact-deleted* exit, `:492-497` the success branch. Both return `false` without throwing. |\n| \"route (c) currently rethrows at `:1469`\" | Rethrow is **`:1477`**; `:1469` is a comment. |\n| \"`:1454-1457` (states the exact inversion)\" | `:1454-1457` is executable code (`flush = flushBeforeAck, onNotDurable = {...}`). The inversion is **`:1462-1465`**. |\n| \"the inline 'ONE BLOB PER MESSAGE' (`:1330-1334`)\" | **`:1338-1342`**. `:1330-1334` is the parameter tail plus **`if (!acceptingSends) return`**. |\n| \"the `AttachmentDeposit` kdoc (`:380-398`)\" | Kdoc is **`:389-407`**; class at `:408`. `:386-387` is `@OptIn` + **`private val confined`**. |\n| \"the vault contact delete (`:1727`)\" | **`:1735`**. |\n| \"`AttachmentCrypto.kt:74-99`'s reuse rationale\" | Reuse-specific text is **`:76-86`**; `:88-92` (GCM nonce) and `:94-97` (token non-derivation) must survive. |\n| \"idle auto-lock (`ZitroneApp.kt:983-996`)\" | **`:982-994`**. |\n| \"`messages.exists(messageId)` … `present && state != BURNING`, O(1), synchronous RAM read\" | `:363` → private `find` `:365-366`, linear scan over all messages; returns `Boolean` only. |\n| \"R-U3-1 forbids work between the durability barrier and `ws.sendMessage`\" | R-U3-1 is the **cover-traffic** subordination requirement (`CoverPressure.kt:16-21`). The tail's rule is **no suspension** (`MessagingCoordinator.kt:456-470`). |\n| \"`HANDED_OFF` — the relay owns it now\" | `send` returns true on **enqueue** (`WsClient.kt:222-223`, and `:210-216` says so). |\n| \"`RESERVED` means nothing landed\" | It means the client does not know; the row may be committed. |\n| \"a stale cleanup … is structurally incapable of touching attempt N+1's blob\" | False for step 4, which is messageId-keyed by construction. |\n| \"enabled for lock / logout\" | No non-revoke logout exists; `onForcedLogout` is called from `onSessionRevoked` (`:2394`), inside the disabled set. |\n| \"all TTL-bounded at 96 h\" | `main` still defaults to **168**; effective value is `max(96, MESSAGE_TTL_UNDELIVERED_HOURS+24)`, deployment-raisable. |\n| \"Fresh-secrets-per-attempt becomes compiler-enforced by the parameters not existing\" | The compiler enforces only that *callers cannot pass* reuse secrets. Freshness lives inside `encrypt` and is pinned by the two tests step 1 deletes. |\n| \"Delete the 2^-256 argument\" | No such argument exists in source (`AttachmentCrypto.kt`, `ApiClient.kt`, `blobs.go`, `store.go`) — it is v2 plan text. Step 9 keeps the *conclusion* that depended on it. |\n| \"the two stale confinement comments are corrected (`dc145ad5`)\" | True, and it is the commit that invalidated every line number in this plan. |\n\nAlso missing from step 8's sweep, same defect class: `config.go:87`, `blobs.go:135` (\"default 1 week\"), `AttachmentCrypto.kt:24`, and `blobs.go:130-136` — `RedeemBlob`'s doc block has no blank line before `:137`, so it reads as `AbandonBlob`'s and `RedeemBlob` (`:171`) is undocumented.\n\n---\n\n# CONFLICTS\n\n1. **Severity of the named window.** window F2 = P2 data loss; arithmetic F3 = P1; threat B7 = P2 needing a fourth state; composition = P3, abandoning is the *desired* outcome. **Checkable:** are the triggers reachable in that window destroy-instructions? **Source says yes** — main-thread `burnAll` (`MainActivity.kt:1677`), `onRemoteBurn`, read-burn/reveal-burn, `discard`; and sender-side TTL cannot fire because `scheduleTtl` is armed only from `markDelivered` (`:104-114`). **Composition wins on harm.** arithmetic/threat are right about a different thing — that a *relay-forged* burn makes the abandon relay-triggerable (BLOCKING 4).\n2. **Is a `compute` before `ws.sendMessage` legal?** composition F8 says no, out of bounds under R-U3-1. **Checkable and FALSE:** R-U3-1 governs cover traffic (`CoverPressure.kt:18-21`, `DecoySendPairing.kt:219-235`); the tail's constraint is no-suspension, satisfied by a non-suspending `compute` (`MessagingCoordinator.kt:463-470`). window's reading is correct. This matters because it is composition's stated reason for rejecting candidate (i) — the rejection stands, for the reasons in the ruling, but not for that reason.\n3. **Which comment \"states the exact inversion\".** Four lenses say `:1462-1465`; arithmetic says `:1448-1452`. **Checkable:** the plan's `:1454-1457` + 8 = `:1462-1465` = \"Abandoning cannot strand a later attempt, because item 5a memoises the token\". The four are right about intent. arithmetic found a **second, genuinely backwards** comment at `:1448-1452` (\"the flush is the last suspension before the atomic deposit\" — the deposit is at `:1428`, *before* the flush). Not a conflict; an extra find to add to step 8.\n4. **threat B4: abandon as a deanonymisation channel.** **Checkable and WRONG as an incremental claim.** `cmd/server/main.go:116` — `v1.Post(\"/blobs\", handlers.RequireAuth, handlers.DepositBlob)`, and the deposit body carries `blob_id`. The relay already holds (account, blob_id) from the deposit; a later unauthenticated redemption of that id is already correlatable. Abandon adds a **timing** signal (teardown-correlated traffic — real, and the substance of BLOCKING 12), not a new linkage. B4's *other* half is correct and is a point in v3's favour: `:1483` today can abandon a blob whose envelope the relay accepted, via a throw from `coverTraffic.cover` at `:1475`, and the `HANDED_OFF` exclusion closes it.\n5. **threat B8(ii): \"`exists` has zero callers in main or test, and its kdoc is stale.\"** **WRONG.** `MessagingCoordinator.kt:589` is the caller, and the kdoc at `:359-361` describes it exactly (\"the coordinator's owed post-ack settling\"). composition F5 got this right and drew the correct consequence: widening `exists` would silently change that gate.\n6. **Step 7's rate-limit sizing.** arithmetic F6 says the per-client worry is misdirected (a retry is a UI tap plus an 11 MB re-upload, so ≤ ~6 abandons/min) and the real exposure is the **collapsed** bucket (`clientKey` behind the Tor/I2P sidecars and `TRUSTED_PROXY_IPS`); the other lenses take the plan's framing. arithmetic is checkable and right: one `ratelimit.New(60, time.Minute, ...)` (`handlers.go:83`) serves `blobs.go:93,152,172`. Step 7's headroom number must be a population-burst figure, and the plan gives none.\n7. **Step 1's completeness.** window and composition both cleared step 1 (\"deletion set complete\", \"does not repeat v1's test breakage\") — both true — while threat B5 showed two tests in the deleted class compile unchanged and are the design's only behavioural pin. Not a factual conflict: a scope miss by two lenses, and threat's finding stands (BLOCKING 8).\n\n---\n\n# VERDICT\n\n**DO NOT SHIP** — the mechanism's defining transition rests on an enqueue receipt mistaken for relay acceptance, `RESERVED`-no-abandon makes v3 measurably worse than the tree it replaces, step 3's issuer cannot authenticate on the one teardown it exists to fix, and step 4 is a messageId-keyed abandon oracle that falsifies the dissolution property the whole design is argued from.\n\n---\n\n# IF REJECTED — WHAT THE NEXT DESIGN MUST BE BUILT AROUND\n\nThe per-token/per-attempt registry is the right shape and every lens said so; keep it. Three mechanisms, none of them a patch to a step:\n\n**1. SINGLE-WRITER ABANDON.** The depositing attempt's own coroutine is the **only** party in the process that may call `abandonBlob`. Every foreign-stack cleanup — burn, remote burn, TTL, read-burn, reveal, discard, `clearAll` — atomically *marks* the entry and returns no-abandon; the owner reads its own mark at whichever exit it reaches and acts. This is one bit, not a fourth lifecycle state, and it is the reframing that dissolves five separate findings at once: the named window (no foreign abandon can exist inside it), BLOCKING 4 (a relay-forged `message.burned` cannot destroy anything), arithmetic F3, threat B3, and the \"guards, not dissolution\" honesty defect. It also gives the design a true structural property in place of the false one: *the only code that can destroy a blob is the code that created it.*\n\n**2. RECLAIM INSIDE THE TEARDOWN ORDER, NOT OUTSIDE THE SCOPE.** v3 asked \"which scope survives the lock?\" — the wrong question, because the vault is closed at `UnlockController.kt:126` before the scope is cancelled at `:135`, and the bearer token is read live per call. The right question is \"**what runs while the vault is still open?**\" The tree already answers it: `coordinator.stop()` runs first, inside `stopSession`, and already contains the pattern — `coverWorker.runTerminalConfined(::coverTeardown)` (`:869`), a bounded dispatch-and-block on the confined worker — and `lockCurrent` already has a bounded drain (`runBlocking { withTimeoutOrNull(drainTimeoutMs) }`, `:147-149`). A terminal reclaim flush that rides that existing bound needs no `NonCancellable`, no process scope, no captured credential, and no duress gate — which also disposes of BLOCKING 12 entirely, because nothing uncancellable is ever created.\n\n**3. NAME THE STATE FOR AN OBSERVABLE, AND MAKE \"NEVER ABANDON\" CONDITIONAL.** The third state may assert only what `send() == true` proves: a frame naming this token entered a buffer we cannot recall. Relay *confirmation* (`message.stored` / `message.delivered`) is a separate, later fact, and it must be allowed to **resolve** the entry — which is the narrowing step 5 needs (\"relay frames may not cause an ABANDON\" instead of \"relay frames may not touch entries\"), and it is what makes the enqueued-but-never-written orphan reclaimable and stops the map growing for the session. Anything the client cannot observe must not appear in a state name again.\n\nCarried requirements for v4, not mechanisms: cite anchored text rather than line ranges, against HEAD, verified by the author before review (three versions in a row have failed this); keep `AttachmentDepositReuseTest`'s two 1-arg tests and replace the release-point counting tripwire with one that counts the new exits; give `abandonBlob` a call deadline; enumerate exits of `deliverAttachment` (six, including `:1397`) separately from exits of `publishOutgoing` (three); and state the residue against the **deployed** 168 h, not the branch's 96 h."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design review"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesis"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "v3:window",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a06a1583d49fe3a62",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785402892123,
      "queuedAt": 1785402885564,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v3.md in full, then READ THE SOURCE IT CITES AND CHECK ITS\nCLAIMS. Do not review the plan's reasoning — review reality against it. v1 of this plan asserted a\ndefect that did not exist and made it the ordering constraint; v2 asserted …",
      "lastProgressAt": 1785403546780,
      "tokens": 107104,
      "toolCalls": 26,
      "durationMs": 654657,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — `HANDED_OFF`'s trigger is factually wrong; the plan's central definition mis-states `ws.sendMessage`'s contract.** Plan \"THE MECHANISM\" line 28 / rule 2, vs `net/WsClient.kt:176-177` → `:222-223` (`webSocket?.send(frame.toString()) ?: false`).\n`ws.sendMessage` returning `true` is an OkHttp **enqueue** receipt, not relay acceptance. The tree says so in three places: `WsClien…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "v3:composition",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a5e70159e9d890939",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785402896903,
      "queuedAt": 1785402885564,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v3.md in full, then READ THE SOURCE IT CITES AND CHECK ITS\nCLAIMS. Do not review the plan's reasoning — review reality against it. v1 of this plan asserted a\ndefect that did not exist and made it the ordering constraint; v2 asserted …",
      "lastProgressAt": 1785403578880,
      "tokens": 115914,
      "toolCalls": 39,
      "durationMs": 681977,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — plan rule 3 / step 1 / step 9 (`MessagingCoordinator.kt:1483`, `net/ApiClient.kt:262-274`)**\nRule 3 says \"`RESERVED` means nothing landed, so nothing to abandon.\" Reality: `RESERVED` means *we do not know whether it landed*.\nSequence: attachment send draws token T, entry set `RESERVED`, `api.uploadBlob` POSTs 8 MiB over Tor; the server's `StoreBlob` commits the row (`store.…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "v3:routes",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a52df9db0d0b8b797",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785403552380,
      "queuedAt": 1785402885564,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v3.md in full, then READ THE SOURCE IT CITES AND CHECK ITS\nCLAIMS. Do not review the plan's reasoning — review reality against it. v1 of this plan asserted a\ndefect that did not exist and made it the ordering constraint; v2 asserted …",
      "lastProgressAt": 1785404261677,
      "tokens": 132353,
      "toolCalls": 45,
      "durationMs": 709296,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — plan rule 3 / step 1 (\"RESERVED means nothing landed, so nothing to abandon\") — REGRESSES the disk metric item 5 exists to bound, exactly as v1 did**\nRESERVED is not \"nothing landed\"; it is \"we don't know\". Sequence: user sends an 8 MiB photo → `deliverAttachment` sets RESERVED → `api.uploadBlob` (`MessagingCoordinator.kt:1428`) writes the whole ~11 MiB base64 body → the re…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "v3:arithmetic",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "ae430045fd80d27a7",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785403583432,
      "queuedAt": 1785402885564,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v3.md in full, then READ THE SOURCE IT CITES AND CHECK ITS\nCLAIMS. Do not review the plan's reasoning — review reality against it. v1 of this plan asserted a\ndefect that did not exist and made it the ordering constraint; v2 asserted …",
      "lastProgressAt": 1785404526619,
      "tokens": 145570,
      "toolCalls": 42,
      "durationMs": 943186,
      "resultPreview": "I have read the plan and verified its claims against source. Findings below.\n\n# FINDINGS\n\n**F1 — P1 — plan §MECHANISM rule 3 + Step 1 (`RESERVED` ⇒ \"nothing landed, so nothing to abandon\"), against `MessagingCoordinator.kt:1343-1347,1428,1483`.**\nSequence: user sends an 8 MiB photo on a lossy circuit. `api.uploadBlob` (`:1428`) writes the whole body, the relay's `INSERT` commits, the response is l…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "v3:threat",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a4352711c28573483",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785404266423,
      "queuedAt": 1785402885564,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/test && grep -rln \"Me…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v3.md in full, then READ THE SOURCE IT CITES AND CHECK ITS\nCLAIMS. Do not review the plan's reasoning — review reality against it. v1 of this plan asserted a\ndefect that did not exist and made it the ordering constraint; v2 asserted …",
      "lastProgressAt": 1785405018919,
      "tokens": 132189,
      "toolCalls": 40,
      "durationMs": 752496,
      "resultPreview": "# FINDINGS\n\n**B1 — P1 — plan step 3 (+ step 7) / `net/ApiClient.kt:292-295`, `net/CertificatePinning.kt:74,130`**\n`abandonBlob` calls `execute(post(...))` with **no `callTimeoutMs`**, and *both* OkHttp clients set `readTimeout(0, MILLISECONDS)`. `execute` is a `suspendCancellableCoroutine`; today the only thing that ever reaps a wedged abandon is `invokeOnCancellation { call.cancel() }` (`ApiClien…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesis",
      "phaseIndex": 2,
      "phaseTitle": "Synthesis",
      "agentId": "a306e13ec2e093e61",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785405025076,
      "queuedAt": 1785405020767,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/server; grep -n \"blobs\" cmd/server/main.go",
      "promptPreview": "READ-ONLY. Five lenses reviewed DESIGN PLAN v3 at l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v3.md before any code was written. Its two\npredecessors were both rejected DO NOT SHIP. Reports follow verbatim.\n\nSYNTHESIS, not re-review. ADJUDICATE AGAINST SOURCE — where lenses disagree, say which claim is\ncheckable and what the source says. Produce:\n\n# BLOCKING\nRanked most severe f…",
      "lastProgressAt": 1785405593143,
      "tokens": 118455,
      "toolCalls": 31,
      "durationMs": 568066,
      "resultPreview": "# BLOCKING\n\nRanked most severe first. Line numbers below are HEAD (`31770972`) of `feat/0.10.2-capacity-fixes`; every one was read at source.\n\n**1 — `HANDED_OFF` is defined by a fact the client cannot observe, and rule 3 turns that into a permanent orphan. (window F1, routes F3, arithmetic F2, composition claim-check — 4 lenses, AGREE; threat did not contest.) CONFIRMED.**\n`WsClient.sendMessage` (…"
    }
  ],
  "totalTokens": 751585,
  "totalToolCalls": 223
}