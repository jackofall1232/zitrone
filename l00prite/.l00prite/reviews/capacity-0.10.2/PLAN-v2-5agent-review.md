{
  "summary": "Second five-agent DESIGN review — plan v2 after v1 was rejected DO NOT SHIP",
  "agentCount": 6,
  "logs": [],
  "result": {
    "lenses": [
      "composition",
      "arithmetic",
      "race",
      "enforcement",
      "residual"
    ],
    "synthesis": "Source root for all Android paths below: `/root/zitrone/apps/android/app/src/main/java/com/zitrone/app/` (`MessagingCoordinator.kt`, `MessageRepository.kt`, `UnlockController.kt`, `crypto/vault/VaultSession.kt`, `net/`); server paths under `/root/zitrone/server/internal/`. Plan under review: `/root/zitrone/l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md`.\n\n# BLOCKING\n\n**B1 — Steps 4 and 5 are mutually unimplementable, and the only resolution step 4 permits destroys a delivered attachment. ALL FIVE LENSES, INDEPENDENTLY, P1. AGREE completely.**\nStep 4 (plan:49-53) makes `published` a coroutine-local `var` in `deliverAttachment`'s frame; step 5 (plan:60) makes the terminal predicate `memo present && !published`. Every step-5 trigger runs on a foreign stack: `burnAll` on the UI thread (`MainActivity.kt:1677`), remote burn on the OkHttp reader thread (`WsClient.kt:258→302` → `MessageRepository.kt:307`), TTL/reveal burns on the repository scope (`MessageRepository.kt:354,340`), contact-delete/`clearAll` on `confined`. Dropping `!published` abandons the blob of an envelope already accepted by the relay (`MessagingCoordinator.kt:484-488`) while the memo is still held — released only at `:2285`/`:2297`, and the relay is conceded, so it may deliver the envelope and withhold `message.stored` indefinitely: recipient's redeem 404s and `attachmentUnavailable` is terminal (`:2247-2262`, `MessageRepository.kt:196-215`). Restoring `published` to shared state reintroduces verbatim the lifetime bug step 4 exists to kill. **Required change: delete both steps as written.** Four lenses independently converged on the same replacement (see CONVERGENCE §1) — a per-**token** registry entry carrying `RESERVED/DEPOSITED → HANDED_OFF`, transitioned by the sending coroutine itself immediately *after* `ws.sendMessage` returns true (R-U3-1 forbids work *between* the barrier and `ws.sendMessage`, not after it), with every abandon being the return value of an atomic `ConcurrentHashMap.remove/compute`. Cleanup abandons only `DEPOSITED`.\n\n**B2 — `releaseDeposit` stays relay-driven and attempt-blind; the guard-succeeding case is a relay-paced orphan generator plus duplicate delivery. RACE (C2), P1. No other lens found this. Checkable.**\nStep 4 fixes only the guard-*rejected* case. `onMessageStored` (`:2281-2286`) runs on the reader thread with no proof we sent that id, and `releaseDeposit` keys on `messageId`, not on the attempt owning the entry. Retry → `retryable` FAILED→SENDING (`MessageRepository.kt:142-147`) → attempt 2 suspended in `uploadBlob` → relay emits `message.stored` → `markSent` precondition `state == SENDING` **passes** → attempt 2's entry is removed mid-upload → its blob is invisible to all cleanup, and step 6's guard (now `SENT`, not BURNING) passes and hands a second envelope for the same id to the relay. **Required change:** entry removal must be owned by the attempt that created it (per-token keys), and must never be triggered by a relay-controlled frame carrying no attempt identity. The B1 registry is a precondition, not a substitute.\n\n**B3 — Step 7's \"non-shared client\" is a deanonymisation defect. COMPOSITION (C3) + ENFORCEMENT (F2), INDEPENDENT, P1. AGREE.**\n`ApiClient` tags every request with the live `@Volatile Transport` (`ApiClient.kt:52-64,435`); the transport client is what carries Tor SOCKS (`CertificatePinning.kt:79`) or the I2P `I2pConnectSocketFactory` + loopback `Dns` override (`:121-128`) plus the pinner. A freshly built client egresses the abandon POST over the default network — the device's real IP hitting the relay seconds after an attachment send, time-correlated, against a conceded relay — or, on I2P, cannot resolve `.b32.i2p` at all, so abandon never works and every orphan waits out 96 h. It also survives transport swaps and keeps dialling a dead transport. **Required change:** `transport.client.newBuilder().callTimeout(...)`, re-derived on every swap; never an independently built client.\n\n**B4 — Steps 1 and 3 must be ONE commit. COMPOSITION (C2), P1. Uncontested; nobody argues the opposite.**\nStep 1 has its own merge gate (plan:31-32) and step 3 lands cleanup. In that window reuse is gone but the memo write is still `if (memo == null)` (`:1337-1339`), so the memo pins attempt 1's token while attempts 2..N draw fresh ones, and route (d) (`:490-494`) returns false without throwing so no abandon fires at all: N retries of one 8 MiB attachment → N unreclaimable orphans where the current tree caps at 1. **That is v1's rejected regression, reproduced by the step boundary.** Step 3 cannot go first either (abandon + stable blobId is the original data-loss composition). One commit.\n\n**B5 — The DEPOSIT has no timeout; that, not \"a frozen process\", is the live unbounded-orphan mechanism. RACE (C3) + RESIDUAL (F5), INDEPENDENT, P2→P1 by consequence. AGREE.**\nBoth clients set `readTimeout(0)` (`CertificatePinning.kt:74,130`) and no `callTimeout` anywhere in the app, and the same instance serves every REST call including `uploadBlob` (`ApiClient.kt:262-267,447-486`). Half-open circuit after the 8 MiB body is written → `suspendCancellableCoroutine` never resumes → no throw, therefore **no `markFailed`, no route-(c) abandon, no memo release**; bubble stuck SENDING forever, so `retryable`'s CAS refuses it and the user cannot even retry. Step 7 gives `callTimeout` only to the abandon. **Required change:** a `callTimeout` on the deposit call (per-call, via `newBuilder()` on the transport client — same mechanism as B3), and say so in step 3's orphan argument.\n\n**B6 — The teardown exclusion rests on a false premise; the real blocker is session-scope cancellation, which also voids step 7's queue and route (c). ARITHMETIC (F2, claim 2) + ENFORCEMENT (F5, WRONG-2) + RESIDUAL (F3, claim 2), INDEPENDENT, P1/P2. AGREE.**\nSee WRONG CLAIMS §2. Consequences the plan does not admit: (i) `abandonBlobQuietly` is `scope.launch` (`:429`) on the per-unlock session scope (`ZitroneApp.kt:951 → :1459 → :1886`) which `UnlockController.lockCurrent()` cancels (verified at `UnlockController.kt:134-135`), so step 7's single-flight queue and every 429-retry are silently discarded at the next lock; (ii) route (c) rethrows `CancellationException` at `:1469` *before* `markFailed` and the abandon, so **coroutine cancellation is an unenumerated fifth route** — the plan's set is (a)(b)(c)(d) only; (iii) idle auto-lock (`ZitroneApp.kt:983-996`) is the highest-frequency teardown in the product, adversary-free, and orphans a full 8 MiB blob on every in-flight photo send. **Required change:** enumerate cancellation as route (e); run cleanup on a process-scope `NonCancellable` issuer gated off for duress; restate the exclusion **path by path** — deniability is sound for duress/revoke/account-delete only, not for lock/logout.\n\n**B7 — Step 5 names routes but no hook, and the only existing hook is `notifyPeer`-gated. RACE (C5) + ENFORCEMENT (F3) + RESIDUAL (F2), INDEPENDENT, P2. AGREE.**\n`MessageRepository.onMessageBurned` (`:55`) is invoked at `:289` only `if (notifyPeer)`. TTL burn (`:354`), remote burn (`:307`) and the **vault contact delete** (`MessagingCoordinator.kt:1727`) all pass `notifyPeer = false` — and the vault path is the default on every shipped install. `clearAll` (`:311-319`) swaps in `emptyMap()` and iterates nothing. **Required change:** name the attachment point (a call in `burn`/`remove` independent of `notifyPeer`, explicit iteration in `clearAll`), not just the route list. Also fix the enumeration: `messages.clearAll()` has exactly two callers, `:2383` and `:1935`, **both inside step 5's own teardown exclusion** — step 5 is self-contradictory as written (RESIDUAL #6).\n\n**B8 — Step 3's own merge gate (\"a written bounded-orphan argument\") is unsatisfiable client-side. ARITHMETIC (F3), P1, and it is arithmetic, so it is checkable.**\n`BlobEffectiveCap` = 8,454,180 B; 16.37 GiB free ⇒ 2,079 orphans; TTL 96 h = 5,760 min ⇒ the box fills at **0.361 orphans/min = 520/day globally across all users**. The plan's accepted residuals (crash, frozen continuation, all five teardown paths, every 429'd abandon, route (e)) each feed that with no bound, and nothing bounds relay blob bytes server-side. **Required change:** either add a server-side bound (per-account blob-bytes cap, total-bytes cap, or a shorter unredeemed-blob TTL) or record 0.361/min as the accepted residual explicitly. Do not let the gate be signed off on prose.\n\n**B9 — Step 7 must move before steps 3 and 5, or cleanup traffic damages real messages. COMPOSITION (C4) + RACE (C4), INDEPENDENT, P2. AGREE on mechanism, DIFFER on victim — both are real.**\nOne 60/min `blobLimit` shared by deposit, redeem and abandon (`blobs.go:93,152,172`), fixed-window (2× boundary burst), and on the overlay routes `clientKey` falls back to the socket peer (`clientkey.go:37-55,107-128`) so it collapses across *all* onion users. Step 3's headline addition is route (d), the *bursty* failure: one socket drop emits M abandons at once where today it emits none. Composition's victim: another user's real send 429s → `markFailed` → another abandon (starvation feedback). Race's victim, worse: a concurrent **inbound** `redeemBlob` 429s → placeholder flips to permanently UNAVAILABLE (`:2258-2261`, `MessageRepository.kt:196-201`). **Required change:** step 7 precedes 3 and 5; steps 3/5 route through the queue explicitly; give redeem 429-retry (or exempt it) before adding abandon traffic to the bucket.\n\n**B10 — Step 4's mandatory `runCatching` around `cover` reds a U3 security tripwire and swallows cancellation. ARITHMETIC (F4) + COMPOSITION (C5) + ENFORCEMENT (F9), INDEPENDENT, P2. AGREE; arithmetic alone found the tripwire.**\n`DecoySendPairingTest.kt:1439-1451` greps main sources for `Regex(\"if\\\\((publishOutgoing|publishReceipt)\\\\([^()]*\\\\)\\\\) coverTraffic\\\\.cover\\\\(\")` and asserts `guarded == total == 3`. Wrapping at `:1241/:1467/:1623` gives `guarded = 0` ⇒ suite red on a tripwire the plan never enumerates, and the cheapest repair retires the only enforcement that every decoy is conditional on a genuine handoff — the U3 round-4 defect. Separately, `cover` is `suspend` and parks in `sleep(gapMs())` (`DecoySendPairing.kt:584`), so the only exception the wrapper realistically catches is `CancellationException`; a bare `runCatching` resurrects work past teardown, contrary to every other swallow in the file (`:431,:1243,:1469,:1764,:1913,:2606`). **Required change:** update the tripwire regex deliberately in the same commit, and specify `.onFailure { if (it is CancellationException) throw it }`.\n\n**B11 — Step 6 needs a NEW repository method and must not re-specify `exists`. COMPOSITION (C8) + ENFORCEMENT (F4b) + RESIDUAL (#1), INDEPENDENT, P2. AGREE.**\n`exists` already has a caller: `settlePostAck`'s staleness gate at `MessagingCoordinator.kt:581`. Re-specifying it as `present && state != BURNING` changes that inbound path's behaviour during the 600 ms dissolve (skipped notification, skipped blob redemption). Additionally `find` is `private` and `Message.state` is exposed by no public predicate, so the new semantics **cannot be expressed through `exists` at all**. **Required change:** add a second, indexed predicate; leave `exists` alone.\n\n**B12 — Step 1's blanket test deletion leaves the property it exists to create with zero enforcement. ENFORCEMENT (F7) + COMPOSITION (C6) + RESIDUAL (#4), INDEPENDENT, P2. AGREE. See CONFLICTS §1 for arithmetic's dissent.**\n`AttachmentDepositReuseTest.kt:28-36` (\"a fresh encrypt draws new secrets every time\") and `:91-105` (\"the token is never derivable from anything the relay sees\") pass unchanged after the reversal and are the only behavioural pins on the property step 1 establishes; the compiler enforces only that no reuse *parameter* exists, not that the token is a random draw. A later `token = sha256(messageId||salt)` compiles clean, breaks no test, and hands the relay the redemption capability — the exact thing `AttachmentCrypto.kt:94-97` exists to prevent. `AttachmentDepositWiringTest:150-159` also pins the release-point count (3: `:481`, `:2285`, `:2297`), which steps 3 and 5 change. **Required change:** keep `:28-36` and `:91-105`; **rewrite** `AttachmentDepositWiringTest` inverted (assert the reuse strings are absent, update the release-point count); delete only the four reuse tests.\n\n**B13 — Step 2's \"409 = idempotent success\" hands the conceded relay a fabricated-delivery channel. RESIDUAL (F6), P3 severity, P1 disclosure. Uncontested.**\nA relay that answers every `POST /api/v1/blobs` with `409 blob_exists` and stores nothing gets the client to publish the envelope, ack it, and render two ticks from retained sender bytes, while the recipient 404s forever and silently. Today the same behaviour yields an honest FAILED bubble with retry. **Required change:** either gate 409-as-success on having seen a 201 for that token earlier in the same attempt, or write the acceptance into the residual list — the current residual line (\"can delete or 404 any blob\") does not cover *converting a refused deposit into a positive delivery*.\n\n# CONVERGENCE\n\nIndependent agreement, strongest first.\n\n1. **The B1 defect AND its fix. Five lenses found the defect; four independently proposed the same replacement mechanism.** Race C1, Enforcement F6, Residual's closing answer, and Arithmetic F1/F6 all arrive at: per-attempt/per-token registry entry, an explicit handoff state written by the sending coroutine after `ws.sendMessage` returns true, and abandon-as-the-return-value-of-an-atomic-removal so single-flight per token is free on any thread with no lock and no ordering. Four lenses reasoning from different priors and landing on one mechanism is the strongest design signal in this review — **adopt it, do not re-derive it.**\n2. **`messages.exists` is O(N), not O(1) — all five lenses, all citing `MessageRepository.kt:363-366`.** The plan's *only* stated justification for placing the guard between the durability barrier and `ws.sendMessage` is false.\n3. **The 401-ordering justification for the teardown exclusion is false on `stop()` and account-delete; the real blocker is session-scope cancellation — three lenses independently, with the same corrected fact** (`:848-869` clears neither; `:1908` says auth is not cleared; `UnlockController.kt:135` cancels the scope — I re-verified this line myself).\n4. **The deposit `RequestBody` is NOT one-shot — four lenses independently answered step 2's open question the same way** (`ApiClient.kt:442-444`, String-backed ⇒ `isOneShot() == false`, so `retryOnConnectionFailure(true)` will replay the POST). Step 2's transport-replay premise holds and the 2^-256 deletion is correct.\n5. **The `notifyPeer`-gated hook (B7) — three lenses, same three unreachable routes, same conclusion that the highest-frequency route in the product is one of them.**\n6. **Coroutine cancellation is an unenumerated route and no fix inside the plan's contract can reach it — three lenses** (`:1469` rethrows before `markFailed` and the abandon; `abandonBlobQuietly` launches on the cancelled scope).\n7. **U3's cover-traffic confinement reasoning comes back CLEAN — two lenses checked it independently and both report a positive check, not an absence** (race's sweep; residual's sweep). Both cite that U3's statements are explicitly *slice*-scoped (`MessagingCoordinator.kt:362-366`, `CoverTrafficWorker.kt:26-30`) and compiler-backed by `publishOutgoing`/`publishReceipt` being non-suspending `private fun`s. The plan's suspicion (plan:117) is not borne out.\n8. **Step 8's \"1 week\" sweep is incomplete — four lenses, converging on a union of sites** beyond the one named: `AttachmentCrypto.kt:23`, `server/internal/api/blobs.go:135`, `config.go:87`, `:123-124`, `:143-144`, `docs/SECURITY_MODEL.md:1042`, `packages/protocol/src/attachments.ts:12,45`, `packages/crypto/src/attachments.ts:14`.\n9. **A large body of the plan's facts was independently re-verified by 3–5 lenses each and is CORRECT** — route (d) returns false without throwing; the whole 409 chain (`store.go:283-293` → `blobs.go:115-120` → `blobs_test.go:310-316` → `ApiClient.kt:258`); `retryable`'s CAS at `:142`; `markFailed` accepting SENDING||SENT; `burn()` flipping BURNING before the hook; `burnAll` from the UI thread at `MainActivity.kt:1677`; unconditional `releaseDeposit` at `:2285/:2297` from the reader thread; one shared 60/min `blobLimit`; the 150 ms floor and blocking `flushBeforeAck`; `limitedParallelism(1)` serialising slices. **v2 does not repeat v1's fabrication.**\n\n# WRONG CLAIMS\n\n1. **`messages.exists(messageId)` is \"O(1), synchronous RAM read (R-U3-1-clean)\" (plan:71-72). FALSE — five lenses.** `MessageRepository.exists` (`:363`) delegates to `private fun find` (`:365-366`): `_messages.value.values.asSequence().flatten().firstOrNull { it.id == messageId }` — **O(total live messages across all conversations)**, with a fresh Sequence/flatten allocation and a lambda per element, per real send, growing with history. There is no message-id index; the map is `conversationId → List<Message>`. It neither blocks nor suspends, so R-U3-1's *letter* survives — but the clean judgement was bought with a false fact and must be re-argued or the guard indexed. Second error in the same sentence: `find` is private and `state` is not publicly exposed, so `present && state != BURNING` cannot be expressed through `exists` at all.\n2. **\"`api.clearTokens()` runs before the queued `messages.clearAll()` (`:2372` vs `:2383`), so every abandon 401s\" offered as the reason for excluding teardown / lock / logout / revoke / account-delete (plan:66-69). The line numbers are right; the reason is FALSE for three of the five paths — three lenses.** That ordering exists only in `onSessionRevoked`. `stop()` (`:848-869`) clears neither tokens nor messages. The account-delete path clears messages at `:1935` and `:1908` states explicitly \"Auth is **NOT** cleared anywhere in this flow now\" — an abandon there would be authenticated and would succeed. Correct fact: abandon fails on those paths because the session scope is cancelled (`UnlockController.kt:134-135`, verified) and route (c) rethrows `CancellationException` first (`:1469`). Same \"TTL only\" conclusion, entirely different cause — and a maintainer who \"fixes the ordering\" gets zero abandons and no test to tell them.\n3. **\"Delete **both** test classes … **they** call `encrypt(plain, first.token, first.key)`, so leaving them breaks the test source set's compilation\" (plan:26-29). HALF FALSE — three lenses.** Only `AttachmentDepositReuseTest` calls the 3-arg overload (`:42,57,70,84,87`). `AttachmentDepositWiringTest` (`:120-160`) calls no `encrypt` at all — it reads `MessagingCoordinator.kt` as **text** and asserts substrings, so it compiles fine and fails at *runtime*, on the coordinator edit rather than the crypto edit. Two tests in the first class (`:28-36`, `:91-105`) also compile and pass unchanged (B12).\n4. **`store.go:307-317`'s `AbandonBlob` rationale described as \"the same undercount as B1\" (plan:86). It is also an OVERCLAIM — composition claim 4.** `:310-314` names three routes, one of which (\"a contact deleted mid-send\") is not wired at all: `publishOutgoing`'s contact-deleted exit (`:477-483`) calls `releaseDeposit` but never `abandonBlobQuietly`. Step 8 catches this overclaim for the `ApiClient` kdoc and misses it here.\n5. **Enumeration/offset errors, minor but they are the plan's own citations.** `AttachmentCrypto.kt` reuse rationale is `:73-98`, not `:74-99` (`:99` is `fun encrypt(`); the `require((reuseToken == null) == (reuseKey == null))` guard and rationale at `:105-110` are not in the deletion set; the `AttachmentDeposit` kdoc is `:381-399`, not `:380-398`; step 5's `burnAll` enumeration names only `MainActivity.kt:1677` and misses the two *inside* the coordinator, `:1727` (`notifyPeer=false`, vault default) and `:1810` (`notifyPeer=true`, legacy); step 5 lists `clearAll` in scope while both its callers sit inside step 5's own exclusion.\n6. **Non-fact, but a wrong attribution:** the plan blames the unbounded upload→send window on \"a frozen process resuming a continuation a day later\" (plan:92-94). The live mechanism is `readTimeout(0)` + no `callTimeout` on the deposit itself (B5), on a fully awake process.\n\n# CONFLICTS\n\n1. **Step 1's compilation claim — Arithmetic vs three lenses. Decidable by grep; the three are right.** Arithmetic states \"both reuse test classes … and `AttachmentCryptoTest.kt` does not use the reuse params, so step 1's compilation claim is exactly right\"; it checked `AttachmentCryptoTest.kt` and did not check `AttachmentDepositWiringTest`. Composition, Enforcement and Residual all opened `AttachmentDepositReuseTest.kt:120-160` and found no `encrypt` call, only source-string assertions. Check: `grep -n 'encrypt' AttachmentDepositReuseTest.kt` and look at which class the hits fall in. Resolution: proceed on B12.\n2. **Does cleanup need to be single-flight for correctness? Four lenses, four positions, and they reconcile.** Composition: neither a set nor single-flighting helps, because the defect is not ordering. Arithmetic F6: single-flight is **not** needed for correctness — abandon is idempotent and token-keyed, so a double abandon is harmless; the pre-overwrite clause is redundant once step 3 gives every attempt its own exit, and keeping both mechanisms is what *creates* the race. Race: necessary but insufficient. Enforcement: not the missing piece. **Reconciliation (all four are compatible):** with fresh-per-attempt tokens no two attempts share a token, so the only unsafe abandon is one against a *handed-off* attempt — excluded by the state field, not by mutual exclusion. Single-flight therefore survives only as **rate-shaping** for B9, and step 3's \"abandon the prior token before overwriting\" clause should be **deleted outright**. This is the direct answer to plan:101-110: there is no ordering to find; make the registry the arbiter.\n3. **Does step 5 have any sound non-empty case? Composition says NO; Enforcement, Race and Residual say yes-but-only-one. Decidable by construction.** Composition C1 is correct *under the plan as written*: once step 3 abandons on every exit, memo-present ⟺ live-or-published, and the only ownerless-memo case is cancellation, which step 5 excludes. Enforcement F6 identifies that same single case and Residual F3 shows it is the *most frequent* teardown in the product (idle auto-lock). Resolution: step 5 becomes non-empty and sound **iff** it (a) predicates on `DEPOSITED` from the B1 registry and (b) runs on a scope that survives session cancellation (B6). Without both, composition is right and step 5 adds only risk.\n4. **Verdicts split 3–2** (DO NOT SHIP: composition, enforcement, residual; SHIP WITH CHANGES: arithmetic, race). This is a disagreement about remedy size, not about facts — all five list the same B1 as P1. Arithmetic's own reasoning undercuts its verdict: it calls steps 4 and 5 \"mutually unimplementable\" and step 3's merge gate \"unsatisfiable\", which is not a change-set, it is a rewrite.\n5. **Not a conflict, worth flagging:** only Arithmetic quantified B8 (0.361 orphans/min). No lens contradicted it, and the underlying constants are checkable (`store.go:313-314`, `config.go:125,146,172`). Treat as verified-by-arithmetic, and re-derive before signing the step-3 gate.\n\n# PLAN CHANGES\n\nOrdered edits. **Step order must change again**, in two places: **steps 1+3 merge into one commit** (B4) and **step 7 moves before steps 3 and 5** (B9).\n\n1. **New step 0 (server, first):** decide the server-side blob-bytes bound demanded by B8 — per-account cap, global cap, or shortened unredeemed TTL — or write 0.361 orphans/min into the residual list as accepted. Nothing downstream can meet its own gate without this.\n2. **New step 1 (was 7):** `abandonLimit` split from `blobLimit`, with the overlay-collapse and fixed-window-2× caveats recorded; client-side cleanup queue built as `transport.client.newBuilder().callTimeout(...)` **re-derived on every transport swap** (B3), hosted on a **process scope under `NonCancellable`**, duress-gated (B6); redeem gets 429-retry before abandon traffic joins the bucket (B9).\n3. **New step 2 (was 1+3, ONE commit):** delete `reuseToken`/`reuseKey` and the coordinator wiring; delete only the four reuse tests, keep `:28-36` and `:91-105`, rewrite `AttachmentDepositWiringTest` inverted with an updated release-point count (B12); replace the single-slot memo with the **per-token registry** (`messageId → Map<token, entry{state}>`), keyed per attempt, with `RESERVED` written **before** the deposit request is issued; enumerate routes (a)(b)(c)(d) **and (e) cancellation**; every `false` exit and every cancellation unwind abandons **its own** token via atomic removal, at the **call site** of `publishOutgoing` (`:1467`), not inside it — `publishOutgoing` is a U3-audited surface shared with `deliverText` (`:1241`) and `publishReceipt`. Capture is `var tokenB64: String? = null` (a `val` outside the lambda cannot be assigned from inside it — composition C7); mirror `var stage` at `:1328`. **Delete the \"abandon the prior token before overwriting\" clause** (CONFLICTS §2). Add the `callTimeout` on the deposit (B5). Write the bounded-orphan argument against step 0's bound, and state that the abandon+release tail stays suspension-free (residual's note on `retry` at `:1493`).\n4. **New step 3 (replaces 4+5):** delete the coroutine-local `published` and the `memo present && !published` predicate. The sending coroutine transitions its own entry `DEPOSITED → HANDED_OFF` immediately **after** `ws.sendMessage` returns true. Terminal cleanup abandons **only `DEPOSITED`** entries, via atomic removal. Fix `releaseDeposit` to be attempt-owned and never relay-triggered (B2), and constrain `markFailed` to `SENDING` only — with the `SENT→FAILED` test in the same commit (`MessageRepositoryTest.kt` exists; nothing pins it today). Name the burn hook explicitly, `notifyPeer`-independent, plus explicit iteration in `clearAll` (B7); drop `clearAll` from the route list or drop it from the exclusion, not both. Restate the teardown exclusion path by path: deniability for duress/revoke/account-delete; lock/logout are **fixed**, not excluded.\n5. **New step 4 (was 6):** a **new indexed** predicate, not a re-spec of `exists` (B11); re-argue R-U3-1 without the O(1) claim, or index the lookup.\n6. **New step 5 (was 4's cover clause):** wrap `cover` at all three sites with the `CancellationException` rethrow, and update `DecoySendPairingTest.kt:1439-1451`'s regex in the same commit (B10).\n7. **New step 6 (was 2):** record the 409 contract, with the not-one-shot fact stated as verified and the relay-fabricated-delivery residual either gated or accepted in writing (B13).\n8. **New step 7 (was 8):** comment sweep, widened to the full \"1 week\" union (CONVERGENCE §8) plus `store.go:307-317`'s overclaim (WRONG §4), the `blobs.go` godoc block that has run \"No auth: possession of the one-time token is the entire capability\" into the *authenticated* `AbandonBlob` leaving `RedeemBlob` undocumented (enforcement F10), and the two confinement sentences in CONFINEMENT SWEEP below. Fix the citation offsets in WRONG §5.\n9. **Cross-cutting:** the cleanup contract for the (now five) `publishOutgoing` exits is comment-only in every version of this plan. Make it a `sealed` result type carrying the abandon obligation — enforcement F8 is right that this costs almost nothing and is the one change that moves the whole contract from \"asserted\" to \"compiler-enforced\" in the exact region where v1's defect and B1 both live.\n\n# CONFINEMENT SWEEP\n\nTwo lenses swept independently. **U3's cover-traffic reasoning is CLEAN, and that is a positive check** (CONVERGENCE §7): its load-bearing statements are scoped to a suspension-free *slice* (`MessagingCoordinator.kt:362-366`, `CoverTrafficWorker.kt:26-30`), enforced by `publishOutgoing`/`publishReceipt` being non-suspending `private fun`s (`:456-462`) so the compiler rejects a suspension there, and cross-suspension exclusion elsewhere uses real Mutexes (`withSessionLock`, `:349-352`; `DecoySendPairing`'s `teardown` Mutex, `:310,443-447`). `attachmentDeposits`'s own kdoc (`:381-399`) makes no confinement claim.\n\nTwo sites do carry the misunderstanding. I re-verified both myself:\n\n- **`VaultSession.kt` ~`:463` — \"Cannot occur under single-worker confinement anyway\"**, appended to the mid-flush-mutation case. Wrong twice: `flush` is reached from at least three threads (the coordinator's `confined` worker via `flushBeforeAck`, the background coalescing timer, and `UnlockController`'s lock thread via `runtime.close()`), and `persist(...)` sits between two separate `synchronized(stateLock)` blocks. **The code is correct** — the real guard is `stateLock` + `version`/`sealedVersion`, which I read at `:452-463`. The sentence is a latent trap: a future simplifier can delete the version check on its strength. **Delete the sentence.** (Residual F7.)\n- **`MessagingCoordinator.kt:177-178` — \"Both run on the `confined` worker … so they cannot interleave with a send's publish-then-pair slice.\"** False whenever `CoverTrafficWorker.runTerminalConfined`'s caller-thread fallback fires after `TERMINAL_TEARDOWN_WAIT_MS`. `CoverTrafficWorker`'s own kdoc says the fallback \"**structurally defeated the confinement argument, precisely when it fired**\" and documents that it was deliberately **kept** for the terminal path (key wipe must not hang) while being removed for the transport swap. Consequence, documented only for the swap case: a real frame on the wire with no decoy behind it, correlated with lock/backgrounding — the R-U3-3 observable round 3 removed. Bounded to the fallback window, on lock/logout/delete only, and the tradeoff is defensible; **the `:177-178` comment must be narrowed to say \"on the worker, except the terminal fallback\"**, and the residual recorded. (Residual F8.)\n\n**Nothing else in the tree rests on `limitedParallelism(1)` for cross-suspension exclusion** (race's sweep, corroborated by composition's spot checks). So this is a two-site comment defect, not a latent defect class — but both sites are in durability/anonymity paths and both read as guarantees.\n\n# VERDICT\n\n**DO NOT SHIP** — steps 4 and 5 are mutually unimplementable (all five lenses, independently), the only resolution step 4 permits abandons the blob of an attachment already accepted by the relay, and fixing it requires replacing both steps with a different mechanism plus reordering the commits again (1+3 merged, 7 first); the plan cannot be implemented as written and needs a v3."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design review"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesis"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "v2:composition",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "ab25be41e9df6b713",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785381063867,
      "queuedAt": 1785381058153,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "sed -n '540,600p' apps/android/app/src/main/java/com/zitron…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md in full, then read the source it cites to CHECK ITS\nCLAIMS AGAINST REALITY. The plan quotes many file:line facts — verify them; v1 of this plan asserted\na defect that did not exist and made it the ordering constraint.\n\nCONTEXT:…",
      "lastProgressAt": 1785381680870,
      "tokens": 130416,
      "toolCalls": 31,
      "durationMs": 617002,
      "resultPreview": "No files were modified. Findings below; every file:line was opened and checked on `feat/0.10.2-capacity-fixes` @ `f2bc2a1c`.\n\n# FINDINGS\n\n**C1 — P1 — steps 4 × 5 (the junction the author names as least-confident).** Step 4 makes `published` a coroutine-local `var` in `deliverAttachment`'s frame; step 5's terminal predicate is \"memo present && !published\". Every step-5 trigger runs on a foreign sta…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "v2:arithmetic",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a661de6181d41852e",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785381066204,
      "queuedAt": 1785381058153,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /root/zitrone/apps/android/app/src/main/java/com/zitrone…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md in full, then read the source it cites to CHECK ITS\nCLAIMS AGAINST REALITY. The plan quotes many file:line facts — verify them; v1 of this plan asserted\na defect that did not exist and made it the ordering constraint.\n\nCONTEXT:…",
      "lastProgressAt": 1785381724303,
      "tokens": 140583,
      "toolCalls": 48,
      "durationMs": 658099,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — steps 4 + 5 are mutually unimplementable: `published` is coroutine-local, the terminal predicate needs it off-coroutine.**\nStep 4 puts `published` in `deliverAttachment`'s frame (\"no other path can delete it\"). Step 5's predicate is `memo present && !published ⇒ abandon`, evaluated from burn (`MessageRepository.kt:274-296`, UI thread via `MainActivity.kt:1677`), remote burn…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "v2:race",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "aa1950421283461fd",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785381686153,
      "queuedAt": 1785381058153,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"1 week\" server/internal/api/blobs.go server/intern…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md in full, then read the source it cites to CHECK ITS\nCLAIMS AGAINST REALITY. The plan quotes many file:line facts — verify them; v1 of this plan asserted\na defect that did not exist and made it the ordering constraint.\n\nCONTEXT:…",
      "lastProgressAt": 1785382466181,
      "tokens": 119437,
      "toolCalls": 31,
      "durationMs": 780028,
      "resultPreview": "# FINDINGS\n\n**C1 — P1 — plan steps 4 + 5 (composition: the two steps name one variable that cannot be one variable)**\nStep 4 makes `published` a **coroutine-local `var` in `deliverAttachment`'s frame** (\"no other path can delete it\"). Step 5's terminal-cleanup predicate is **`memo present && !published ⇒ abandon`**, and every cleanup trigger runs on a *different* coroutine and thread than the send…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "v2:enforcement",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "a017e587fdad0eb8f",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785381729323,
      "queuedAt": 1785381058153,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"blob_exists\\|StatusConflict\\|409\\|Abandon\" /root/z…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md in full, then read the source it cites to CHECK ITS\nCLAIMS AGAINST REALITY. The plan quotes many file:line facts — verify them; v1 of this plan asserted\na defect that did not exist and made it the ordering constraint.\n\nCONTEXT:…",
      "lastProgressAt": 1785382372126,
      "tokens": 120726,
      "toolCalls": 43,
      "durationMs": 642803,
      "resultPreview": "I have verified the plan's cited facts against source. Findings below.\n\n# FINDINGS\n\n**F1 — P1 — plan steps 4 + 5 (composition; `MessagingCoordinator.kt:2280-2298`, `MessageRepository.kt:284-289`, `MainActivity.kt:1677`)**\nStep 4 makes `published` a coroutine-local `var` in `deliverAttachment`'s frame and explicitly forbids a \"unpublished\" predicate (\"Failure predicate reads **memo-absent ⇒ do noth…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "v2:residual",
      "phaseIndex": 1,
      "phaseTitle": "Design review",
      "agentId": "aed9c490033b72228",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785382377320,
      "queuedAt": 1785381058153,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "sed -n '1440,1460p' apps/android/app/src/main/java/com/zitr…",
      "promptPreview": "READ-ONLY. Do not modify, create or delete any file. Report only.\n\nReview a DESIGN PLAN, not code. Read l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md in full, then read the source it cites to CHECK ITS\nCLAIMS AGAINST REALITY. The plan quotes many file:line facts — verify them; v1 of this plan asserted\na defect that did not exist and made it the ordering constraint.\n\nCONTEXT:…",
      "lastProgressAt": 1785383076244,
      "tokens": 158720,
      "toolCalls": 49,
      "durationMs": 698923,
      "resultPreview": "# FINDINGS\n\n**F1 — P1 — steps 4 and 5 contradict each other; the resolution either abandons a blob whose envelope is already on the socket, or restores the defect step 4 exists to fix.**\nStep 4 mandates `published` be a **coroutine-local `var` in `deliverAttachment`'s frame** (\"no other path can delete it\"). Step 5 mandates the **terminal-cleanup predicate be `memo present && !published`**. Termin…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesis",
      "phaseIndex": 2,
      "phaseTitle": "Synthesis",
      "agentId": "a686f6fc331cbecfc",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785383081788,
      "queuedAt": 1785383078270,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "f=$(find ./apps/android -name CoverTrafficWorker.kt | head …",
      "promptPreview": "READ-ONLY. Five lenses reviewed DESIGN PLAN v2 at l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md before any code was written. Its\npredecessor v1 was rejected DO NOT SHIP for containing a composition defect that would have made the\nproblem worse. Reports follow verbatim.\n\nSYNTHESIS, not re-review. Produce:\n\n# BLOCKING\nMust be resolved before implementation, ranked most severe …",
      "lastProgressAt": 1785383312969,
      "tokens": 65203,
      "toolCalls": 7,
      "durationMs": 231180,
      "resultPreview": "Source root for all Android paths below: `/root/zitrone/apps/android/app/src/main/java/com/zitrone/app/` (`MessagingCoordinator.kt`, `MessageRepository.kt`, `UnlockController.kt`, `crypto/vault/VaultSession.kt`, `net/`); server paths under `/root/zitrone/server/internal/`. Plan under review: `/root/zitrone/l00prite/.l00prite/reviews/capacity-0.10.2/PLAN-item5-reversal-v2.md`.\n\n# BLOCKING\n\n**B1 — S…"
    }
  ],
  "totalTokens": 735085,
  "totalToolCalls": 209
}