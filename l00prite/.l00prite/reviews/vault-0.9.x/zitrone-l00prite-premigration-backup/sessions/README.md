# Session Logs

Store optional per-session logs in this folder when a run needs more detail than the ledger entry can hold.

Recommended naming:

- `YYYY-MM-DDTHH-MM-SSZ-agent-name.md`

Each session log should summarize goal, context read, actions taken, verification, failures, decisions, and the memory files updated before stopping.
