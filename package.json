{
  "name": "zitrone",
  "version": "0.9.0-beta",
  "private": true,
  "description": "Zero-knowledge, end-to-end encrypted ephemeral messaging app for browser, iOS, and Android",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter './packages/**' build && pnpm --filter @zitrone/web build && pnpm --filter @zitrone/website build",
    "build:packages": "pnpm -r --filter './packages/**' build",
    "dev:web": "pnpm --filter @zitrone/web dev",
    "dev:website": "pnpm --filter @zitrone/website dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19.2.0",
      "@types/react-dom": "^19.2.0"
    }
  }
}
