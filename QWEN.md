# l00prite protocol — Qwen Code context file

This project uses the l00prite protocol. Full agent instructions, loop prompts, and
persistent memory live in `l00prite/`. The complete operating rules are imported below
from `l00prite/AGENTS.md` — they are not optional context, they are the actual protocol.

@./l00prite/AGENTS.md
