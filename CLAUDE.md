# CLAUDE.md

This project uses the l00prite protocol. The project blueprint, full agent instructions,
loop prompts, and persistent memory live in `l00prite/`.

Start here: l00prite/CLAUDE.md (the project blueprint and protocol rules), then
l00prite/AGENTS.md (the full agent operating rules).

Do not duplicate instructions into this file. If your tool only reads repo root, open the
linked files next — they are not optional context, they are the actual protocol.
